# CLAUDE.md — Die V1

This file is the source of truth for building. It lives at C:\Users\figja\Die_v1\CLAUDE.md.

Read this file at the start of every session before doing anything else.

---


# PROJECT

Single HTML file (index.html) plus eleven plain JavaScript files under /js/, loaded via ordinary `<script src>` tags in a fixed order: config.js, state.js, listener-registry.js, audio.js, pipeline.js, cards-mods.js, run-and-map.js, phase-machine.js, rendering.js, dev-tools.js, bootstrap.js. No ES modules — file:// origins are null and module scripts are CORS-blocked, so this is a hard constraint, not a style choice. No build step. No npm. No server.

config.js (added BUILD 102) is the one constants file, loaded first, before state.js. Every tunable number and structural constant enumerated in the BUILD 102 prompt lives on one object, GAME_CONFIG — every other file reads it from there instead of repeating a literal. Its header comment carries the FACTS block (F01-F28) verbatim from the Notion "Die — V1" page, one line per fact beside the GAME_CONFIG field(s) that implement it — the one place the F-numbers live in code.

All eleven files share one global lexical scope, the same way one giant inline `<script>` block would. The only eager trigger anywhere in the codebase is `window.addEventListener('DOMContentLoaded', init)` in bootstrap.js — nothing calls a game function at parse time, so cross-file references are safe regardless of script tag order.

File: C:\Users\figja\Die_v1\index.html (loads the eleven js/ files above).
Open in browser to test. Double-click index.html only — never through a local server. /audio/ and /art/ exist as empty asset folders (created BUILD 091); nothing currently populates them — see AUDIO MODULE.

tests/mods.test.js (added BUILD 100) — a plain Node script (this project has no test runner installed, only the raw `playwright` library), run via `node tests/mods.test.js`. For each of config.mods' reward-eligible entries, dev-loads it onto a face and forces that roll through the real forcePlayerRoll()/MOD_TRIGGER dispatch, then asserts the exact numeric effect on gameState. Not loaded by index.html — a standalone verification script, not part of the eleven-file game bundle above.

tests/build101_enemy_roll.test.js (added BUILD 101) — same shape as tests/mods.test.js, run via `node tests/build101_enemy_roll.test.js`. Dev-jumps straight to the boss fight, force-rolls the enemy die's face 5 every round through round 4, dumps the resulting log lines, and screenshots the roll strip (tests/build101_roll_strip.png) right after a forced trigger. Not loaded by index.html.

tests/facts.test.js (added BUILD 102) — same shape, run via `node tests/facts.test.js`. Asserts every F-number (F01-F28) against GAME_CONFIG and against the running gameState/DOM after a fresh New Run, entering the opening fight or dev-jumping to the elite/boss where a fact needs live combat state. A mismatch between a documented fact and the live value is a failing test. Not loaded by index.html.

tests/autoplay.js (added BUILD 105) — a headless autoplayer, run via `node tests/autoplay.js`. Plays complete runs against real gameplay only (New Run, map node clicks, playCard(), nextPhase()'s own natural rolls, the real die-action/rite/card-reward panels) — never forcePlayerRoll()/forceEnemyRoll()/devJumpToSlot()/devLoadMod() — with a seeded Math.random (page.addInitScript, installed before any js/ file runs) so every roll and shuffle is reproducible from one seed. Fixed, documented-in-file policy: always the elite lane; cheapest block cards until block covers intent, then remaining cards by points-per-soul (damage+block+5/card drawn, live-evaluated); Load (priority list, highest blank face) or Strengthen face 20 as the die reward; heal below 35 HP at a rite, else a die action; the highest points-per-soul card reward, skipped under 5. Two CLI/policy parameters, dieRewardRule (asBuilt | elitesAndRitesOnly) and riteHeal (20 | 25, the latter via an extra healPlayer(5), no config edit) — neither edits a game file. Appends one CSV row per run to tests/autoplay_results.csv; caps a fight at 60 rounds (reported as a bug, not an outcome) and fails the batch on any console/page error or on any '[CARD] cannot play'/'not enough soul' log line. A measurement tool, not a floor or ceiling for a human — never loaded by index.html. CHECKPOINT 3 MAP: playOneAct()'s lane traversal is read live off gameState.run.act.upper (slot count and each slot's own type/label), not a hardcoded 5-slot array, so the identical policy plays both the pre-checkpoint-3 5-slot lane and the 8-slot lane with no code change; runBatch()'s summary now also reports mean rounds per fight (every completed fight's own round count, not just each run's last) and, per act, mean HP entering that act's own boss.

---

# WHAT THIS GAME IS

A roguelike deckbuilder built on Slay the Spire's skeleton, with a D20 as the second power axis.

Every face of the die starts blank. The player rolls once per turn before playing cards. Between fights they reshape the die — loading mods onto blank faces, weighting faces to come up more often.

Core sentence: You are not rolling for an outcome. You are building the distribution you roll on.

Every mechanic must let the player shape or exploit the distribution. If it does not, it does not belong in V1.

---

# SCOPE — V1

One class only: The Ordained. Dante, Obelisk and EX-0 are deferred and must not be built.

Blank faces are not empty. Rolling a blank face generates 2 block for The Ordained. This is what makes loading a mod a real decision rather than a free upgrade.

Two die actions only: Load (place a mod on a blank face) and Strengthen (add 1 weight to a face). Remove, Enchant, Purify and Expand are deferred.

Design floor: every mod must clearly outperform a guaranteed 2 block, measured on the turn it triggers, not scaled by trigger frequency. Trigger frequency cancels out of this comparison: a mod and a blank loaded on the same face are gated by the identical roll chance, so comparing their per-trigger value directly is correct. An earlier version of this floor incorrectly multiplied mod value by trigger rate (treating the mod as if it alone paid a 5%-frequency tax while the blank did not), which inflated the bar roughly twentyfold and is wrong — do not use that arithmetic. Current band: 10 to 16 points of value on the triggering turn. This was raised from an earlier "3 to 5 times a blank" (6 to 12 points) figure after the first mod pool draft came out weaker than Consecrate — 10 to 16 is the standard every future mod is checked against.

Enemy HP is the axis that carries progression across a run, not mod numbers. As enemies get tougher across a run, their HP pool scales; mod values themselves should stay in readable single or low double digits, and multipliers on those values should stay modest. Do not design a mod's power budget by inflating its raw numbers to keep pace with a harder run — that's enemy HP's job. This keeps every mod's value legible at a glance regardless of what stage of a run it's being evaluated in.

A run now has three fight types, each with its own HP/intent band and its own die (see ENEMY DIE PER TYPE): normal fights, one elite per act, and the act boss. This superseded the single flat "test enemy" the design floor above predates.

Not in V1: rings, markings, resonance, shops, meta-progression, multiple enemies per fight, story and flavour, animations, art.

---

# THREE LAWS — NEVER VIOLATE

LAW 1 — DAMAGE PIPELINE
All damage passes through calculateDamage(). Never calculate damage inline.
Order: base → flat additions → multipliers. Always. Multipliers always last.
Flat additions and multipliers are registered via the listener system. Not separately.

LAW 2 — LISTENER SYSTEM
All mod effects, card effects, class passives, and pipeline additions attach via registerListener().
The phase machine calls callListeners() at each phase boundary.
Adding a new effect never modifies existing phase logic. New effect = new listener.
This is the only registration system. There is no second system.

LAW 3 — SINGLE STATE OBJECT
All game state lives in gameState. Nothing mutates gameState directly.
All changes go through updatePlayer(), updateEnemy(), updateTurn(), updateDie(), updateRun().
If you are writing gameState.anything = x outside a helper function, stop.

---

# TERMINOLOGY

Mods trigger. Never fire. Never activate. Always trigger.
Soul is the resource spent on cards. Never energy.
A loaded face has a mod. A blank face does not.

---

# CORE NUMBERS

Player HP 70. Carries between fights.
Soul 3 per turn. Resets every turn.
Draw 5 cards at start of turn.
Deck size 10 at run start (5 Strike, 4 Ward, 1 Rite); grows by one card per reward picked. gameState.player.ownedCards holds the full permanent collection — deck/hand/discard are reshuffled from it on every fight reset, not from the fixed starting-deck constant, so reward picks survive Restart Fight.
Block clears at start of every turn.
Rounding always Math.ceil(). No exceptions.
Permadeath.
Penitence (player Nat 1) lasts exactly 3 turns from onset (PENITENCE_TURNS), once per fight — see CLASS OBJECT STRUCTURE.
Nat 20 (player and enemy alike) is uncapped and not once-per-fight: it fires every loaded face on that die, ascending, every single time it comes up.

These numbers are modelled on Slay the Spire and exist so that the player always has more cards than soul. Leaving cards unplayed and choosing which is the core decision of the card layer.

---

# STATE SCHEMA

gameState = {

  player: {
    hp: 70,
    maxHp: 70,
    block: 0,
    soul: 3,
    maxSoul: 3,
    deck: [],
    hand: [],
    discard: [],
    classId: 'ordained',
    poisonStacks: 0,
    penitenceActive: false,
    penitenceTurnsRemaining: 0,   // set to PENITENCE_TURNS (3) at Nat 1 onset, ticked down at START_OF_TURN
    natOneFiredThisFight: false,  // Nat 1 is once per fight; once true, face 1 rolls a plain blank for the rest of the fight
    ownedCards: []                // full permanent collection; deck/hand/discard reshuffle from this every fight reset
  },

  enemy: {
    id: 'Fight' | 'Elite' | 'Boss',
    hp: 0, maxHp: 0,
    intent: 0, intentMin: 0, intentMax: 0,  // intent re-rolled in this range every START_OF_TURN
    hasDie: false,                 // true only for elite/boss — see ENEMY DIE PER TYPE
    die: { faces: [] },
    poisonStacks: 0,
    activeBuffs: [],
    natOneFiredThisFight: false,   // mirrors the player's field; only meaningful on the boss (only die with ENEMY_NAT_ONE)
    buffPoisonStacks: 5            // BUILD 125 (F31) — this fight's own enemy_buff_poison amount, fixed at buildAct() time from GAME_CONFIG.ENEMY_BUFF_POISON_STACKS scaled (Math.ceil) by the current act's ACT_INTENT_MULTIPLIER — see ACTS
  },

  die: {
    faces: []                      // the player's own die, 20 faces, persists across fights. BUILD 108: the old separate triggerCounts array (BUILD 103) is gone — each loaded face's own modData now carries triggerCount (for modId) and, on a two-mod face, triggerCount2 (for modId2), run-scoped like the rest of modData, not fight-scoped. See MULTI-MOD FACES.
  },

  turn: {
    phase: 'START_OF_TURN',
    cardsPlayedThisTurn: 0,
    round: 0,
    rollOutcome: null,             // 'blank' | 'mod' | 'nat_twenty' | 'nat_one'; cleared every START_OF_TURN
    rolledFaceWeight: null,
    rolledFaceNumber: null,        // drives the die-row highlight
    enemyRollOutcome: null,        // BUILD 101: enemy-side mirror of rollOutcome, same values; set in resolveEnemyRoll()
    enemyRolledFaceNumber: null,   // BUILD 101: enemy-side mirror of rolledFaceNumber; drives the enemy die-row highlight
    modTriggeredThisTurn: false,   // true whether the trigger came from a normal roll or Nat 20's loop
    enemyAttackCancelledThisTurn: false  // set by the enemy's own Nat 1, read once by ENEMY_ACT_PHASE, cleared next START_OF_TURN
  },

  run: {
    stage: 1,
    node: 1,
    status: 'active',              // 'active' | 'win' | 'loss' — THIS FIGHT only
    screen: 'map',                 // 'map' | 'fight'
    outcome: 'active',             // 'active' | 'won' | 'lost' — the WHOLE run
    lane: null,                    // null | 'upper' | 'lower', fixed at the divergence
    currentSlot: null,             // null (at the fork) | { lane, index } | 'boss'
    act: null,                     // built by buildAct(actNumber) — opening/upper[]/lower[]/boss slots, that act's own numbers baked in
    actNumber: 1                   // BUILD 125 (F31) — 1-based, GAME_CONFIG.ACTS total. Incremented only when a non-final act's boss is defeated — see ACTS
  },

  // BUILD 109: the run record. Distinct from run above (which is fight/
  // run-progress state read by the game itself) — this is a write-once-
  // per-event log of the run for the player's own reference, reset only by
  // startNewRun() (resetRunRecord()), never by a fight reset. See RUN
  // RECORD for the full mechanic.
  runRecord: {
    started: false,                // true once at least one real slot has been entered this run
    flushed: false,                // true once this run's line has been written to localStorage
    source: 'human',               // 'human' | 'bot' — always 'human' from real play
    node: null,                    // last slot entered, e.g. 'opening' | 'upper-2' | 'boss'
    arrivalHpAtBoss: null,         // player.hp the instant the boss slot is entered; null if never reached
    outcome: null,                 // 'won' | 'lost' | 'abandoned' — set once, at flush time
    fightRounds: [],               // [{ label, rounds }, ...] — one entry per fight that has ended or was in progress at flush
    dieActionEvents: []            // [{ type:'load', offered:[modId,...], picked }|{ type:'skip' }] — one per Load offer shown or Skip chosen
  },

  registry: {
    listeners: {}
  },

  config: {
    classes: {},
    cards: {},
    mods: {},
    cardPool: {}                   // the 15-card reward pool — see CARDS
  }

}

PENITENCE_TURNS = GAME_CONFIG.PENITENCE_TURNS (BUILD 102), assigned once in state.js and read by both Nat 1's onset and the START_OF_TURN tick, so they cannot drift apart.

---

# STATE HELPERS — USE THESE, NOTHING ELSE

updatePlayer(changes)   — Object.assign into gameState.player
updateEnemy(changes)    — Object.assign into gameState.enemy
updateTurn(changes)     — Object.assign into gameState.turn
updateDie(changes)      — Object.assign into gameState.die
updateRun(changes)      — Object.assign into gameState.run
updateRunRecord(changes) — Object.assign into gameState.runRecord (BUILD 109) — see RUN RECORD

config and registry are never updated through helpers.
config is set once at init and never changed.
registry is managed only through registerListener() and clearListeners().

---

# PHASE ORDER — NEVER CHANGE

START_OF_TURN → ROLL_PHASE → CARD_PHASE → END_PLAYER_TURN → ENEMY_ROLL_PHASE → ENEMY_ACT_PHASE → CHECK_WIN_LOSS → START_OF_TURN

ENEMY_ACT_PHASE returns immediately — before intent, block, or damage are touched — when gameState.turn.enemyAttackCancelledThisTurn is true, set by the enemy's own Nat 1 earlier the same turn.

---

# EVENT HOOKS — COMPLETE LIST

These are the names registerListener() is designed around. The phase names in PHASE ORDER (START_OF_TURN, ROLL_PHASE, CARD_PHASE, END_PLAYER_TURN, ENEMY_ROLL_PHASE, ENEMY_ACT_PHASE, CHECK_WIN_LOSS) are also, in practice, real dispatchable hooks: runPhase(phase) calls callListeners(phase) unconditionally near its top, once per phase visit, before that phase's own if-branch runs — so any registerListener() call using one of the seven PHASE_ORDER strings as its hook fires at that phase's boundary, ahead of the phase's own logic. BUILD 100 correction: the previous version of this section (BUILD 099's documentation pass) asserted the opposite — that callListeners() is never invoked with a phase name — and called Vigil's registration on 'END_PLAYER_TURN' a dead hook on that basis. Both claims were checked against the current code and are false: phase-machine.js's runPhase() has called callListeners(phase) generically since before this build, Vigil's registration was never dead, and a live Playwright run (see CONFIRMED WORKING, BUILD 100) confirms it fires and generates block correctly. END_PLAYER_TURN specifically is exercised today only by Vigil, and fires before that phase's own hand-to-discard logic, which is what lets it read hand size pre-discard.

Player-side hooks:
BLANK_ROLL — {} — a genuinely blank player roll, and also the player's own Nat 1 once it has already fired this fight
MOD_TRIGGER — { modId, faceNumber } — a real mod trigger, from either a normal single-face roll or Nat 20's loop
NAT_TWENTY — {} — player rolls face 20
NAT_ONE — {} — player rolls face 1
ON_CARD_PLAY — { card }
ON_DAMAGE_DEALT — { amount, source }
ON_BLOCK_GENERATED — { amount, source }
ON_HEAL — { amount } — added BUILD 092 alongside healPlayer()

Enemy-side hooks:
ENEMY_BUFF_TRIGGER — { buffId, faceNumber } — a loaded enemy buff face triggers, from a normal roll or the enemy's own Nat 20 loop
ENEMY_NAT_TWENTY — {} — enemy rolls face 20 (boss only — the only die that carries this modId)
ENEMY_NAT_ONE — {} — enemy rolls face 1 (boss only)

Pipeline hooks — read directly off gameState.registry.listeners, not through callListeners(); each fn returns a number:
DAMAGE_FLAT_ADDITION — fn() returns amount to add
DAMAGE_MULTIPLIER — fn(sourceType) returns multiplier value; sourceType lets a multiplier scope itself (e.g. Fervour doubles 'attack' only, never 'poison')
BLOCK_FLAT_ADDITION — fn() returns amount to add
BLOCK_MULTIPLIER — fn() returns multiplier value (same pipeline shape as DAMAGE_MULTIPLIER; no mod currently registers one)

BUILD 100 correction: the "known dead registration" reported here by BUILD 099 was itself wrong. cards-mods.js registers Vigil's block-per-card-held effect on hook 'END_PLAYER_TURN'; that hook IS dispatched — see this section's note above — and Vigil fires correctly, verified live via Playwright (CONFIRMED WORKING, BUILD 100). No code change to Vigil or to phase-machine.js was needed; this build's fix was to the documentation and to add tests/mods.test.js as a standing regression guard so a genuinely dead mod registration can't pass as confirmed again.

---

# LISTENER API

registerListener(hook, id, fn, clearOn)
  hook    — one of the names in EVENT HOOKS above; a PHASE_ORDER phase name is also accepted and does fire, at that phase's boundary, via runPhase()'s own generic callListeners(phase) call (see EVENT HOOKS)
  id      — unique string identifier for this listener
  fn      — pipeline hooks: fn() (fn(sourceType) for DAMAGE_MULTIPLIER) returns a number. other hooks: fn(data) returns nothing.
  clearOn — 'turn' | 'permanent'. 'fight' is accepted by the function signature but no call site has ever passed it, and clearListeners('fight') itself was removed in BUILD 092 as dead code — every fight-scoped field is reset explicitly instead (see FIGHT RESET).
  Same hook + same id registered twice is a no-op (BUILD 092 dedup guard): registerListener logs '[LISTENER] duplicate registration skipped' and returns without re-adding it. This matters for a mod whose own effect() would otherwise re-register a persistent listener on every trigger.

callListeners(hook, data)
  calls all listeners for hook in registration order
  stops immediately if run.status is not 'active'

clearListeners(clearOn)
  removes all listeners where clearOn matches the argument
  called with 'turn' automatically at START_OF_TURN
  'permanent' never cleared automatically

---

# DAMAGE PIPELINE

calculateDamage(baseDamage, sourceType)
  1. total = baseDamage
  2. for each DAMAGE_FLAT_ADDITION listener: total = total + fn()
  3. for each DAMAGE_MULTIPLIER listener: total = Math.ceil(total * fn(sourceType))
  4. return Math.ceil(total)
  sourceType (e.g. 'attack', 'poison') lets a multiplier scope itself to one source; flat additions are untagged and apply to every call.

generateBlock(baseBlock)
  1. total = baseBlock
  2. for each BLOCK_FLAT_ADDITION listener: total = total + fn()
  3. for each BLOCK_MULTIPLIER listener: total = Math.ceil(total * fn())
  4. return Math.ceil(total)

Two multipliers compound. () => 2 then () => 3 produces x6 not x5. This is correct. Do not change.

dealDamage(target, amount, sourceType, sourceId, fireListener = true) — shared helper (pipeline.js). Runs calculateDamage(), applies it to the target's hp via updateEnemy()/updatePlayer(), plays the damage-landing sound when damage > 0, and fires ON_DAMAGE_DEALT unless fireListener is explicitly false (used once, by the enemy's own attack, which never fired that hook before dealDamage() existed and must not gain it now). Returns the final damage dealt. Every card/mod that deals damage calls this instead of repeating the three lines inline.

dealBlock(amount, sourceId) — same shape for block: generateBlock(), add to gameState.player.block, fire ON_BLOCK_GENERATED. Returns the final block generated.

healPlayer(amount) — added BUILD 092. newHp = Math.min(gameState.player.hp + amount, gameState.player.maxHp); updates hp; fires ON_HEAL with the actual, post-cap amount healed; returns that amount. No flat-addition or multiplier stage — a direct clamp, not a third pipeline. Currently used by the post-win rite's Heal option.

All turn-scoped pipeline listeners clear at START_OF_TURN automatically.

---

# ROUNDING

Always Math.ceil(). No exceptions.

---

# DECK STORAGE

deck, hand, discard contain card id strings only. Never card objects.
Card objects live in config.cards.
getCard(id) returns config.cards[id].
Multiple copies of same card = same id string repeated.
gameState.player.ownedCards holds the player's full permanent collection (one id string per owned copy). deck/hand/discard are the current fight's draw pile and are reshuffled from ownedCards on every fight reset — not from the fixed starting-deck constant — so a card picked at a reward survives Restart Fight.

---

# CARD OBJECT STRUCTURE

Cards live in config.cards. Not config.mods.

{
  id: 'unique_string',
  name: 'Display Name',
  soulCost: 1,
  type: 'attack' | 'block' | 'utility',
  classRestriction: null | 'ordained',
  getCost: function(gameState) { ... },  // optional, overrides soulCost when present — only Rapture uses this
  effect: function(gameState) {
    // calls helpers and pipeline functions only
    // never mutates gameState directly
  }
}

---

# CARDS

Eighteen cards defined in total: the three Ring 0 cards the run always starts with, plus the fifteen-card reward pool (config.cardPool) the reward screen draws its three offered options from. Both live in the same config.cards object; cardPool holds references to the same fifteen objects, not copies.

Ring 0 — the starting deck (5 Strike, 4 Ward, 1 Rite, 10 cards):
Strike — 1 soul, attack — 5 damage.
Ward — 1 soul, block — 5 block.
Rite — 2 soul, attack, Ordained-only — 5 damage + 6 block.

Pool (config.cardPool), fifteen cards:
Rebuke — 0 soul, attack — 4 damage.
Censure — 2 soul, attack — 14 damage.
Judgement — 3 soul, attack — 20 damage.
Vestment — 2 soul, block — 13 block.
Litany — 2 soul, attack — 7 damage + 7 block.
Scripture — 1 soul, utility — draw 2.
Communion — 0 soul, utility — +2 soul this turn.
Censer — 1 soul, utility — 4 poison to the enemy.
Purge — 1 soul, attack — 6 damage, 10 if the enemy is currently poisoned.
Interdict — 1 soul, block — 5 block, 10 if enemy intent is 12 or higher.
Reckoning — 1 soul, attack — 3 + 2× the enemy's current poison stacks.
Retribution — 1 soul, attack — damage equal to current block, capped at 12.
Covenant — 1 soul, attack — 2 + 3× the weight of the face rolled this turn (gameState.turn.rolledFaceWeight).
Rapture — 2 soul (0 if a mod triggered this turn, via getCost), attack — 12 damage.
Orison — 1 soul, attack — 5 damage, 9 if this turn's roll was a blank (gameState.turn.rollOutcome === 'blank').

---

# DIE FACE OBJECT STRUCTURE

{ number: 1, modId: null, modId2: null, weight: 1 }

number: 1-N, where N is that die's own GAME_CONFIG.DIE_SIZE entry (BUILD 107, closes KI-12) — PLAYER, ELITE or BOSS. All three read 20 today, but each is its own named value, not one shared literal: the player, an elite and the boss are each free to vary independently later (D-11, Parked) without a second refactor. Nothing in js/ reads a bare 20 for a die size.
modId: null = blank. string = mod id from config.mods (player die) or a buff id (enemy die). weight: default 1.
modId2 (BUILD 115): a face can hold up to two mods, cap two, never three. null = only one mod (or blank). string = a second mod id, loaded after modId, only ever onto a face where modId is already set. Blank faces, Nat faces, and the enemy die's faces never carry a modId2 — see MULTI-MOD FACES below.
All of a die's faces must always be explicitly defined. No implicit blanks.
Player die and enemy die share this face object shape, but the Nat-face rule differs by die:

Player die — face 1 is always modId 'NAT_ONE', face GAME_CONFIG.DIE_SIZE.PLAYER (20) is always modId 'NAT_TWENTY'. Cannot change. Both are stub ids, never real mods, and both are excluded from Nat 20's own loaded-face loop.

Enemy die — whether faces 1/N carry a Nat modId depends on which enemy type the die belongs to; it is not fixed the way the player's is. See ENEMY DIE PER TYPE. buildEnemyDieFaces(poisonFaceNumbers, includeNats, dieSize) (run-and-map.js) is the one function that builds every enemy die; dieSize (BUILD 107 — was a bare 20) is the calling entity's own GAME_CONFIG.DIE_SIZE.{ELITE,BOSS}; includeNats is what switches faces 1/dieSize between an ordinary blank and a Nat face.

Weight display: any face at weight above 1 shows ×N in the die rows, on both the player's and the enemy's die, live (landed BUILD 065). The only place a face's weight is ever written is strengthenFace(faceNumber) (pipeline.js, BUILD 107) — Strengthen (rendering.js's dieActionPickStrengthenFace) and Ordain's effect (cards-mods.js) both call it instead of each cloning-and-incrementing gameState.die.faces independently, which is what they did before this build. Player die only — nothing Strengthens or Ordains the enemy's die. BUILD 116: the ×N text badge is no longer the only cue — a weight-2+ face also shows a bottom-anchored fill inside its own face-btn (`.face-weight-fill`, index.html), height 25% per point of weight above 1, capped at weight 5 (100%). Uses `background: currentColor` so the fill is always the same identity colour already governing that row (--blank/--nat/--player-mod/--enemy-mod) at reduced opacity — no new colour, emphasis only — see CURRENT SUBSTAGE for the full "why" (the thesis mechanic wasn't visible at a glance before this build).

Trigger-count display (BUILD 103, folded into per-face modData BUILD 108 — see MULTI-MOD FACES/TRIGGER COUNTS below): any loaded face on the player's own die that has triggered at least once this fight shows a badge — same box, same colour-inheritance selectors as the ×N weight badge (a separate CSS class, `.die-trigger-count`), zero triggers renders nothing. Player die only: renderDieList() gates this on reference equality between the faces array it was passed and gameState.die.faces, which is false for the enemy die and the map's static elite/boss previews. Closes KI-11. One counter per mod slot, not per face — a two-mod face shows both counts separated by a slash, first-loaded mod first. BUILD 116: the badge text changed from '↻N' to '#N' (and '#N1/N2' for two mods) — '↻' reads as a reload/refresh glyph to most players, not "count of," and '#' doesn't collide with the weight badge's own '×N' notation beside it; font-size bumped 14px -> 17px (with an explicit line-height:1, since without one the browser's default line box at 17px is taller than the die row's fixed height and pushes that one row taller than its neighbours — measured and fixed in the same build) so the number reads at a glance without hovering. BUILD 138: faces 1 and 20 (NAT_ONE/NAT_TWENTY) now also carry this exact badge — each counts how many times it has been rolled this run, in modData.triggerCount, the same field/render path every other face's own trigger count already uses (bumpNatFaceTriggerCount(), pipeline.js, called from resolvePlayerRoll()'s two Nat branches). Nothing in renderDieList() needed a code change for this — `face.modId !== null` was already true for both Nat stub ids, so the existing single-mod badge branch picked it up for free; only the existing nat-one/nat-twenty CSS colour-override selectors (already listing `.die-trigger-count`) needed to exist, and they already did.

---

# MULTI-MOD FACES (BUILD 115)

A face can hold up to two mods — modId (first loaded) and modId2 (second loaded) — cap two, never three. Per-face state (Zeal's accumulated bonus, per-mod trigger counts — see TRIGGER COUNTS below) stays on that face's own modData field regardless of which slot the mod is in; there is no parallel array keyed by face number anywhere in this system, and there must never be one (the carry-forward rule — BUILD 103's original triggerCounts array was exactly that shape, and BUILD 108 folded it back into modData for this reason; nothing new should ever repeat that mistake).

Trigger order: both mods on a face trigger when that face is rolled, in load order — modId first, modId2 second. Each resolves fully (its effect() returns) before the next begins. resolvePlayerRoll() (pipeline.js) dispatches this with two sequential, synchronous callListeners('MOD_TRIGGER', ...) calls; the generic mod_dispatch listener (cards-mods.js) needed no changes, since it was already written to handle one MOD_TRIGGER at a time regardless of caller.

Nat 20: onNatTwenty() (cards-mods.js) loops the player's loaded faces ascending, exactly as before; a two-mod face now dispatches both of its mods (modId then modId2) inside that same forEach iteration, before the loop advances to the next face — "within that face's own turn in the ascending sequence."

Load: dieActionChooseLoad() (rendering.js) still excludes the anchor and any mod already on the die, in either slot. The face picker (load_pick_face) offers any blank face AND any already-loaded, non-Nat, not-yet-full face together, always — BUILD 115's "only once no blank face is left" gate was removed in BUILD 116, per the prompt's explicit instruction: a second mod is a valid Load target at any point in a run, regardless of how many blanks remain. isEligible collapses to one condition, `f.modId !== 'NAT_ONE' && f.modId !== 'NAT_TWENTY' && !f.modId2` — a blank face (modId null) trivially satisfies it, so no separate blank-vs-loaded branch is needed any more. dieActionPickLoadFace() writes modId on a blank face, modId2 on an already-loaded one, and refuses (no write) if both slots are already full — a defensive guard, since the picker's own eligibility should never offer a full face in the first place. The cap of two mods per face is unchanged.

Pool exhaustion (D-54, BUILD 126, revised BUILD 127): eligibleLoadModIds() (rendering.js — every unloaded, non-anchor mod, the same list dieActionChooseLoad() draws its 3-mod offer from) is checked before the Load button itself is ever rendered — the die action panel's 'choose' step shows only Strengthen and Skip once fewer than 3 remain, so a real 3-mod offer that can no longer be built is never presented at all, short or otherwise. dieActionChooseLoad() keeps the same eligibility check as a defensive fallback (converts to Strengthen) for a direct call bypassing the UI. tests/autoplay.js's bot policy checks the identical count before calling dieActionChooseLoad(), mirroring what a human would see.

Faces 1 and 20 are untouched by this change: both are always single-mod Nat stubs (NAT_ONE / NAT_TWENTY), excluded from the Load face picker exactly as before, and never gain a modId2. Face 20 can still be Strengthened; nothing else about it changes.

The enemy die shares the same face shape (modId2 always present, always null) for structural symmetry, but nothing ever writes an enemy face's modId2 — no enemy action loads a second buff. ENEMY_NAT_TWENTY's own loop (cards-mods.js) and resolveEnemyRoll() (pipeline.js) were deliberately left reading only modId; this is a scope choice, not a gap, since the enemy die's faces can structurally never carry a second buff.

Display: a face holding two mods shows both mod names in the one die row, side by side — `.die-mod-pair` (index.html, replaced BUILD 115's vertical `.die-mod-stack` at BUILD 117) wrapping two `.die-mod` spans, each with its own inline trigger-count badge. No new row; the die column still renders exactly twenty rows. faceHoverText() (rendering.js) appends the second mod's MOD_DESCRIPTION entry to the hover tip the same way. BUILD 137 (playtest readiness): each name is truncated to its first TWO_MOD_NAME_CHARS letters (rendering.js, 6 by default), no ellipsis — replaces BUILD 117's fixed-max-width CSS ellipsis, which could cut a name down to two or three letters plus "…" (e.g. "Her…"/"San…"). The full name is available via a native `title` tooltip set on the name span itself, not the row's own hover-tip. 6 was measured (Playwright canvas measureText, worst-case pairing) to fit both names plus their inline trigger badges inside the narrower #dieActionDieList at the game's default 1600×1080 window with room to spare; drop to 5 if a future name addition stops fitting.

Dev tooling: devLoadMod() (dev-tools.js) mirrors the real Load flow's cap — fills modId if blank, else modId2 if empty, else refuses and logs "already holds two mods, refused" with no write. devClearFace() clears both slots.

TRIGGER COUNTS (BUILD 108): folded gameState.die.triggerCounts — BUILD 103's separate array, indexed by face number, fight-scoped — into each face's own modData, one count per mod slot: modData.triggerCount for modId, modData.triggerCount2 for modId2 on a two-mod face. mod_dispatch (cards-mods.js's registerListener('MOD_TRIGGER', 'mod_dispatch', ...)) matches data.modId against the triggering face's modId/modId2 to decide which counter to bump, so a face holding two different mods increments only the one that actually fired, never its neighbour — even under Nat 20, where both of a two-mod face's mods are dispatched separately (see Nat 20 above) and each bump lands on its own counter. Run-scoped, not fight-scoped: modData lives on the face object itself, which clearFightScopedState() (a fight reset — Restart Fight) never touches, so both counts survive a fight reset intact; only startNewRun()'s brand-new faces (buildFreshPlayerDieFaces(), carrying no modData at all) wipe them — the same mechanism that already wiped Zeal's own accumulatedBonus before this build. Zeal's own effect (cards-mods.js) was corrected in the same build to merge into the face's existing modData rather than replace it wholesale — it used to write `{ modData: { accumulatedBonus } }` as a bare object literal, which would have silently erased a face's trigger count on every Zeal trigger. Display: renderDieList() (rendering.js) shows a single ↻N badge on a one-mod face exactly as before (zero renders nothing); a two-mod face shows both counts separated by a slash, first-loaded mod first (↻N1/N2), shown whenever at least one of the two is nonzero.

---

# OUTSIDE-ROLL TRIGGER (BUILD 132)

triggerFaceOutsideRoll(faceNumber) (pipeline.js) is the one shared function every "trigger a face without rolling it" card/mod goes through — Threnody, Reverberation, Magnificat today; any future piece with the same shape uses this, never a second copy of the dispatch logic.

Refuses outright (no state change, returns false) for face 1 or face GAME_CONFIG.DIE_SIZE.PLAYER (20) — both are Nat stubs, never a real mod or a blank.

Refuses a face already triggered this way once this round — gameState.turn.outsideTriggeredFaces (state.js), an array of face numbers, cleared to [] at START_OF_TURN (phase-machine.js) alongside every other per-round roll flag. A face rolled normally and then re-triggered outside a roll (Reverberation) is not blocked by this rule — the record only tracks outside triggers, not the roll itself.

D-51 — per-round trigger cap. GAME_CONFIG.ROUND_TRIGGER_CAP (config.js) is 10. gameState.turn.roundTriggerCount (state.js) counts every real MOD_TRIGGER dispatch this round — mod_dispatch (cards-mods.js, the one permanent MOD_TRIGGER listener) increments it on every call, whether reached via a normal roll, triggerFaceOutsideRoll(), or anywhere else — except a Nat 20 sweep's own calls, which onNatTwenty() (cards-mods.js) tags natTwentySweep: true so mod_dispatch can skip counting them; this is the one exemption. A blank face's BLANK_ROLL dispatch bypasses mod_dispatch entirely, so triggerFaceOutsideRoll() bumps the same counter directly in that branch. triggerFaceOutsideRoll() refuses once the counter has already reached the cap; cleared to 0 at START_OF_TURN, same site as outsideTriggeredFaces above. BUILD 137 (playtest readiness): the "round trigger cap (N) reached" log line now prints at most once per round — gameState.turn.roundTriggerCapLogged (state.js), set true the first time the cap refuses a trigger, checked before logging, cleared at the same START_OF_TURN site as every other round-scoped flag above. Refusals after the first still return false and still refuse the trigger; only the repeated log line is suppressed.

Dispatch: a loaded face triggers through the identical MOD_TRIGGER callListeners() dispatch a rolled face uses — modId first, then modId2 if present, same load order, same mod_dispatch funnel — so permanent per-face growth (Zeal's accumulatedBonus, Ordain's/Elevation's weight write via strengthenFace(), Cope's copeBonus) accrues exactly as it would on a roll. A blank face dispatches BLANK_ROLL for the same GAME_CONFIG.BLANK_ROLL_BLOCK (2) block. Never writes gameState.turn.rolledFaceNumber/rollOutcome/rolledFaceWeight — an outside trigger never changes which face is considered "the face rolled this round."

---

# THE HOP (BUILD 138)

Die feedback: every face that fires WITHOUT being the face actually rolled this round — a Nat 20 sweep, a Bound scan, or an outside-roll trigger (Threnody/Reverberation/Magnificat/Novena) — moves its die row to the exact same look a rolled face gets (indent + yellow, `.die-row-rolled`/`.die-row-rolled-flash`, index.html — byte-for-byte the same classes/CSS the BUILD 090 rolled-face highlight already uses, no new colour, no new indent). Player die only; the enemy die's own Nat 20 sweep (ENEMY_NAT_TWENTY) is unpaced (no playSweep()) and out of this build's scope.

gameState.turn.hoppedFaces (state.js) is the record — an array of face numbers, appended in firing order, a face never added twice in the same round. Two call sites mark it, both at the instant a face's dispatch actually fires (pipeline.js's markFaceHopped()):
- triggerFaceOutsideRoll() — covers every outside-roll trigger AND the Bound scan, since runBoundScan() dispatches through this same function; one call right after the round's outsideTriggeredFaces record is written, before the MOD_TRIGGER/BLANK_ROLL dispatch below it.
- onNatTwenty()'s own playSweep() dispatch callback (cards-mods.js) — the one sweep path that dispatches directly via callListeners() rather than through triggerFaceOutsideRoll(), so it needs its own call.

Paced by playSweep() exactly as before (BOUND ENGINE's Fast sweep timing, unchanged by this build) — the hop is marked inside the same dispatch callback playSweep() already staggers, so it lands at the same moment (and same pace) the underlying trigger does, never separately timed.

Display: renderDieList() (rendering.js) applies the rolled-face look to any row whose face.number is in gameState.turn.hoppedFaces, player-die containers only (playerDieList/dieActionDieList, never enemyDieList or the map's static previews — same tracksRolledFace/isEnemyContainer gate the rolled-face highlight itself already uses), skipped for the row that is already the tracked rolled face (already covered by the existing highlight, so a re-trigger of the rolled face itself — e.g. Reverberation — adds nothing extra, no double-apply). Same flash-once-then-sustained split as the rolled face's own highlight: a per-container "already seen hopped" record (lastSeenHoppedFacesByContainer, module-scope, rendering.js — same idiom as lastSeenRollSignatureByContainer) picks the animated `-flash` class only on the render immediately after a face is newly added to hoppedFaces; every later re-render of that same hop gets the plain sustained `.die-row-rolled` instead. Stays in that look for the rest of the round, exactly as a rolled face does, and clears at the same site: gameState.turn.hoppedFaces is reset to `[]` at START_OF_TURN (phase-machine.js), alongside every other round-scoped roll flag.

Faces 1 and 20's own run-scoped roll counts (a separate BUILD 138 change, not part of the hop record): each counts how many times it has been rolled this run, stored in that face's own modData.triggerCount — the identical field/render path every other face's trigger-count badge already uses (see TRIGGER-COUNT DISPLAY above) — bumped by bumpNatFaceTriggerCount() (pipeline.js), called from resolvePlayerRoll()'s NAT_TWENTY/NAT_ONE branches on every roll of that face, including a spent face 1's later blank-equivalent rolls. Reset for free on a new run: buildFreshPlayerDieFaces() (run-and-map.js) already returns faces with no modData at all, the same mechanism that wipes every other face's trigger count.

---

# BOUND ENGINE (BUILD 133)

A face is Bound if a mod loaded on it (either slot) has Bound printed — carries the 'bound' tag, permanent — or the face was granted Bound for the fight. isBoundFace(face) (pipeline.js) checks both: face.modId/modId2 against gameState.config.mods[...].tags, and face.modData.boundGranted.

grantBoundToFace(faceNumber) (pipeline.js) is the one setter — grants Bound to a loaded face for the rest of the current fight, merged into that face's own modData (ARCH-CF2, same merge pattern Zeal/Cope already use). Refuses outright (no state change, returns false) for face 1 or face GAME_CONFIG.DIE_SIZE.PLAYER (20) — both are Nat stubs — and for a genuinely blank face (both slots null). The grant itself is the one piece of a face's modData that is fight-scoped rather than run-scoped (unlike trigger counts or Zeal's/Cope's own accumulators — see MULTI-MOD FACES/TRIGGER COUNTS above): clearFightScopedState() (run-and-map.js, shared by resetFight() and every real fight transition) strips boundGranted from any face that carries it at fight end, leaving the rest of that face's modData untouched.

Display (BUILD 137, playtest readiness): every Bound face — printed (isBoundFace() true via a mod's own 'bound' tag) or granted (isBoundFace() true via modData.boundGranted) — shows a small "Bound" badge on its die row (renderDieList(), `.die-bound-badge`, same box/font as the `.die-weight` ×N badge, D-28's no-new-palette rule). Shown on every container renderDieList() draws, not gated by the trigger-badge's showTriggerBadges (a printed badge is part of what the mod IS, visible wherever its name is); a granted badge only ever appears on the live player die, since grantBoundToFace() only ever writes gameState.die.faces. Appears the instant a grant lands and disappears the instant clearFightScopedState() strips it — no separate render step needed, since every updateDie() call re-renders every die list.

Bound scan: runBoundScan(rolledFace) (pipeline.js), called only from resolvePlayerRoll()'s own mod-trigger branch (never from onNatTwenty()'s Nat 20 sweep, so the scan never runs during a Nat 20). When the rolled face is itself Bound, every OTHER loaded Bound face on the die triggers through triggerFaceOutsideRoll(), ascending face order, any position on the die — gameState.die.faces is already ascending, so filtering preserves order with no extra sort. A face triggered this way never starts a further scan (triggerFaceOutsideRoll() has no notion of what called it). Counts toward GAME_CONFIG.ROUND_TRIGGER_CAP (D-51) exactly like any other outside trigger, no exemption.

Fast sweep timing: playSweep(faceNumbers, dispatchFn) (pipeline.js) paces WHEN each face in a multi-face sweep (Nat 20's onNatTwenty() loop, the Bound scan above) plays — state itself still updates the instant each dispatch runs, no layout change, no other number changes. gameState.turn.roundSweepPlays (state.js) counts every sweep-played trigger this round, both kinds of sweep alike, cleared at START_OF_TURN. The first three plays in a round land GAME_CONFIG.SWEEP_TRIGGER_DELAY_MS (200ms) apart; every play after the third lands at a quarter of that delay (50ms) instead.

Three plain Bound mods (checkpoint 3, BUILD 133): Unison — 6 damage. Accord — 10 block. Kinship — applies 4 stacks of poison, tags Bound+Poison. All three tier common, tags include 'bound'.

The remaining Bound pieces (checkpoint 3, BUILD 134) — five more, closing checkpoint 3: Kyrie (card, common, cost 1, tags Bound) — 5 damage, 10 if the rolled face has Bound (isBoundFace()). Novena (card, rare, cost 2, tags Bound) — every loaded Bound face triggers, each through triggerFaceOutsideRoll(). Canticle (card, uncommon, cost 1, tags Bound) — 6 block; if the rolled face is loaded, it gains Bound for this fight via grantBoundToFace() (never face 1 or 20, since grantBoundToFace() itself refuses those, and Nat faces are never treated as "loaded" here). Concord (mod, uncommon, tags Bound+Soul) — Bound. +1 soul, 3 block. Herald (mod, rare, tags Bound) — Bound. 6 damage; one other random loaded face without Bound gains Bound for this fight, through grantBoundToFace() and pickRandom() (state.js, BUILD 134 — picks a random item from a list off the same Math.random() source shuffle() uses, returning undefined for an empty list); no eligible face is a no-op, only the damage happens. Mod count 23 -> 25 (24 offerable plus Consecrate); reward card count 33 -> 36.

---

# WEIGHTED ROLL ALGORITHM

function rollDie(faces) {
  const pool = [];
  faces.forEach(face => {
    for (let i = 0; i < face.weight; i++) { pool.push(face); }
  });
  return pool[Math.floor(Math.random() * pool.length)];
}

Default: GAME_CONFIG.DIE_SIZE.PLAYER faces (20) weight 1 = 5% each.
Strengthen to weight 2 = that face appears twice in pool.
This function never changes. Only weight values change.

Strengthen may target face GAME_CONFIG.DIE_SIZE.PLAYER (20), in addition to any loaded face. Face 1 is never targetable — the Strengthen face picker (rendering.js) excludes number 1 explicitly. Face 20 never gains a mod; Strengthen only adds weight to it, raising how often Nat 20 itself comes up.

---

# CLASS OBJECT STRUCTURE

Classes live in config.classes. Only one class exists in V1.

{
  id: 'ordained',
  name: 'The Ordained',
  anchorModId: 'consecrate',
  onNatTwenty: function() { ... },
  onNatOne: function() { ... },
  onBlankRoll: function() { ... },
  startingDeck: []
}

classId in player state is a lookup key for config.classes[classId].

onBlankRoll: generateBlock(2) via dealBlock(2, 'blank_face'), fires ON_BLOCK_GENERATED.

onNatTwenty (Nat 20): every loaded face on the player die triggers this turn, ascending face number order. Not capped, not once per fight — it fires in full every single time face 20 comes up. Face 1 and face 20 are excluded (their modIds are the NAT_ONE/NAT_TWENTY stubs, not real mods). Each qualifying face fires through callListeners('MOD_TRIGGER', ...) — the same dispatch a single rolled mod face already uses, so there is no second trigger path. Loop-safe because no mod in the pool re-rolls the die.

onNatOne (Nat 1 — Penitence): fires once per fight, gated on gameState.player.natOneFiredThisFight. First time: sets penitenceActive: true and penitenceTurnsRemaining: PENITENCE_TURNS (3), and logs onset. The actual 1-soul loss happens at each of the next three START_OF_TURNs, immediately after the soul reset, floored at 0; Penitence expires automatically after the third tick. Every subsequent face 1 rolled this fight instead dispatches BLANK_ROLL directly — an ordinary blank roll, identical in every way, with no distinguishing tag.

anchorModId references consecrate, built in config.mods — see MODS. The Ordained's die starts with exactly one loaded face: Consecrate on face 10. That starting face is Strengthen-eligible like any other loaded face, and Consecrate is excluded from the reward pool for that reason.

Enemy classes do not exist — the enemy's Nat 20 / Nat 1 / buff-trigger behaviour is registered unconditionally in init() (cards-mods.js), not attached to a class object, because it belongs to whichever enemy is being fought, not to a player class. See ENEMY DIE PER TYPE.

---

# MOD OBJECT STRUCTURE

Die mods live in config.mods. Not config.cards.

{
  id: 'unique_string',
  name: 'Display Name',
  effect: function(data) {
    // data.faceNumber is always supplied; a mod that doesn't need it ignores the argument
    // registers listeners via registerListener() for anything that must persist past this trigger
    // never mutates state directly
  }
}

---

# MODS

Thirteen mods, all in config.mods. Consecrate is excluded from the reward pool because it is already loaded on the Ordained's starting die (see CLASS OBJECT STRUCTURE); the other twelve are reward-eligible.

Consecrate (anchor) — +2 soul this turn; each card played this turn also generates 3 block, turn-scoped.
Smite — 16 damage. Flat, no conditions.
Penance — 8 damage + 8 block. Flat, no conditions.
Offering — +2 soul this turn, draw 1.
Blight — 6 poison to the enemy.
Virulence — 3 poison to the enemy, then doubles the enemy's total poison stacks (including the 3 just applied).
Sanctuary — 16 block. Flat, no conditions.
Vigil — grants 5 block per card still held in hand at end of turn, via a turn-scoped listener on hook 'END_PLAYER_TURN'. BUILD 100: confirmed working, not dead as BUILD 099 previously reported — see EVENT HOOKS. END_PLAYER_TURN is a real, dispatched hook (runPhase()'s generic callListeners(phase) call fires it before that phase's own discard logic), so Vigil's listener triggers correctly, reading hand size before the hand is discarded.
Zeal — 10 damage plus an accumulated bonus that permanently increases by 4 every time Zeal triggers again from the same specific face; stored per-face on that face's own modData (via updateDie()), not as a global counter, so Zeal loaded on two faces accrues independently on each.
Fervour — registers a turn-scoped DAMAGE_MULTIPLIER listener that doubles damage tagged 'attack' only (poison ticks are untouched). The first and only mod that uses the multiplier stage of the pipeline.
Ordain — 10 damage, then permanently adds 1 weight to the specific face it triggered from.
Anthem — 6 damage, plus 4 per point of weight on its own face (weight 1 -> 10, weight 2 -> 14, weight 3 -> 18; BUILD 113). Reuses two existing paths rather than adding new ones: dealDamage() tagged 'attack' (the same call Smite makes, so Fervour doubles it identically) and gameState.turn.rolledFaceWeight (Covenant's own read). Reads weight only; writes nothing.
Elevation — 10 damage; the face directly above the triggering face (faceNumber + 1) permanently gains +1 weight, but only if that face is loaded and is not face GAME_CONFIG.DIE_SIZE.PLAYER (20; BUILD 114). If the face above is blank or is face 20, only the 10 damage happens — no weight write, no error. Like Ordain, the weight write goes through the shared strengthenFace() (pipeline.js) — the only place any face's weight is ever written.

---

# ENEMY DIE PER TYPE

Replaces the single hardcoded "test enemy" this file used to describe. There are three enemy types, distinguished entirely by their die (buildAct(), run-and-map.js) — the die is what separates them:

Normal (the opening fight and every plain 'Fight' slot) — hasDie: false. No die is ever read; the fight screen's die slot still renders, empty, for layout consistency. A fixed intent band you can plan around, nothing to read.

Elite (one per act, upper lane) — hasDie: true. Die has exactly two loaded poison faces, on 7 and 14, and nothing else: faces 1 and 20 are ordinary blanks (modId: null), no ENEMY_NAT_ONE/ENEMY_NAT_TWENTY at all. Built via buildEnemyDieFaces([7, 14], false, GAME_CONFIG.DIE_SIZE.ELITE). Poison triggers on roughly 10% of the elite's rolls.

Boss (act finale) — hasDie: true. Die has three loaded poison faces (5, 10, 15) plus both Nat faces: face 1 is ENEMY_NAT_ONE, face 20 is ENEMY_NAT_TWENTY. Built via buildEnemyDieFaces([5, 10, 15], true, GAME_CONFIG.DIE_SIZE.BOSS). Poison triggers on roughly 15% of rolls, plus both Nat behaviours below.

buildEnemyDieFaces(poisonFaceNumbers, includeNats, dieSize) (run-and-map.js) is the one function that builds all three shapes above; there is no separate code path per enemy type. dieSize (BUILD 107) is each caller's own GAME_CONFIG.DIE_SIZE entry, not a shared literal — see DIE FACE OBJECT STRUCTURE. beginFightFromSlot() copies the entering slot's own static die onto the live gameState.enemy.die when a fight begins.

Enemy buff/Nat mechanics — all registered unconditionally in init() (cards-mods.js), not tied to gameState.player.classId, since they belong to the enemy's own die rather than to a player class, and exist regardless of which enemy is being fought:

enemy_buff_poison (the only buff that exists) — on trigger, applies gameState.enemy.buffPoisonStacks stacks of poison to the player, through gameState.player.poisonStacks, the same field every player-facing poison source (Blight/Virulence/Censer/the dev poison applier) already writes to. No second poison system. BUILD 125 (F31): that amount is not the flat GAME_CONFIG.ENEMY_BUFF_POISON_STACKS — it's fixed per act at buildAct() time (scaled by that act's ACT_INTENT_MULTIPLIER, Math.ceil'd) — see ACTS. BUILD 137 (playtest readiness): the base value was lowered from 5 to 3 (3/4/5 across acts 1/2/3, act scaling itself unchanged); ENEMY_NAT_ONE_SELF_POISON (below) is untouched.

ENEMY_NAT_TWENTY (boss only — no other die carries this modId) — fires every loaded buff face on the enemy's own die, ascending by face number. Not capped, not once per fight — a deliberate mirror of the player's own Nat 20. On the current boss die (three poison faces) this applies 15 total poison stacks to the player in one roll.

ENEMY_NAT_ONE (boss only) — two effects at once, both gated on gameState.enemy.natOneFiredThisFight (once per fight, mirroring the player's own Nat 1): (1) cancels the enemy's attack that turn entirely — sets gameState.turn.enemyAttackCancelledThisTurn, read once by ENEMY_ACT_PHASE, which returns before intent/block/damage are touched at all — and (2) self-poisons the enemy a flat 5 stacks, regardless of how many poison faces its own die carries. Corrected in BUILD 098 from an earlier version that applied 5 stacks per loaded poison face (15 on the boss — roughly two thirds of the boss's own HP from a single 5% roll); the player-facing poison amount above was never affected by that bug. Once fired, every subsequent face 1 this fight resolves as a plain blank instead.

Enemy intent: rolled fresh each turn at START_OF_TURN, a random integer in [intentMin, intentMax] (normal 6-18, elite 10-18, boss 10-20 — act 1 values; see ACTS for other acts), fixed for that turn's ENEMY_ACT_PHASE and always visible to the player before CARD_PHASE. The die only determines whether a buff or Nat triggers — it never modifies intent.

---

# ACTS (BUILD 125, checkpoint 2, F31/F32)

The run is GAME_CONFIG.ACTS (3) acts, played in sequence. Each act is a fresh map of the same two-lane shape (F16) — same slot types, its own boss — built by buildAct(actNumber) (run-and-map.js), which now takes the 1-based act number and bakes that act's own scaled numbers into every enemy at build time (never read live off GAME_CONFIG mid-fight).

Scaling — GAME_CONFIG.ACT_HP_MULTIPLIER and ACT_INTENT_MULTIPLIER, indexed by actNumber-1, both [1.0, 1.4, 1.9] and [1.0, 1.2, 1.45]:
- Every enemy's hp is Math.ceil(base hp × that act's HP multiplier).
- Every enemy's intentMin/intentMax is Math.ceil(base × that act's intent multiplier).
- The enemy buff's poison amount (enemy_buff_poison, ENEMY DIE PER TYPE) rides the intent multiplier the same way: Math.ceil(GAME_CONFIG.ENEMY_BUFF_POISON_STACKS × intent multiplier) — 3/4/5 stacks across acts 1/2/3 (BUILD 137 — base lowered from 5 to 3 for playtest readiness; the ×1.0/1.2/1.45 act scaling itself is unchanged). Stored on gameState.enemy.buffPoisonStacks, copied from the slot's own enemy config by beginFightFromSlot(), same as hp/intentMin/intentMax.
- The enemy Nat 1 self-poison (ENEMY_NAT_ONE, GAME_CONFIG.ENEMY_NAT_ONE_SELF_POISON) stays a flat, unscaled 5 — this is the one enemy number acts do not touch.
- Act 1's multipliers are both 1.0, so buildAct(1) is byte-identical to the pre-125 buildAct()'s output. Die face layouts (F20/F21, which faces carry poison/Nats) do not change per act — only the four scaled numbers above vary.

Transition — a boss win's outcome depends on which act it ends (runPhase()'s win branch, phase-machine.js):
- Acts 1 and 2: the same reward flow any other fight win gets — one die reward (GAME_CONFIG.DIE_REWARDS.SINGLE; 'Boss' is never 'Elite', so never the two-action grant) plus a card reward. boss_defeated still plays (it is still a boss kill), but the run does not end. Closing that reward flow (advanceRun(), run-and-map.js) increments gameState.run.actNumber and rebuilds gameState.run.act via buildAct(actNumber) for the next act, resetting currentSlot to 'opening' and lane to null — the same fields startNewRun() itself sets for act 1. Player hp, die and ownedCards are untouched; no heal between acts (D-27).
- Act 3 (the final act): true VICTORY (D-22) — run.outcome becomes 'won', no die reward, no card reward, exactly as a single-act boss win always worked before this build.

UI: the act number is shown on both the map screen ("ACT N MAP" title, renderMapScreen()) and the fight screen (#actStamp, set by refreshInspector() from gameState.run.actNumber) — gameState.run.actNumber is the only source for either.

---

# AUDIO MODULE

audio.js (BUILD 093, extended 094/095, retuned 096). One AudioContext plus a name-to-sound table, the same shape as the listener registry: game logic never calls a sound function directly, only playAudioEvent('event_name'); SOUND_TABLE decides what that sounds like, so any sound can be swapped by editing that one table without touching a call site. Synthesised only via playTone(waveform, freqStart, freqEnd, durationMs, peakGain, attackMs) — no audio files; /audio/ stays empty.

Browser autoplay policy: an AudioContext built before any user gesture starts (and stays) suspended — every scheduled sound is silent, no error — until resumed from inside a real gesture handler. unlockAudioOnce() (audio.js), called once on the page's first pointerdown (bootstrap.js), is that resume.

SOUND_TABLE — 22 events, no entries added/removed/renamed since BUILD 096: roll, roll_blank, card_attack, card_block, card_hybrid, mod_trigger, damage_enemy, damage_player, block_absorb, end_turn, nat_20, nat_1, fight_won, fight_lost, fight_start_normal, fight_start_elite, fight_start_boss, boss_defeated, die_action_load, die_action_strengthen, card_reward_basic, card_reward_rich.

Two pitch chains climb as an action repeats within a turn, reset to 0 by resetSoundChains() at every START_OF_TURN, capped at CHAIN_STEP_CAP (8): CARD_CHAIN_EVENTS = ['card_attack', 'card_block', 'card_hybrid'] (cardChainStep) and MOD_CHAIN_EVENTS = ['mod_trigger'] (modChainStep). playAudioEvent(eventName) threads the relevant chain's current step into a chained event's sound function and advances that chain's counter afterward; every non-chained event is called exactly as it always was, with no argument.

Mute: devMuteAudioCheckbox (index.html, dev chrome) sets the module-level audioMuted flag (bootstrap.js); playTone() returns immediately when muted, before scheduling any oscillator. Default unmuted.

---

# FIGHT RESET

Player: block→0, soul→maxSoul, deck/hand/discard reshuffled from ownedCards, poisonStacks→0, penitenceActive→false, penitenceTurnsRemaining→0, natOneFiredThisFight→false. hp carries over.
Enemy: hp→maxHp, poisonStacks→0, activeBuffs→[], natOneFiredThisFight→false. die is overwritten from the entering slot's own static config on next fight entry (beginFightFromSlot()), not reset here.
Die (player's): weights and mods unchanged. Persists between fights.
Turn: phase→'START_OF_TURN', cardsPlayedThisTurn→0, round→0.
Registry: no clearListeners('fight') call exists anywhere — removed in BUILD 092 as dead code, since no listener has ever registered with clearOn: 'fight'. Every fight-scoped field above is reset explicitly, field by field, in clearFightScopedState()/resetFight() (run-and-map.js), not by a registry sweep.
Run: status→'active'.
Run record (BUILD 109): entirely untouched by a fight reset — clearFightScopedState() never mentions gameState.runRecord. See RUN RECORD.

---

# RUN RECORD (BUILD 109)

Player-facing, not a dev tool. One line per run (human or bot), written to localStorage as the run happens — never reconstructed at the end — because a page opened via file:// (see PROJECT's "double-click index.html only" rule) cannot write files. gameState.runRecord (STATE SCHEMA) is run-scoped: reset only by startNewRun() (resetRunRecord(), run-and-map.js), untouched by a fight reset (FIGHT RESET above).

Write as it happens:
- enterSlot() (run-and-map.js) sets runRecord.started true and runRecord.node to the slot just entered (describeSlot() — 'opening' | '<lane>-<index>' | 'boss') on every real slot entry; the one time laneName is 'boss', it also captures runRecord.arrivalHpAtBoss = gameState.player.hp before the boss fight begins.
- dieActionChooseLoad() (rendering.js) pushes a `{ type:'load', offered:[modId,...], picked:null }` entry onto runRecord.dieActionEvents the moment the 3-mod offer is shown; dieActionPickLoadFace() patches that same entry's `picked` field once a mod is actually committed to a face. dieActionChooseSkip() pushes a `{ type:'skip' }` entry instead — a genuinely distinct outcome, not a Load offer with no pick (Skip is chosen at the top-level 'choose' step, before any offer is ever shown; there is no way to abandon a shown Load offer short of leaving the run itself, which the abandon-flush below already covers).
- runPhase()'s win/loss branches (phase-machine.js) call recordFightRoundEnd(gameState.enemy.id) the instant a fight ends (gameState.turn.round is still that fight's own final round count — it only resets at the next START_OF_TURN), pushing one `{ label, rounds }` entry onto runRecord.fightRounds. The boss-win and player-death branches also call flushRunRecord('won'|'lost') immediately after.

Per-mod trigger counts are never accumulated as the run goes — collectTriggerCountsByMod() (run-and-map.js) reads gameState.die.faces fresh every time the record is serialized, seeds every real mod at 0, and SUMS each face's own trigger count into that mod's running total (both a face's own modId and modId2 contribute). BUILD 118 (KI-19 fix): this used to overwrite (`counts[modId] = faceCount`) instead of sum, which stayed silently correct only because the real in-game Load flow never lets the same mod appear on two faces (DIE_ACTION_EXCLUDED_MOD_IDS + the "already on the die" check — see MULTI-MOD FACES) — but devLoadMod() and devLoadAll() (dev-tools.js) enforce no such thing, so a die built by either can genuinely carry one mod on many faces, and the old code kept only whichever face was processed last, silently discarding every other face's count for that mod. Now sums correctly regardless of how many faces carry a given mod, and a mod on zero faces still reports 0 rather than being missing from the trigger-counts column entirely.

Flush points — flushRunRecord(outcome) (run-and-map.js), guarded by runRecord.flushed/started so a run that never began writes nothing and a run cannot be flushed twice:
- Boss defeated → flushRunRecord('won') (phase-machine.js, inside the existing boss-win branch).
- Player death → flushRunRecord('lost') (phase-machine.js, inside the existing loss branch).
- New Run clicked while gameState.run.outcome is still 'active' → flushRunRecord('abandoned'), called in the #startGameBtn click handler (bootstrap.js) BEFORE startNewRun() resets the record for the run about to begin. A run already won/lost has already flushed itself, so this is a no-op then.
- A closed tab → the same flushRunRecord('abandoned') call, from a window 'beforeunload' listener (bootstrap.js) — best-effort, since a browser can refuse to run further script during unload, but the flush is synchronous localStorage.setItem(), not async.
An abandon mid-fight (screen 'fight', status still 'active' — the win/loss branches above never fired) has flushRunRecord() itself call recordFightRoundEnd() once more for that still-unfinished fight before serializing, so its partial round count is never lost.

Line format — buildRunRecordLine() (run-and-map.js) — one comma-separated CSV row, columns in this order: source, node, arrivalHpAtBoss, outcome, fightRounds, totalRounds, dieActionEvents, triggerCounts. Lists within a column are "|"-joined and key:value pairs "label:rounds" / "modId:count" — a raw comma never appears inside any column, so the result pastes into a spreadsheet as a clean CSV with no quoting needed. dieActionEvents encodes a load event as `load:mod1|mod2|mod3>picked` (picked empty if the offer was never resolved) and a skip as the literal `skip`, multiple events ";"-joined in chronological order. totalRounds is fightRounds' own rounds summed at serialize time, never tracked as a separate running total (the same "read from the one source of truth" principle TRIGGER COUNTS above already follows).

Storage and the copy button — RUN_RECORD_STORAGE_KEY = 'dieRunRecordLines' (run-and-map.js), a JSON array of line strings in localStorage, appended to by flushRunRecord(), never overwritten. #copyRunRecordBtn (index.html, wired in bootstrap.js) calls collectAllRunRecordLines() — every line ever flushed this browser profile, prefixed with the RUN_RECORD_CSV_HEADER row, joined with '\n' — and copies the whole thing to the clipboard via copyTextToClipboard() (navigator.clipboard.writeText(), falling back to a hidden-textarea/execCommand('copy') trick). The player pastes that into one CSV file in the project folder (C:\Users\figja\Die_v1) by hand — this game never writes to disk itself. source is always 'human' from real play today; the column exists so a future headless writer (tests/autoplay.js, which currently has no round-length column — the gap this build closes) can mark its own lines 'bot' and land in the same file.

---

# INIT FUNCTION

init() (cards-mods.js) runs once, on DOMContentLoaded (bootstrap.js), before anything else. It:
1. Populates config.cards with the 3 Ring 0 cards and the 15-card pool (config.cardPool references the same objects, not copies) — see CARDS
2. Populates config.classes with the Ordained class object — onNatTwenty/onNatOne/onBlankRoll are all implemented, not stubs
3. Populates config.mods with all 11 mods — see MODS
4. Registers the Ordained's three permanent passives (BLANK_ROLL, NAT_TWENTY, NAT_ONE), the enemy's own permanent buff/Nat listeners (ENEMY_BUFF_TRIGGER, ENEMY_NAT_TWENTY, ENEMY_NAT_ONE), and the generic MOD_TRIGGER dispatcher
5. Resets dev-chrome state to closed/unchecked regardless of any browser-restored form state
6. Logs [INIT] gameState initialised, then [INIT COMPLETE]
7. Calls startNewRun() (run-and-map.js), which builds the act (buildAct()), sets up the player's starting die and deck, and lands on the map screen — not mid-combat

Nothing in the game runs before init() completes. No phase machine, no listeners, no rolls before it.
init() must not dump full arrays into the log. Log a summary line per structure, not the whole faces array or deck array.

---

# BUILD METHODOLOGY

One substage per Claude Code session. No exceptions.
Every prompt starts with: Read CLAUDE.md.
Every prompt ends with: Do not change any other logic.
State only changes through helpers.
Everything attaches via listeners.
All damage through calculateDamage(). All block through generateBlock().
Stage 0: confirmed when log output matches expected output exactly.
Stage 1+: five consecutive clean fights with zero errors before next substage.
Never skip a failing substage. Never advance on partial verification.

---

# WHO EDITS THIS FILE

Claude Code may append a one-line confirmation to CONFIRMED WORKING and update CURRENT SUBSTAGE. Claude Code also updates whichever standing section describes a mechanic, number, file, or structure that build actually changed — see the ownership rule below. No other rewrite of a standing section happens without Fergus's own pasted text in the planning chat.

Every other change to this file — mechanics, rulings, schema, numbers, structure not touched by the build just shipped — is written in the planning chat and pasted in by Fergus.

OWNERSHIP RULE. Any build that changes a mechanic, a number, a file, or a structure updates the standing section describing it in the same build, and says so explicitly in its paste-back. The log (CONFIRMED WORKING / HISTORY.md) records that the change happened; the standing section records what is now true. A build that touches only CONFIRMED WORKING/CURRENT SUBSTAGE — no mechanic, number, file, or structure change — has nothing else to update.

BUILD 102: a build that changes any GAME_CONFIG value updates the F-line comment beside it in config.js's header and the FACTS block on the Notion page in the same build, and runs tests/facts.test.js before pasting back.

BUILD 117: every build updates #buildStamp (index.html) to that build's own stage and build number, in the same build, whether or not the build touches any other part of index.html — this is the one standing exception to "don't touch files the build doesn't need to." The stamp had gone stale repeatedly (stuck at BUILD 104 through BUILD 109, eleven builds) because nothing enforced this; it is not optional busywork, it is how a player or a future session can tell what code is actually running. Say so explicitly in the paste-back the way every other OWNERSHIP RULE update already does.

BUILD (KI-6): every build regenerates verify/ (`node tests/screenshots.js`) and reports the diff (`node tests/screenshots.js --compare`, changed-pixel count per screen against the previous run's set) in its paste-back — a visual defect this catches costs a diff to notice, not a build. Same standing-exception status as the build stamp line above: run it even in a build that doesn't touch index.html/rendering, so a regression from any build is caught by the next one's screenshot set, not discovered cold several builds later.

At the end of every session, paste the CONFIRMED WORKING and CURRENT SUBSTAGE sections back into the planning chat for verification.

This file is the source of truth for building. Notion is the source of truth for planning. Neither mirrors the other. The Notion project page is 3d27b97ff65a81d396d5f6abf687468d, titled Die — V1. The previous page, 3be7b97ff65a81cd8836fd33c0a08b70, is now the Archive and is not read at session start.

---

# DEV MODE — ALWAYS PRESENT, NEVER SHIPS

Face buttons: trigger any face result without rolling
State inspector: full gameState as JSON at all times
Pipeline inspector: calculateDamage() breakdown — base, additions, multipliers, final
Registry inspector: all listeners grouped by hook and clearOn
Log: every event and calculation. Prefixed [PHASE] [MOD] [CARD] [DAMAGE] [BLOCK] [LISTENER]

---

# CONFIRMED WORKING

Full reports for every build below live in HISTORY.md, verbatim, in order. This section is an index only — read only when a specific build's full detail (exact numbers, exact code paths, exact Playwright verification) is needed.

Stages 0.1-0.5 (early builds, no individual build numbers cited in the original log) — HTML shell and gameState object; phase machine skeleton; listener registration system; state mutation helpers and state inspector; init divider log line.
Stages 1.2-1.10 (early builds, no individual build numbers cited) — config sync and repair; build stamp; enemy roll and blank-face passive; MOD_TRIGGER verification; blank-roll bug fix; START_OF_TURN behaviour; card drawing and deck cycling; card play; enemy action; win and loss.
Stage 1.11 (BUILD 050) — first real card reward screen, three throwaway placeholder cards (TEST_A/B/C), first substage of Stage 1. Superseded by the real 15-card pool (BUILD 051-59, folded into this entry in the original log).
Stage 2.8 (BUILD 077) — run-outcome guards added on the two remaining unguarded dev paths.
Stage 2.9 (BUILD 078) — riteStep dev-jump fix identified in BUILD 077's trace, applied.
Stage 2.10 (BUILD 079) — the die action screen's own 20-row die becomes the Load/Strengthen face picker, replacing two separate face-button screens.
Stage 2.11 (BUILD 080) — presentation pass on the BUILD 079 die-row picker, three browser-observed bugs fixed, CSS only.
Stage 2.12 (BUILD 081) — widened the Load/Strengthen picker's die rows to stop mod-name/badge clipping, one CSS rule.
Stage 2.13 (BUILD 082) — act grows from three lane slots to five; the elite grants two die actions instead of one.
Stage 2.14 (BUILD 083) — all dev chrome moved behind one toggle, closed by default; the setup pause made optional.
Stage 2.15 (BUILD 084) — Nat 1 becomes once per fight; Penitence changed from "rest of the fight" to 3 turns.
Stage 2.16 (BUILD 085) — boss intent band dropped from 16-28 to 10-20, average 15.
Stage 2.17 (BUILD 086) — elite intent band dropped from 12-22 to 10-18, average 14.
Stage 2.18 (BUILD 087) — New Run moved off End Turn and gated behind a confirm when a run is live.
Stage 2.19 (BUILD 088) — third rite option added, Remove a card, alongside Heal and Take a die action.
Stage 2.20 (BUILD 089) — the class card can now be removed at a rite; one discard restriction dropped.
Stage 2.21 (BUILD 090) — rolled-face highlight added to the player's die column.
Stage 2.22 (BUILD 091) — the file split: one ~3,217-line inline script becomes nine files in /js/, script-tag loaded, structural only.
Stage 2.23 (BUILD 092) — three engine fixes from the BUILD 091 code audit: registerListener dedup guard, dead clearListeners('fight') removed, healPlayer()/ON_HEAL added.
Stage 2.24 (BUILD 093) — three independent UI/audio items, verified and reported separately; the audio module (audio.js) is introduced.
Stage 2.25 (BUILD 094) — sound pass: twelve new synthesised sounds added on the BUILD 093 audio module.
Stage 2.26 (BUILD 095) — sound chain mechanic (two pitch chains, 8-step cap) plus five new sounds.
Stage 2.27 (BUILD 096) — tone pass retuning existing sounds; no new sounds, events, gameplay, numbers, or visuals.
Stage 2.28 (BUILD 097) — the boss die's enemy half: poison, Nat 20, and Nat 1 wired up on the boss's own die for the first time.
Stage 2.29 (BUILD 098) — Nat 1 self-poison corrected to a flat 5; elites given their own die (poison on 7 and 14, no Nat faces), closing the normal/elite/boss distinction.
Stage 2.30 (BUILD 099) — this documentation rewrite: standing spec sections restated to match the code, CONFIRMED WORKING compacted to this index, full history moved to HISTORY.md.
Stage 2.31 (BUILD 100) — Vigil audited and confirmed already working (BUILD 099's "dead hook" finding was a documentation error, not a code bug); tests/mods.test.js added as a standing per-mod regression suite for all eleven mods.
Stage 2.32 (BUILD 101) — enemy roll made visible: resolveEnemyRoll() audited and confirmed already fully resolving (not a no-op); the rolled-face highlight (BUILD 090, player-only) extended to the enemy die column via new enemyRollOutcome/enemyRolledFaceNumber turn fields.
Stage 2.33 (BUILD 102) — one constants file: js/config.js (new, loaded first — eleven files total) holds GAME_CONFIG plus the FACTS block (F01-F28) verbatim from Notion; every tunable number/structural constant the prompt named now reads from it; tests/facts.test.js (new, 23/23 passing) asserts every fact against GAME_CONFIG and live gameState; F28's wording corrected to the real measured sound-duration exemption set (four sounds beyond the two Nat sounds it previously named).
Stage 2.34 (BUILD 103) — trigger-count indicator: gameState.die.triggerCounts (new, fight-scoped) tracks how many times each face has triggered, bumped once per MOD_TRIGGER (cards-mods.js's mod_dispatch, the single funnel both a normal roll and Nat 20's loop already use); renderDieList() badges every loaded face on the player's own die with ↻N (zero renders nothing), same box/font/size as the ×N weight badge. Closes KI-11.
Stage 2.35 (BUILD 104) — font unification pass: every player-facing text selector that didn't already carry var(--game-font) at .panel-title's own weight (die-mod, face-btn, die-weight, die-trigger-count, stat-label, stat-value) now does. No layout/colour/gameplay change; font-family/font-weight only.
Stage 2.36 (BUILD 105) — tests/autoplay.js: a headless autoplayer, a measurement tool (never ships), plays complete runs against real gameplay only (New Run, map clicks, playCard(), nextPhase()'s own natural rolls, the real die-action/rite/card-reward panels — never a dev forcing tool) under a seeded Math.random and one fixed, in-file-documented policy, writing one CSV row per run to tests/autoplay_results.csv. No gameplay/numbers change. See tests/ list above for the full description.
Stage 2.37 (BUILD 113) — twelfth mod, Anthem: 6 damage plus 4 per point of weight on its own face (weight 1 -> 10, weight 2 -> 14, weight 3 -> 18). Reuses dealDamage() tagged 'attack' (Smite's path — Fervour doubles it identically) and gameState.turn.rolledFaceWeight (Covenant's own read) — no new function, no new listener, no new sound. tests/mods.test.js gained one test (12/12); tests/facts.test.js's F24 updated to 12 (23/23 unchanged). See HISTORY.md for the Playwright verification.
Stage 2.38 (BUILD 114) — thirteenth mod, Elevation: 10 damage; the face directly above the triggering face (faceNumber + 1) permanently gains +1 weight, but only if that face is loaded and is not face 20. Weight write goes through strengthenFace() (pipeline.js), the only place any face's weight is ever written — same as Ordain. No new function, no new listener, no new sound. tests/mods.test.js gained one test (13/13); tests/facts.test.js's F24 updated to 13 (23/23 unchanged). See HISTORY.md for the Playwright verification.
Stage 2.39 (BUILD 115) — multi-mod faces: a face can now hold up to two mods (modId, modId2 — load order), cap two, never three. Both trigger on that face's roll, first-loaded first, each resolving fully before the next; a two-mod face triggers both under Nat 20 too, within its own turn in the ascending sequence. Load offers a second mod onto an already-loaded face only once no blank face remains. Display: both mod names stack in the one die row (new .die-mod-stack), no new row, still twenty rows. Face weight still only ever written by strengthenFace() — untouched by this build. Faces 1/20 untouched. tests/mods.test.js gained three tests (16/16); tests/facts.test.js unchanged (23/23). See HISTORY.md for the Playwright verification.
Stage 2.40 (BUILD 108) — per-mod trigger counts: BUILD 103's separate gameState.die.triggerCounts array (fight-scoped, indexed by face number) folded into each face's own modData — modData.triggerCount for modId, modData.triggerCount2 for modId2 on a two-mod face — matching the carry-forward rule the original array had violated. Now run-scoped, not fight-scoped: survives a fight reset (Restart Fight), wiped only by a brand new run. mod_dispatch (cards-mods.js) matches data.modId against the firing face's modId/modId2 to bump only the mod that actually triggered. Zeal's own effect corrected in the same build to merge into modData instead of replacing it wholesale (it would otherwise have erased a face's trigger count on every Zeal trigger). Display: one-mod face unchanged (↻N); two-mod face shows both counts separated by a slash, first-loaded mod first (↻N1/N2). tests/mods.test.js gained four tests (20/20); tests/facts.test.js unchanged (23/23). See HISTORY.md for the Playwright verification.
Stage 2.41 (BUILD 109) — the run record: a player-facing, localStorage-backed log of every run (see RUN RECORD), built from scratch (no prior copy-run-line button or clipboard/localStorage code existed anywhere — flagged and approved before writing any code). Every Load offer shown/picked, every Skip, per-mod trigger counts (read fresh off face.modData at flush time), node reached, arrival HP at the boss, outcome, per-fight and total round counts, and a source column ('human'/'bot') — written as it happens, flushed on win/loss/New-Run-mid-run/tab-close. New #copyRunRecordBtn dumps everything stored to the clipboard. Build stamp corrected to BUILD 109 (was stuck at BUILD 104). tests/facts.test.js gained five tests (28/28); tests/mods.test.js unchanged (20/20). See HISTORY.md for the Playwright verification.
Stage 2.42 (BUILD 116) — three fixes: the trigger-count badge's '↻N' glyph replaced with a legible '#N' label at a bigger font size (14px -> 17px, with an explicit line-height fix for a 1px row-growth bug that font bump exposed); weight made visible on the face itself via a currentColor fill inside face-btn, scaled to weight (no new colour — see DIE FACE OBJECT STRUCTURE's weight display note); and BUILD 115's "second mod only once no blanks remain" Load gate removed entirely — any loaded, non-Nat, not-yet-full face is now a valid Load target at any point in a run (see MULTI-MOD FACES). tests/facts.test.js gained three tests (31/31); tests/mods.test.js unchanged (20/20). See HISTORY.md for the Playwright verification.
Stage 2.43 (BUILD 117) — three die-row fixes plus one investigation: two mods now render side by side on one line, each with its own trigger count beside it (replaces BUILD 115's vertical stack, which broke row height — a genuine layout bug found and fixed this build, not assumed away); the weight fill is now visible starting at weight 2 (raised floor + opacity — weight 2 previously looked identical to weight 1 in real play); anthem and elevation both gained the MOD_DESCRIPTION hover entries they'd been missing since their own builds (checked, not just Elevation, the one symptom reported — anthem had the identical gap). The run-record dieActionEvents gap was investigated and could not be reproduced in three realistic scenarios; no fix applied since no defect was found. Build stamp corrected to BUILD 117, and CLAUDE.md gained a standing instruction (WHO EDITS THIS FILE) that every build updates it going forward. tests/facts.test.js gained three tests (34/34); tests/mods.test.js unchanged (20/20). See HISTORY.md for the Playwright verification.
Stage 2.44 (BUILD 118) — KI-19: collectTriggerCountsByMod() (run-and-map.js) OVERWROTE a mod's trigger count with whichever face was processed last instead of summing across every face carrying it, silently discarding every earlier face's count — invisible through the real Load flow (which can never put the same mod on two faces) but real once a die was built through devLoadMod(), exactly how two real runs recorded consecrate:1/blight:0 against dice where those mods sat on many faces with real nonzero counts. Now sums correctly across faces and both slots, and seeds every real mod at 0 so one never loaded this run reports 0 instead of being omitted. New dev tool, Load All (#devLoadAllBtn): fills every available slot on the player die with whichever mod is selected in the existing dev-load dropdown — a stress tool built specifically to reach the "same mod on many faces" shape this bug needed to surface, and to make it trivial to build again for any future test. tests/facts.test.js gained three tests (37/37); tests/mods.test.js unchanged (20/20). See HISTORY.md for the Playwright verification.
Stage 2.45 (BUILD 119) — measurement session, not a feature build: re-measured the bot win rate D-08 recorded at 34% at BUILD 105. No design/policy change to the bot (resolveDieAction()/LOAD_PRIORITY untouched); two run-record plumbing additions to tests/autoplay.js only — source:'bot' tagging via updateRunRecord() right after #startGameBtn's click, and playRun() now returns its own run's localStorage-captured run-record line(s) (needed once browser.newPage()'s per-call context isolation was discovered this build to prevent localStorage accumulating across a batch's runs the way a human's persistent profile would). 400 seeded games (seeds 1-400, asBuilt, riteHeal 20): win rate 29.75% (119/400), down from 105's 34%. Second-slot usage and Strengthen-20 usage both measured at 0, for structural reasons stated in full in CURRENT SUBSTAGE, not because run-record coverage missed them. tests/facts.test.js and tests/mods.test.js both unchanged (37/37, 20/20). New git-trackable artifact: run_record_master.csv (404 lines incl. header) in the project root, per D-40. See CURRENT SUBSTAGE for the full measurement and the Playwright verification.
Stage 2.46 (BUILD 112) — "make verification honest": closes KI-14 (facts.test.js's circular comment-vs-GAME_CONFIG assertions now labelled SPEC-ONLY, 18 assertion-sites/14 tests, real behavioural assertions untouched), KI-9 (checked the resetFight()/clearListeners('turn') gap empirically — real for a moment, never reaches actual gameplay), and KI-2 (one-line run.status guard on playCard()); builds the registry inspector from REG-E2. tests/facts.test.js gained two tests (39/39, up from 37/37); tests/mods.test.js unchanged (20/20). Found and reported, not fixed: BUILD 107 has no CLAUDE.md index line either, and "Stage 2.37" is used by both BUILD 107 and BUILD 113. See HISTORY.md for the full write-up and the Playwright verification. (This index line itself was missed when BUILD 112 landed — added retroactively by BUILD 120, which found the gap.)
Stage 2.47 (BUILD 120) — the screenshot baseline KI-6 had queued: tests/screenshots.js (new) drives to five screens (map, a fight with a two-mod face and a strengthened face visible, the die action panel, the card reward panel, the dev drawer open) and saves one PNG each into verify/ (new, git-trackable), overwriting every run, on the same chromium.launch()/browser.newPage()/page.goto(FILE_URL) path every other test file already uses. `--compare` mode rotates the previous verify/ set into verify_prev/ and reports a per-screen changed-pixel count via tests/pngdiff.js (new) — a hand-rolled PNG decoder over Node's built-in zlib, no new dependency added. First baseline generated and confirmed real (two files opened and visually inspected); a real bug was caught and fixed while building it — see CURRENT SUBSTAGE. New standing instruction (WHO EDITS THIS FILE): every build regenerates verify/ and reports the diff in its paste-back. tests/facts.test.js and tests/mods.test.js both unchanged (39/39, 20/20) before and after — no js/ file was touched. See CURRENT SUBSTAGE for the full build and the caught-bug detail.
Stage 2.48 (BUILD 121) — D-53 sweep: every bare use of "stacks" (a stacking-status count with no status named beside it) in a log string or a GAME_CONFIG comment now names the status it counts. Three log-string fixes, all in the poison path only (poison is the only stacking status built, per D-53) — cards-mods.js's enemy-buff-on-player line and its Nat-1 self-poison line, both changed from "...stacks now N" to "...N stacks of poison to X, now N stacks of poison"; phase-machine.js's enemy poison-tick line changed from "damage, stacks now N" to "damage, N stacks of poison remaining". config.js's mirrored FACTS comments (F19, F23) reworded the same way, synced to 1 State §2.1 in the same build. MOD_DESCRIPTION (rendering.js) audited — no bare "stacks" found there; none of the thirteen mod descriptions use the word. Variable names (poisonStacks, GAME_CONFIG.*_STACKS, etc.) untouched, per the prompt's explicit instruction — this is wording only, no gameplay change. tests/facts.test.js and tests/mods.test.js both unchanged (39/39, 20/20) before and after. See CURRENT SUBSTAGE for the Playwright-verified log line.
Stage 2.49 (BUILD 122) — KI-18, the stale build stamp: GAME_CONFIG.BUILD (config.js, new) is now the single source of truth for the on-screen build number. index.html's #buildStamp keeps its own static "STAGE X.Y" text but the number after "BUILD" is a new empty `<span id="buildStampNumber">`, written once at init() (cards-mods.js, right before the existing "[INIT] gameState initialised" log line) from GAME_CONFIG.BUILD — no second hand-typed copy of the number left anywhere in index.html. The standing CLAUDE.md instruction alone (added BUILD 117) had already failed twice (BUILD 116, BUILD 118 — KI-18), so the fix this time is a test, not a third instruction: tests/facts.test.js gained one new test that reads GAME_CONFIG.BUILD from config.js and independently reads CLAUDE.md's own CONFIRMED WORKING section, extracts every "(BUILD nnn)" entry it names, and asserts the highest one equals GAME_CONFIG.BUILD — plus a live-page check that the rendered #buildStampNumber DOM text matches. A future build that bumps GAME_CONFIG.BUILD without adding its own CONFIRMED WORKING line (or vice versa) now fails this test immediately instead of being noticed cold several builds later. tests/facts.test.js gained one test (40/40); tests/mods.test.js unchanged (20/20). FOUND, NOT FIXED: this build never wrote a CURRENT SUBSTAGE section — see BUILD 123's CURRENT SUBSTAGE entry.
Stage 2.50 (BUILD 123) — checkpoint 1 on 8 North star, OQ-13: LOAD_PRIORITY (tests/autoplay.js) replaced with the twelve loadable mods in Fergus's own rank order from 1 State §6 (Fervour 1 through Vigil 12; Consecrate is the anchor, never ranked, never offered) — queried directly against the live Notion mod-pool database and confirmed to match byte-for-byte. Replaces the BUILD 105 hand-tuned 9-mod list (missing Virulence, Anthem, Elevation entirely, and special-casing Virulence-after-Blight) with a flat rank walk — nothing left to decide, per the checkpoint's own framing. New test, tests/facts.test.js: "BUILD 123: LOAD_PRIORITY ranks every loadable mod exactly once" — reads the live page's gameState.config.mods, asserts LOAD_PRIORITY has no duplicates and matches the loadable set exactly; tests/facts.test.js gained one test (41/41); tests/mods.test.js unchanged (20/20). Re-ran the single-combo 400-run seeded baseline (seeds 1-400, asBuilt, riteHeal 20) that D-08's 34% and BUILD 119's 29.75% both rest on: **win rate 25.8% (103/400)**, median arrival HP 38, median faces at boss 7, median final round 6 — this is now the number D-08 should read, down from both prior measurements. See CURRENT SUBSTAGE for the caught invocation bug and two found-not-fixed documentation gaps.
Stage 2.51 (BUILD 124) — closes the CSV stale-build-stamp bug BUILD 123 flagged: tests/autoplay.js's own `const BUILD = 105` (never bumped since the file was written) used to stamp every CSV row's `build` column regardless of which build actually produced the run; removed outright, and the column is now read live from GAME_CONFIG.BUILD per run via `page.evaluate()`. tests/facts.test.js gained one new test ("BUILD 124: autoplay.js has no own BUILD constant and stamps its CSV from GAME_CONFIG.BUILD") that asserts the source has no such constant and that a real one-run batch's CSV row carries GAME_CONFIG.BUILD; tests/facts.test.js gained one test (42/42); tests/mods.test.js unchanged (20/20). Existing tests/autoplay_results.csv rows (1,606 total, all bot-sourced) relabelled by provable build boundary, never guessed: rows 1-402 → BUILD 105, 403-804 → BUILD 119, 805-1606 → BUILD 123, established from backups/ snapshot row-counts at four timestamps cross-checked against each build's own documented batch composition (BUILD 105's 2 reproducibility rows + 400-run 4-combination sweep; BUILD 119's 2 reproducibility rows + single-combination 400-run baseline; BUILD 123's caught-bug 402-row default-protocol misfire + its corrected 400-run baseline) — content (dieRewardRule/riteHeal/seed pattern) matched exactly at every claimed boundary, not inferred from timestamps alone. D-40's own run_record_master.csv has no build column at all (RUN_RECORD_CSV_HEADER, run-and-map.js) and was not touched. See CURRENT SUBSTAGE for the full evidence trail and the paste-back detail.
Stage 2.52 (BUILD 125) — checkpoint 2, the three-act run (F31/F32): buildAct(actNumber) now bakes per-act enemy scaling (GAME_CONFIG.ACT_HP_MULTIPLIER/ACT_INTENT_MULTIPLIER, [1.0,1.4,1.9]/[1.0,1.2,1.45], Math.ceil'd at act-build time) into every enemy's hp/intentMin/intentMax and the enemy buff's own poison amount (gameState.enemy.buffPoisonStacks, 5/6/8 across acts 1/2/3); the enemy Nat 1 self-poison stays flat, unscaled. Beating the act 1 or act 2 boss now grants a die + card reward exactly like any fight win, then starts the next act (advanceRun()'s new 'boss' branch — same currentSlot:'opening'/lane:null startNewRun() itself sets, no heal, per D-27); only the act 3 boss is VICTORY, no reward (D-22, unchanged there). Act number shown on both the map screen title and a new #actStamp on the fight screen. tests/facts.test.js gained two new tests (44/44); tests/mods.test.js unchanged (20/20). FOUND, NOT FIXED: the build stamp's hand-typed "STAGE X.Y" text stayed at 2.51 this build (the GAME_CONFIG.BUILD-driven number itself was correct) — caught and fixed at BUILD 126. See ACTS (new standing section) for the full mechanic and HISTORY.md for the verification.
Stage 2.53 (BUILD 126) — pool exhaustion stopgap (D-54): dieActionChooseLoad() now converts to Strengthen the instant fewer than three unloaded, non-anchor mods remain in the pool, before dieActionMods/dieActionStep are ever touched — a real 3-mod Load offer can no longer be built at that point, so this is what keeps that offer from ever coming up short or repeating a mod. tests/facts.test.js gained one new test (45/45); tests/mods.test.js unchanged (20/20). Also closes BUILD 125's own build-stamp gap (index.html's stage text was stuck at 2.51; now 2.53, covering both the missed bump and this build's own). REVISED AT BUILD 127: this click-time redirect was a real behaviour fix but not a UI fix — a player still saw a clickable "Load" button that silently did something else, and the bot (which calls functions directly, never DOM buttons) had no way to know the redirect had fired, causing a real crash during BUILD 127's three-act baseline. The Load button is now hidden outright at the 'choose' step once the pool is this low (renderDieActionPanel(), gated on the same new eligibleLoadModIds() count); dieActionChooseLoad() itself keeps the redirect as a defensive fallback for a direct call, but the button is the real fix. See BUILD 127's CONFIRMED WORKING and CURRENT SUBSTAGE.
Stage 2.54 (BUILD 127) — the bot plays all three acts (checkpoint 2 closes): tests/autoplay.js's playRun() now loops every act via a new playOneAct() helper instead of stopping at one boss; tests/autoplay_results.csv gained an actReached column (existing rows blanked, never guessed). Found and fixed mid-build: the bot crashed once a real 400-run baseline actually reached pool exhaustion (never possible in a single-act run) — traced to D-54's BUILD 126 fix being a click-time redirect a DOM-clicking player would never trigger by accident but a function-calling bot could reach in a state it didn't check for; fixed by hiding the Load button outright (see BUILD 126's amended entry) and updating the bot's own policy and the D-54 test to match. Test suites unchanged (20/20 mods, 45/45 facts — the D-54 test rewritten, not added). A 20-run smoke check confirmed three-act runs complete cleanly post-fix; the full 400-run baseline was not run this session, on explicit instruction mid-build. See CURRENT SUBSTAGE for the full account.
Stage 2.55 (BUILD 128) — checkpoint 3 map: each lane grows from five slots to eight (F16), so every act now has 7 fights (opening + 5 lane fights/elite + boss), 21 a run. buildAct() (run-and-map.js) keeps each lane's original five slots as an exact prefix (byte-identical shapes to the pre-128 build) and appends three more (Fight, Fight, Rite) — upper: Fight, Rite, Fight, Elite, Rite, Fight, Fight, Rite; lower: Fight, Rite, Fight, Fight, Rite, Fight, Fight, Rite. A normal fight's enemy "type" (which of the three NORMAL_FIGHT_HP entries it uses) is a per-lane 0/1/2 rotation over slot order (rites/the elite don't advance it), which by construction never repeats on the very next normal fight in that lane; each normal fight's enemy now carries its own normalTypeIndex for this to be asserted directly. Rewards (a card after every non-final-boss win; die rewards fight 1/elite 2/rite 1/boss 1 per non-final act, none on the final boss) needed no change — phase-machine.js's win branch already keys off wonSlot.label/currentSlot==='boss', not a slot index, so it generalises to the extra slots automatically. GAME_CONFIG.SLOTS_PER_LANE is now 8; ELITE_ALLOWED_SLOT_INDICES narrowed to [3] (F16 now names one slot, not a range); new RITE_SLOT_INDICES [1,4,7]. KI-24 (D-29): refreshInspector()'s enemy panel no longer hides the die column outright for a dieless (hasDie: false) enemy — it now renders the column with an empty die slot (renderDieList() also fixed to clear a container's stale rows when handed zero faces, not just early-return), the same code path for every act, closing the gap for acts 1-3 alike. KI-25 (D-53): the two remaining bare "N poison" log lines (cards-mods.js's blight and virulence mod effects) now read "N stacks of poison" ("...doubled to N stacks of poison" for virulence), matching every other poison log line's BUILD 121 wording. tests/autoplay.js's playOneAct() lane traversal is now read live off gameState.run.act.upper's own length/slot types instead of a hardcoded 5-slot array, so the same bot policy plays both the old and new lane shapes with no code change; it also now tracks every completed fight's own round count (not just each run's last) and each act's own boss-arrival HP, so runBatch()'s summary reports mean rounds/fight and per-act mean HP entering that boss. tests/facts.test.js gained three new tests (F16 rewritten for the 8-slot shape, a no-repeat-adjacent-normal-enemy assertion, a 7-fights-per-path assertion) plus the KI-25 log-wording assertion (48/48); tests/mods.test.js unchanged (20/20). 200-run seeded bot baselines (seed 1-200, asBuilt, riteHeal 20) before/after: see this build's paste-back for the full before/after table (win rate, mean rounds/fight, mean HP entering each act's boss).

Stage 2.56 (BUILD 129) — checkpoint 3 tiers: every offerable mod and card now carries a tier field ('common'/'uncommon'/'rare', gameState.config.mods/cardPool) — mods: common Smite/Sanctuary/Penance/Blight/Offering/Anthem, uncommon Vigil/Ordain/Zeal/Elevation, rare Virulence/Fervour; cards: common Rebuke/Censure/Vestment/Litany/Scripture/Interdict/Orison/Censer/Purge, uncommon Judgement/Reckoning/Communion/Rapture/Covenant/Retribution. Consecrate and the three starters (Strike/Ward/Rite) carry no tier field and remain unofferable, unchanged. Card rewards (openCardRewardScreen()) and Load offers (dieActionChooseLoad()) now each roll a tier per choice, independently, off a per-slot [common,uncommon,rare] split (GAME_CONFIG.TIER_SPLIT) keyed by which slot the reward/offer came from (rendering.js's new currentOfferTierSplit()): fight/rite 65/30/5, elite 40/40/20, boss 0/70/30 — a rite's Load offer (dieActionOrigin === 'rite') shares the fight split. New shared picker (state.js's rollTier()/pickFromTier()/pickTieredOffer()) walks GAME_CONFIG.TIER_ORDER down (toward common) then up (toward rare) whenever a rolled tier's pool is empty, and never repeats a piece within one offer. D-54's Load-visibility rule changed: the Load button now shows whenever a blank face exists among faces 2-19 (renderDieActionPanel()), with the existing fewer-than-3-eligible-mods gate kept only as a last guard underneath it (dieActionChooseLoad()'s own defensive Strengthen-conversion fallback is unchanged). tests/facts.test.js gained five new tests — every mod's tier, every card's tier, Consecrate/Strike/Ward/Rite never offered (500 samples each of Load and card reward), a seeded 10,000-choice fight-split sample within 2 points of 65/30/5 (fallbacks tallied separately, off the rolled tier not the delivered piece's tier), and a 2000-sample x 3-splits x 2-pools no-repeat check — 48/48 -> 53/53; tests/mods.test.js unchanged (20/20). See CURRENT SUBSTAGE for the full write-up.

Stage 2.58 (BUILD 131) — checkpoint 3, sixteen new cards needing no new engine code: card count 15 -> 31 (F25 updated in config.js's header comment and tests/facts.test.js's own assertion — CLAUDE.md itself carries no separate copy of the FACTS text, only config.js's header and the Notion "Die — V1" page do, per PROJECT's own description of config.js; the Notion page was not reachable this session and was not updated — flagged, not silently skipped). All sixteen added to gameState.config.cards and referenced (not copied) into config.cardPool exactly like every existing reward-pool card: Tenet (uncommon, Growth+Mass — 6 damage +3 per the rolled face's own modData.triggerCount, run-scoped per BUILD 108, cap 24; a blank rolled face carries no modData so the read falls through to 0, the same fallthrough Zeal/Cope's own bonus reads already use); Gradual (uncommon, Mass — 3 damage +3 per the heaviest loaded face's weight, cap 12); Vacancy (common, Mass — 1 damage per blank face among faces 2-19, cap 16); Lauds (common, Growth — 4 damage +3 per loaded mod carrying the Growth tag across both slots, two mods on one face counting twice, cap 13, same modId/modId2 tag scan Congregation already uses); Chastise (common, no tags — 7 damage); Cloister (common, no tags — 7 block); Psalm (common, no tags — draws 1 through the same drawCards() path Scripture already uses); Reliquary (common, Bastion — 6 block, plus 5 damage if block was 10 or more before playing it, block read before Reliquary's own dealBlock() call raises it); Vindication (rare, Bastion — damage equal to twice block, cap 24, block read only, never spent, same shape Retribution/Anathema already use); Myrrh (uncommon, Poison — 6 block +1 per stack of poison on the enemy, cap 12); Exequy (rare, Poison — damage equal to the enemy's stacks of poison, cap 12, stacks not removed); Hosanna (common, Soul — 6 damage, 11 if soul is still 3 or more after playCard()'s own cost deduction, which runs before a card's effect(), so no separate "after paying" read was needed); Gloria (uncommon, Soul — 30 damage, cost 4, no special-case code: renderCardButtons()'s existing soul>=cost check already dims it on base soul 3 like any other card); Oblation (rare, Soul — spends all soul via the same updatePlayer({soul:...}) path Communion/Largesse already use, 7 damage per soul spent, cap 42, soul becomes 0); Tabernacle (common, Mass — 3 block +3 per the rolled face's own weight, reading gameState.turn.rolledFaceWeight exactly as Covenant already does, cap 12); Jubilee (rare, Growth+Mass — 4 damage +2 per weight added to the die this run, cap 24, weight added being the sum of all 20 faces' own weight fields minus 20, since every face starts at weight 1). tests/facts.test.js gained eighteen new tests — one per card (two each for Tenet, Oblation and the Gloria/Reliquary/Vacancy/Lauds/Hosanna condition-plus-cap pairs folded into their own single tests) — each playing the real card through playCard() (never card.effect() directly), picking inputs that push the raw value past the cap so the cap is actually exercised, plus the reward-pool tier and tags deepStrictEqual maps extended for all sixteen — 63/63 -> 81/81; tests/mods.test.js unchanged (26/26). GAME_CONFIG.BUILD bumped 130 -> 131; index.html's stage text corrected to 2.58 in the same build (WHO EDITS THIS FILE / KI-18).

Stage 2.57 (BUILD 130) — checkpoint 3 tags, plus six new mods needing no other new engine code: every mod and card now carries a `tags` field (a list of words, several allowed per piece) — Poison: Blight/Virulence/Censer/Purge/Reckoning; Mass: Fervour/Elevation/Zeal/Ordain/Anthem/Covenant; Growth: Zeal/Elevation/Ordain; Soul: Consecrate/Offering/Communion/Rapture; Bastion: Sanctuary/Penance/Vigil/Retribution; everything else (including the three starters) gets `[]`. Six new mods, mod count 13 -> 19 (F24 updated in config.js's header comment, CLAUDE.md's FACTS, and tests/facts.test.js's assertion, all three): Largesse (common, Soul — +2 soul, 4 block); Tithe (uncommon, Soul — end of round, 5 damage per soul remaining, capped 20, registered on the same generic phase-name hook Vigil already uses); Congregation (uncommon, Growth — 8 damage, 16 if another loaded mod carries the Growth tag, never counts itself, which falls out for free since eligibleLoadModIds() already forbids loading the same mod twice); Cope (common, Growth+Bastion — 8 block, permanently +2 per trigger, stored in the triggering face's own modData exactly the way Zeal already stores its +4, ARCH-CF2); Anathema (uncommon, Bastion — end of round, damage equal to current block capped 16, block read not spent, same END_PLAYER_TURN-equivalent hook as Tithe/Vigil); Thurible (common, Poison — 8 damage, applies 3 stacks of poison, log line reads "stacks of poison" from the start, never bare). All six added to config.mods (offerable through the existing tier/Load machinery unchanged) and appended to the end of tests/autoplay.js's own LOAD_PRIORITY list (never run this session, per instruction). tests/mods.test.js gained six new per-mod tests (20/20 -> 26/26); tests/facts.test.js gained ten new tests — mod tags, card tags, the three starters' empty tags, one behavioural test per new mod (numbers and cap), and a Load-offerability spot-check (ARCH-CF6) — plus updated its F24/D-54/collectTriggerCountsByMod/MOD_DESCRIPTION-coverage assertions for 19 mods (53/53 -> 63/63). See CURRENT SUBSTAGE for the full write-up.

Stage 2.59 (BUILD 132) — checkpoint 3, trigger a face outside a roll (prompt D): one shared function, triggerFaceOutsideRoll() (pipeline.js) — refuses face 1/20, refuses a face already triggered this way once this round (gameState.turn.outsideTriggeredFaces, cleared at START_OF_TURN), and refuses once GAME_CONFIG.ROUND_TRIGGER_CAP (D-51, new, 10) is reached — used by three new pieces: Threnody (card, uncommon, cost 2, Growth — the lowest-numbered loaded face triggers), Reverberation (card, rare, cost 2, Mass — the rolled face triggers again, or 6 block on a Nat roll), Magnificat (mod, rare, Mass — the heaviest other loaded face triggers, ties to the lowest number, own face never counts). Every real MOD_TRIGGER dispatch counts toward the round cap except a Nat 20 sweep, tagged natTwentySweep to exempt it. Mod count 19 -> 20, reward card count 31 -> 33 (F24/F25 updated in config.js's header comment and tests/facts.test.js's assertions). tests/mods.test.js gained four new tests (Magnificat plus three engine-level triggerFaceOutsideRoll() tests: refuses 1/20, refuses a repeat trigger, stops at the cap) — 26/26 -> 30/30; tests/facts.test.js gained six new tests (Threnody, Reverberation x3, a Magnificat Load-offerability spot-check, plus the count/tier/tags map updates) — 81/81 -> 86/86. See CURRENT SUBSTAGE for the full write-up.
Stage 2.60 (BUILD 133) — checkpoint 3, the Bound engine: a die-wide Bound scan (runBoundScan(), pipeline.js), a fight-scoped Bound setter (grantBoundToFace()), a fast sweep (playSweep() — Nat 20 sweeps and Bound scans both speed up after the round's third trigger, timing only), and three plain Bound mods (Unison 6 damage, Accord 10 block, Kinship 4 stacks of poison — all common, tags Bound). Mod count 20 -> 23 (F24 updated in config.js's header comment and tests/facts.test.js's assertions). tests/mods.test.js gained seven new tests (Unison, Accord, Kinship, two loaded Bound faces firing together, the scan not running during a Nat 20 sweep, the setter's refusals, and grants clearing at fight end while unrelated modData survives) — 30/30 -> 37/37; tests/facts.test.js unchanged in count (86/86), updated in place for the new mod total. See CURRENT SUBSTAGE for the full write-up.
Stage 2.61 (BUILD 134) — checkpoint 3, the remaining Bound pieces, closing checkpoint 3: Kyrie (card, common, cost 1, tags Bound — 5 damage, 10 if the rolled face has Bound, via isBoundFace()); Novena (card, rare, cost 2, tags Bound — every loaded Bound face triggers, each through triggerFaceOutsideRoll(), paced by playSweep() the same way runBoundScan() already is); Canticle (card, uncommon, cost 1, tags Bound — 6 block; if the rolled face is loaded, it gains Bound for this fight via grantBoundToFace(), never face 1 or 20); Concord (mod, uncommon, tags Bound+Soul — +1 soul, 3 block, same shape as Largesse's own soul+block combo); Herald (mod, rare, tags Bound — 6 damage; one other random loaded face without Bound gains Bound for the fight, through grantBoundToFace() and a new pickRandom() (state.js) off the same Math.random() source shuffle() uses; no eligible face is a no-op). Mod count 23 -> 25 (24 offerable plus Consecrate: 12 common, 8 uncommon, 4 rare); reward card count 33 -> 36 (18 common, 12 uncommon, 6 rare) — F24/F25 updated in config.js's header comment and tests/facts.test.js's assertions, plus a new explicit tier-total test. Kyrie/Novena/Canticle added to config.cardPool; Concord/Herald added to config.mods (offerable through the existing tier/Load machinery unchanged) and appended to the end of tests/autoplay.js's own LOAD_PRIORITY list (never run this session, per instruction). tests/mods.test.js gained two new tests (Concord; Herald, with Math.random replaced by a fixed sequence to prove its grant lands on a loaded, non-Bound, non-own face and clears at fight end) — 37/37 -> 39/39; tests/facts.test.js gained five new tests (Kyrie x2, Novena, Canticle, the tier-total check) plus updated F24/F25/D-54/collectTriggerCountsByMod/MOD_DESCRIPTION-coverage assertions and both tier/tags maps — 86/86 -> 91/91. See CURRENT SUBSTAGE for the full write-up.
Stage 2.62 (BUILD 135) — win the fight the moment the enemy dies. A card kill already ended the fight without End Turn (BUILD 053/068's own top-of-runPhase() win/loss guard, re-entered on demand) — the gap was BUILD 133's playSweep(): a Nat 20 sweep's or a Bound scan's dispatches are staggered via setTimeout, so a kill from one of those could sit unnoticed until the next real phase transition saw enemy.hp <= 0 cold. New shared checkWinNow() (phase-machine.js) re-enters that same guard on demand; playSweep() (pipeline.js) now takes an optional onComplete, called once every dispatch it queued has actually run (not merely scheduled) — checkWinNow() is passed as that onComplete from runBoundScan(), onNatTwenty() (cards-mods.js), and Novena's own sweep (cards-mods.js), so a kill partway through a sweep still lets its remaining triggers land (permanent growth from them still counts) before the win registers. cards-mods.js's existing card-kill check (playCard()) now calls the same shared checkWinNow() instead of its own inline copy — no behaviour change there, one fewer duplicate. Enemy HP display (rendering.js's renderStats()) now clamps to 0 (`Math.max(0, gameState.enemy.hp)`) — internal state is still allowed to go negative (overkill damage is applied raw, unclamped, exactly as before), only the on-screen text changed. tests/mods.test.js gained one new test (a Nat 20 kill from the sweep registers run.status === 'win' while turn.phase is still ROLL_PHASE, never having reached CARD_PHASE) — 39/39 -> 40/40; tests/facts.test.js gained two new tests (a card kill likewise wins with turn.phase still CARD_PHASE, no nextPhase() call made; the enemy HP display reads "0 / maxHp" off a forced -37 internal hp) — 91/91 -> 93/93. See CURRENT SUBSTAGE for the full write-up, the pre-change findings, and the screenshot diff.
Stage 2.63 (BUILD 136) — on-screen text for every checkpoint 3 piece, plus New Run working while a fight-won panel is open. Twenty-one new cards (Tenet through Canticle) had never been added to CARD_EFFECT_TEXT (rendering.js) — they rendered with no hand text and no reward hover text since the build that added each one — now all twenty-one are entered, in the project's existing style, exact numbers taken from each card's own effect()/log() line. Largesse's MOD_DESCRIPTION read "+2 soul this turn, 4 block" though its effect (updatePlayer(), no turn-scoped clear) is a permanent gain — a real meaning mismatch, fixed to "+2 soul, 4 block"; the other eleven checkpoint-3 mods already matched their own effects in meaning and were left as worded. Every mod carrying the 'growth' tag (zeal, ordain, elevation, congregation, cope) now appends ". Growth" to its MOD_DESCRIPTION, the same suffix convention Bound already used, so a player can see what Lauds'/Congregation's own "loaded Growth mod" count actually counts. New Run (#startGameBtn) used to be flatly disabled whenever dieActionStep/cardRewardStep/riteStep was non-null (refreshInspector(), rendering.js) — a line traced back through BUILD 068's own paste-back to before BUILD 050 (the very first card reward screen), so older than BUILD 129 by roughly eighty builds. That disable is now removed outright, and the click handler (bootstrap.js) resets all three flow variables first, the same dismiss-the-panel fix BUILD 078 already gave devJumpToSlot() — so New Run now both works and actually clears whatever panel was open, rather than becoming clickable but leaving a stale panel rendered. tests/facts.test.js gained six new tests (every reward-pool card has a CARD_EFFECT_TEXT entry, every offerable mod has a MOD_DESCRIPTION entry, a new card renders its hand text, a new card's reward hover is non-empty, New Run works and dismisses an open die-action panel, New Run works and dismisses an open card-reward panel) — 93/93 -> 99/99; tests/mods.test.js unchanged (40/40). See HISTORY.md for the full pre-change findings and the screenshot diff.
Stage 2.64 (BUILD 137) — playtest readiness: weaker enemy poison, a Bound badge, readable two-mod names, a quieter log. Enemy poison buff (ENEMY_BUFF_POISON_STACKS, config.js) lowered from 5 to 3 — act scaling (buildAct(), unchanged) now yields 3/4/5 across acts 1/2/3, was 5/6/8; ENEMY_NAT_ONE_SELF_POISON (a separate knob) untouched. Every Bound face — printed (a mod's own 'bound' tag) or granted for the fight (grantBoundToFace()) — now shows a small "Bound" badge on its die row (renderDieList(), `.die-bound-badge`, same box/font as the existing `.die-weight` ×N badge, D-28's no-new-palette rule), appearing/disappearing exactly when isBoundFace() would flip. A two-mod face's row now shows each name's first 6 letters (TWO_MOD_NAME_CHARS, rendering.js) with no ellipsis, full name via a native title tooltip — replaces BUILD 117's fixed 44px CSS ellipsis, which could cut a name to 2-3 letters plus "…" ("Her…"/"San…"); 6 was measured (Playwright canvas measureText, worst-case pairing) to fit the narrowest die list (#dieActionDieList) at the game's default 1600×1080 window with room to spare, so the 5-letter fallback was not needed. The round-trigger-cap log line (triggerFaceOutsideRoll(), pipeline.js) now prints at most once per round (gameState.turn.roundTriggerCapLogged, cleared at START_OF_TURN) instead of once per refusal. tests/facts.test.js gained four new tests (a printed-Bound face shows the badge; a granted face shows it and loses it at fight end via resetFight(), while the mod itself survives; a two-mod face's row shows both truncated names with no ellipsis and full names via title; the cap line appears once in a round that hits the cap eighteen times) plus F19/F22/F31 rewritten in place for the new poison numbers — 99/99 -> 103/103; tests/mods.test.js unchanged (40/40). See HISTORY.md for the full write-up and the screenshot diff.
Stage 2.65 (BUILD 138) — die feedback: every face that triggers is now SEEN to trigger, and faces 1/20 count their own rolls. A face that fires without being the rolled face (a Nat 20 sweep, a Bound scan, or an outside-roll trigger — Threnody/Reverberation/Magnificat/Novena) now hops to the exact rolled-face look (indent + yellow, the same `.die-row-rolled`/`-flash` classes the BUILD 090 highlight already uses — no new colour, no new indent) at the moment it fires, in firing order, paced by the existing sweep delay, and stays in that look for the rest of the round exactly as a rolled face does — see the new THE HOP standing section. Faces 1 and 20 each gained a run-scoped roll count, stored in modData.triggerCount, the same field/render path every other face's own #N badge already uses (bumpNatFaceTriggerCount(), pipeline.js), so the badge and the map's die preview show it with no new display code. tests/facts.test.js gained four new tests (a Bound scan's other fired face carries the rolled-face look until the next round starts; hopped faces are recorded in firing order; a Nat 20/Nat 1 roll each raise their own face's count by one; both counts reset on a new run) — 103/103 -> 106/106 (102/106 then 105/106 shown mid-session before a test bug on the second forced Nat 1 roll was fixed, see CURRENT SUBSTAGE); tests/mods.test.js unchanged (40/40). See CURRENT SUBSTAGE for the full write-up and the screenshot diff.

---

# CURRENT SUBSTAGE

Stage 2.65 (BUILD 138) — die feedback: the hop (every non-rolled face that fires gets the rolled-face look too) and faces 1/20's own run-scoped roll counts.

BACKUP: a full project copy at C:/Users/figja/Die_v1_backup_2026-09-19_feedback — a byte-for-byte copy of the prior session's own C:/Users/figja/Die_v1_backup_2026-09-19_playtestready (diffed to confirm: only the six files this session went on to edit differed from it, nothing else had drifted), copied under this session's own name in place of a fresh pre-edit copy. PROCESS NOTE, reported per this session's own "stop and report on any other failure" instruction rather than silently corrected: this session began investigating and editing code before taking its own backup, out of the prescribed step order. Recovery: since the project has no git history, the prior build's own pristine backup was confirmed (via `diff -rq`) to be byte-identical to this session's pre-edit tree everywhere except the six files this session intentionally changed, so copying it forward under this session's name satisfies "a full project copy taken before any edit this session" honestly rather than by assertion.

INVESTIGATION (before any edit, per the session's own instruction — findings below, code unchanged at this point):
- The rolled face's yellow, indented look: renderDieList() (rendering.js) adds `.die-row-rolled-flash` (animated, on the render right after a genuinely new roll) or the plain `.die-row-rolled` (same look, no animation, on every later re-render of that same roll) to the row matching gameState.turn.rolledFaceNumber, whenever trackedRollOutcome isn't 'blank' (a blank gets its own separate dim pulse, no indent). Both classes (index.html) share `transform: translateX(6px)` (the indent) and a rgba(251,191,36,...) (amber/yellow) background. Cleared by rolledFaceNumber/rollOutcome being reset to null — one single site, phase-machine.js's START_OF_TURN handler (and mirrored at run-and-map.js's own fight-reset updateTurn() call) — so the look survives every re-render within a round and disappears the instant the next round's START_OF_TURN runs.
- playSweep(faceNumbers, dispatchFn, onComplete) (pipeline.js) paces a multi-face sweep (Nat 20's onNatTwenty() loop, cards-mods.js; the Bound scan, runBoundScan(), pipeline.js) by scheduling each face's dispatchFn via a cumulative setTimeout: the first three plays this round (gameState.turn.roundSweepPlays, counted across both kinds of sweep, round-scoped) land GAME_CONFIG.SWEEP_TRIGGER_DELAY_MS (200ms) apart, every play after the third at a quarter of that (50ms). State updates the instant each dispatch fires; only WHEN each one plays is staggered, never the order (faceNumbers is walked in the order given) or what it does. onComplete (checkWinNow) fires once every scheduled dispatch has actually run.
- A single outside-roll trigger (Threnody, Magnificat) goes through triggerFaceOutsideRoll(faceNumber) (pipeline.js) directly, not through playSweep() — it dispatches immediately (refusal checks, then MOD_TRIGGER/BLANK_ROLL synchronously), since there's only ever one face to pace against. The Bound scan is the one caller that hands triggerFaceOutsideRoll() to playSweep() as its per-face dispatchFn, so a Bound scan's own faces ARE paced by the sweep delay even though triggerFaceOutsideRoll() itself has no timing of its own.
- Neither face 1 nor face 20 kept any run count of its own rolls before this build. Every other loaded face's own trigger count lives in that face's modData.triggerCount/triggerCount2 (BUILD 108, folded from the old fight-scoped gameState.die.triggerCounts array — run-scoped, survives a fight reset, wiped only by a brand new run's buildFreshPlayerDieFaces()); faces 1/20 (modId 'NAT_ONE'/'NAT_TWENTY') never dispatch MOD_TRIGGER (they dispatch the NAT_ONE/NAT_TWENTY hooks, or BLANK_ROLL once a spent face 1 falls back to a plain blank), so mod_dispatch's own increment (cards-mods.js) never runs for them — nothing recorded a count.

THE HOP: gameState.turn.hoppedFaces (state.js, new) records every face number that fires without being the rolled face, in firing order, marked by markFaceHopped() (pipeline.js) at the exact moment each one's dispatch runs. Two call sites: triggerFaceOutsideRoll() (covers every outside-roll trigger AND the Bound scan, since runBoundScan() dispatches through it) and onNatTwenty()'s own playSweep() dispatch callback (cards-mods.js) — the one sweep path that calls callListeners() directly rather than going through triggerFaceOutsideRoll(). renderDieList() (rendering.js) applies the identical rolled-face look (die-row-rolled/-flash) to any row whose face.number is in hoppedFaces, player-die containers only, skipping the row that's already the tracked rolled face (already covered by the existing highlight, so a re-trigger of the rolled face itself, e.g. Reverberation, adds nothing extra). Same flash-once-then-sustained split as the rolled face's own highlight, via a new per-container "already seen hopped" record (lastSeenHoppedFacesByContainer, module scope, same idiom as lastSeenRollSignatureByContainer). Cleared at the same single site rolledFaceNumber itself clears at — START_OF_TURN (phase-machine.js) — so it survives every re-render within the round and disappears the instant the next round starts. New standing section, THE HOP, added; OUTSIDE-ROLL TRIGGER and MULTI-MOD FACES/TRIGGER COUNTS' Trigger-count display note both updated per the OWNERSHIP RULE.

FACE 1/20 ROLL COUNTS: bumpNatFaceTriggerCount(faceNumber) (pipeline.js, new) merges into that face's own modData.triggerCount exactly the way strengthenFace()/mod_dispatch already merge into a face's modData — called from resolvePlayerRoll()'s two Nat branches (pipeline.js) on every roll of that face, including a spent face 1's later blank-equivalent rolls, so the count reads "how many times this face has been rolled this run," not just "how many times Penitence/Nat 20 actually fired." No renderDieList() change was needed: `face.modId !== null` was already true for both Nat stub ids, so the existing single-mod `#N` badge branch (and the map's own mapPlayerDieList preview, which renders the same gameState.die.faces reference) picked the new count up for free, in the same colour the existing nat-one/nat-twenty CSS selectors already apply to `.die-trigger-count`.

TESTS: tests/facts.test.js gained four new tests — after a Bound scan, the other fired face carries the rolled-face look until the next round starts (and hoppedFaces itself is asserted, not just the CSS classes); hopped faces are recorded in the same ascending order a Nat 20 sweep fires them in; one Nat 20 roll raises face 20's own count by exactly one and one Nat 1 roll raises face 1's, both shown via the real `#N` badge; both counts are wiped by startNewRun(), same as every other face's modData — 103/103 -> 106/106. One test bug found and fixed mid-session, not a game defect: the Nat 1 half of the count test forced a second round's face-1 roll without the explicit nextPhase() call the pre-existing F10 Penitence test's own second-round step already documents as necessary ("no leftover autoAdvance() timer is pending" once a fight's first CARD_PHASE has been reached) — the test timed out waiting for CARD_PHASE; adding the same explicit nextPhase() call fixed it. tests/mods.test.js unchanged (40/40) — no mod effect was touched, only die-row rendering, one pipeline dispatch site, and one cards-mods.js sweep callback.

VERIFIED: tests/mods.test.js 40/40 before this session's edits, 40/40 after. tests/facts.test.js 103/103 before, 106/106 after (105/106 shown momentarily mid-session against the not-yet-fixed test, see TESTS above; KI-18's own build-stamp test also fails until GAME_CONFIG.BUILD/CLAUDE.md are back in sync at the very end of the session, expected and not a regression).

SCREENSHOTS (KI-6): see this session's paste-back for the `node tests/screenshots.js --compare` diff, run after this write-up.

NOT DONE THIS BUILD: CARDS/MODS (the two oldest standing sections listing every card/mod by name) were not rewritten — unchanged since around BUILD 102, and this session touched no card/mod roster (only die-row rendering, one dispatch site, and one sweep callback). The Notion "Die — V1" page was not reached this session — no FACTS number changed (faces 1/20's roll counts are a display mechanic, not a documented F-number), so nothing needed syncing there.

FILES TOUCHED: js/state.js (new gameState.turn.hoppedFaces field), js/phase-machine.js (hoppedFaces cleared at START_OF_TURN alongside every other round-scoped flag), js/pipeline.js (bumpNatFaceTriggerCount() and markFaceHopped() added; resolvePlayerRoll()'s two Nat branches call bumpNatFaceTriggerCount(); triggerFaceOutsideRoll() calls markFaceHopped()), js/cards-mods.js (onNatTwenty()'s playSweep() dispatch callback calls markFaceHopped()), js/rendering.js (lastSeenHoppedFacesByContainer module-scope record; renderDieList() applies the rolled-face look to a hopped, non-rolled row), js/config.js (BUILD: 138), index.html (build stamp, STAGE 2.65 / BUILD 138), tests/facts.test.js (four new tests), CLAUDE.md (THE HOP new standing section; MULTI-MOD FACES/TRIGGER COUNTS' Trigger-count display note updated; this write-up, the new CONFIRMED WORKING one-liner), HISTORY.md (BUILD 137's migrated CURRENT SUBSTAGE text).

MIGRATED FIRST: BUILD 136's own CURRENT SUBSTAGE (the on-screen-text write-up) moved to HISTORY.md verbatim before this section was overwritten. A same-session correction: this work was first recorded as a same-build revision of BUILD 136 and reported that way in this session's first paste-back; per a follow-up instruction (CLAUDE.md marks build numbers to code changes, with no standing exception for small items), it was split out as its own build/stage instead — BUILD 136's CONFIRMED WORKING entry restored to its original on-screen-text-only text, this session's four changes given their own Stage 2.64/BUILD 137 entry, GAME_CONFIG.BUILD bumped 136 -> 137, index.html's stage text corrected to 2.64, and every in-code "BUILD 136" comment this session had added renamed to "BUILD 137" (the pre-existing, legitimate BUILD 136 comments from the on-screen-text build were left alone).

BACKUP: a full project copy at C:/Users/figja/Die_v1_backup_2026-09-19_playtestready (CLAUDE.md names no narrower whole-project backup convention, same whole-project-copy fallback prior builds have used) — taken before any edit this session.

INVESTIGATION (before any edit, per the session's own instruction):
- Enemy poison-stack count: GAME_CONFIG.ENEMY_BUFF_POISON_STACKS (config.js), scaled per act by ACT_INTENT_MULTIPLIER and Math.ceil'd inside buildAct() (run-and-map.js), stored on the built slot's own enemy.buffPoisonStacks and copied onto gameState.enemy.buffPoisonStacks by beginFightFromSlot(); applied to the player by enemy_buff_dispatch (cards-mods.js). Recorded by fact F19 (config.js header comment, tests/facts.test.js).
- grantBoundToFace(faceNumber) (pipeline.js) is the one fight-scoped Bound setter — merges `{ modData: { boundGranted: true } }` onto the target face (refusing face 1/20 and a blank face outright); isBoundFace(face) (pipeline.js) is the one shared read, true either for that modData.boundGranted or for a mod carrying the printed 'bound' tag on either slot. clearFightScopedState() (run-and-map.js) strips boundGranted from any face at fight end, leaving the rest of that face's modData untouched.
- Die row rendering is renderDieList() (rendering.js): the weight badge is `.die-weight` (×N, shown above weight 1), the (single-mod) trigger badge is `.die-trigger-count` (#N, shown above 0 triggers) — both byte-identical box/font per D-28's no-new-palette rule, both appended to the row's `.die-mod-wrap`. A two-mod face renders a `.die-mod-pair` (added BUILD 117, replacing an earlier vertical stack) holding two `.die-mod` spans with their own inline `#N` counts; each span's text was the mod's FULL name (modDisplayName()) but CSS (`.die-mod-pair .die-mod`, index.html) clipped it to a fixed 44px box with `text-overflow: ellipsis` — that fixed width was measured against BUILD 117's own worst-case pairing and is what produced "Her…"/"San…": the ellipsis itself was eating most of the 44px budget, leaving as few as 2-3 real letters.
- The "outside-roll trigger refused: round trigger cap (10) reached" line is logged once per refusal inside triggerFaceOutsideRoll() (pipeline.js), the shared function Threnody/Reverberation/Magnificat/Novena/the Bound scan all funnel through — nothing previously deduplicated it, so a round that refused the cap many times printed the identical line that many times.

POISON: GAME_CONFIG.ENEMY_BUFF_POISON_STACKS (config.js) lowered from 5 to 3; act scaling (buildAct(), unchanged) now yields 3/4/5 across acts 1/2/3 (was 5/6/8). ENEMY_NAT_ONE_SELF_POISON (the boss's own flat Nat 1 self-poison, a separate knob) is untouched, per instruction. config.js's F19 header comment and tests/facts.test.js's F19/F22/F31 assertions updated to the new numbers; F31 now also asserts all three acts' new values in one place.

BOUND BADGE: renderDieList() (rendering.js) now appends a `.die-weight.die-bound-badge` span reading "Bound" to any row where isBoundFace(face) is true — same box/font as the weight badge, shown on every die-list container (not gated by the trigger badge's player-die-only check), so a printed-Bound mod's badge shows wherever its name shows, and a granted badge shows on the live player die the instant grantBoundToFace() lands and disappears the instant clearFightScopedState() strips it (both via the existing updateDie() -> refreshInspector() re-render path — no new render call needed).

TWO-MOD NAMES: a new TWO_MOD_NAME_CHARS constant (rendering.js, 6) replaces the CSS ellipsis truncation — each name in a `.die-mod-pair` now shows modDisplayName(id).slice(0, TWO_MOD_NAME_CHARS) with no ellipsis, and the full name via a native `title` tooltip on the span itself. CSS's `.die-mod-pair .die-mod` max-width widened 44px -> 56px (just over the worst 6-letter width measured) and its `text-overflow: ellipsis` removed (overflow:hidden kept only as a safety net). 6 was chosen after measuring (Playwright, canvas measureText against the row's real computed font) the worst realistic pairing (Congregation + Sanctuary/Magnificat, the pool's two longest names) inside both the roomy main die column and the much narrower #dieActionDieList at the game's default 1600×1080 window — both fit with margin to spare even with both inline trigger badges shown; the 5-letter fallback the prompt allowed for was not needed.

LOG: triggerFaceOutsideRoll() (pipeline.js) now gates the cap-reached log line on a new gameState.turn.roundTriggerCapLogged flag (state.js) — set true the first time it logs, checked before every subsequent log call, cleared at START_OF_TURN (phase-machine.js) alongside every other round-scoped flag. The refusal itself (returning false, not incrementing the count) is unaffected; only the repeated log line is suppressed.

TESTS: tests/facts.test.js gained four new tests — a printed-Bound face (Unison) shows the badge; a granted face shows it and loses it at fight end (via resetFight(), the same real code path the existing mods.test.js grant-clears test uses) while the mod itself survives; a two-mod face's row shows both names' exact truncated prefixes with no ellipsis character and the full names via title; the cap-reached log line appears exactly once in a round that refuses the cap eighteen times — plus F19/F22/F31 rewritten in place for the new poison numbers (no new poison test beyond that, since F31 already covers all three acts) — 99/99 -> 103/103. tests/mods.test.js unchanged (40/40) — no mod effect was touched, only the enemy buff's stack count (asserted via facts.test.js, which already covered it) and pure UI/log changes.

VERIFIED: tests/mods.test.js 40/40 before this session's edits, 40/40 after. tests/facts.test.js 99/99 before, 103/103 after.

SCREENSHOTS (KI-6): see this session's paste-back for the `node tests/screenshots.js --compare` diff, run after this write-up.

NOT DONE THIS BUILD: CARDS/MODS (the two oldest standing sections listing every card/mod by name) were not rewritten — unchanged since around BUILD 102, and this session touched no card/mod roster (only one mod's numeric constant, already covered by GAME_CONFIG/F19, and pure UI/log code). The Notion "Die — V1" page was not reached this session (GAME_CONFIG.ENEMY_BUFF_POISON_STACKS changed, but the FACTS text itself — "enemy buff applies N stacks of poison" — was already generic per-act prose before this build, needing no wording change; only config.js's header comment, which already lives outside Notion's own copy, was updated with the new numbers).

FILES TOUCHED: js/config.js (ENEMY_BUFF_POISON_STACKS 5 -> 3, F19 header comment reworded for the per-act numbers), js/rendering.js (Bound badge in renderDieList(); TWO_MOD_NAME_CHARS constant and JS-truncated two-mod names with title tooltips), js/pipeline.js (round-trigger-cap log line gated on roundTriggerCapLogged), js/state.js (new gameState.turn.roundTriggerCapLogged field), js/phase-machine.js (roundTriggerCapLogged cleared at START_OF_TURN), index.html (`.die-mod-pair .die-mod` max-width 44px -> 56px, ellipsis removed; no build-stamp number change needed, GAME_CONFIG.BUILD was already 136), tests/facts.test.js (F19/F22/F31 updated; four new tests), CLAUDE.md (ENEMY DIE PER TYPE, ACTS, MULTI-MOD FACES, OUTSIDE-ROLL TRIGGER and BOUND ENGINE standing sections updated per the OWNERSHIP RULE; this write-up), HISTORY.md (this build's prior on-screen-text CURRENT SUBSTAGE migrated verbatim).

Stage 2.36 — tests/autoplay.js, a headless autoplayer/measurement tool (BUILD 105) — landed. Never ships, same status as tests/facts.test.js/tests/mods.test.js. LAW 3 / KI-4 / KI-14: every action calls the exact function the corresponding button calls (#startGameBtn click, chooseLane(), enterSlot(), nextPhase(), playCard(), dieActionChooseLoad()/dieActionChooseStrengthen()/dieActionChooseSkip()/dieActionPickMod()/dieActionPickLoadFace()/dieActionPickStrengthenFace(), riteChooseHeal()/riteChooseDieAction(), cardRewardPickCard()/cardRewardSkip(), healPlayer(5) for riteHeal=25) — never forcePlayerRoll()/forceEnemyRoll()/devJumpToSlot()/devLoadMod(). SEEDING: page.addInitScript() installs a seeded mulberry32 PRNG as Math.random before any js/ file executes; grep-confirmed the game's only three real Math.random call sites — js/state.js:231 (shuffle()), js/pipeline.js:122 (rollDie()), js/phase-machine.js:106 (enemy intent) — all funnel through it. RACE FOUND AND FIXED DURING VERIFICATION: the first version drove phases with nextPhase() alone and intermittently threw "did not reach CARD_PHASE within 10 phase steps" on some seeds, not others, and not reproducibly on the same seed — traced to the real autoAdvance() timer every fight's own startFreshTurnPaused() arms once (ROLL_PHASE_PAUSE_MS, 1800ms, a UX pause) at fight start: this script's own driving is fast enough that a whole multi-round fight can finish inside that 1800ms window, so the stale timer could fire while the script was sitting in a LATER round's unresolved ROLL_PHASE and resolve that roll itself, racing the script's own next nextPhase() call. Fixed by checking the real devPauseBeforeRollCheckbox (dev-tools.js's devPauseBeforeFirstRoll, CLAUDE.md's DEV MODE) once per page before any fight begins — every fight now starts paused at START_OF_TURN instead, so autoAdvance() and its timer are never armed at all; every phase step, every round, every fight, is driven by the script's own nextPhase() calls with nothing else able to touch the phase machine concurrently. Confirmed fixed: the same 10-run batch (seed 1, asBuilt, riteHeal 20) produced the byte-identical summary line three consecutive times after the fix, having failed intermittently before it. VERIFIED (Playwright, real Chromium, file://): `node tests/autoplay.js` — seed 42/asBuilt/riteHeal 20 run twice produced byte-identical CSV lines (CONFIRMED IDENTICAL); 100 runs each of the four {dieRewardRule, riteHeal} combinations (seeds 1-100) all completed with zero console/page errors and zero illegal-play log lines across all 402 runs (the two reproducibility runs plus 400 batch runs) — see CSV header and all four summaries in this build's paste-back. Per the prompt, these summaries are not interpreted here (win rate/arrival HP/faces-at-boss/final-round are read off, not judged) — the tool is a reference player, not a floor or ceiling for a human. tests/autoplay_results.csv (new, git-trackable CSV, 403 lines including header) and tests/autoplay.js (new) are the only files this build added; no js/ file, index.html, or GAME_CONFIG value was touched — this build's own OWNERSHIP RULE scope is the tests/ list line above (PROJECT section) and this CONFIRMED WORKING/CURRENT SUBSTAGE pair, nothing else.

Stage 2.35 — one consistent font family and weight across every player-facing text element (BUILD 104) — landed. TARGET: `.panel-title` (the "PLAYER DIE"/"PLAYER"/"ENEMY" headers) — `font-family: var(--game-font); font-weight: bold;`. AUDIT (before any edit): grepped every CSS rule touching player-facing text and compared its computed font-family/font-weight against the target. Mismatched (all previously had no font-family, so fell through to `body`'s `'Consolas', 'Courier New', monospace`, and no font-weight, so rendered at normal 400): `.face-btn` (die face numbers 1-20 plus the NAT 1/NAT 20 buttons) — was `font-family: inherit` (Consolas via body), no weight; `.die-mod` (mod names, blanks '—', NAT 1/NAT 20 labels) — no font-family, no weight; `.die-weight` (the ×N weight badge) — no font-family, no weight; `.die-trigger-count` (the ↻N trigger badge, BUILD 103) — no font-family, no weight; `.stat-label` (Active debuffs / Deck / Discard / Poison Stacks / Loaded buffs / Active this turn) — no font-family, no weight; `.stat-value` (the values beside each of those labels) — no font-family, already `font-weight: bold` (weight was already correct, family was not). Already-correct and left untouched: `.panel-title` itself, `.stat-hero-value`/`.stat-hero-sublabel` (HP/Block/Soul/Intent), `.round-chip`, `.roll-hero-number`/`.roll-hero-label`, `.phase-badge`, `.result-banner`, `.die-action-title`/`.die-action-row button`, `.hand-card-name`/`.hand-card-effect`/`.hand-card-cost` (inherit `var(--game-font)` from `.hand-card-el`), `.map-node` and family — none of these were in the audited scope (die face labels, Active debuffs/Deck/Discard, and other enemy/player stat-row labels/values) and none were touched. FIX: added `font-family: var(--game-font); font-weight: bold;` to each of the six mismatched selectors above — no other property changed. Log panel and dev drawer untouched, per scope. VERIFIED (Playwright, real Chromium, file://): `node tests/mods.test.js` 11/11 and `node tests/facts.test.js` 23/23 both still pass unchanged, confirming zero gameplay/numeric effect. A standalone script (tests/build104_verify.js) set a face to the longest mod name (Consecrate) with weight 3 and a trigger count of 2 (via `updateDie()`, per Law 3) so `.die-mod`/`.die-weight`/`.die-trigger-count` all rendered their worst-case text simultaneously, then checked `scrollWidth` vs `clientWidth` on every audited selector — zero overflow anywhere (no text clipped), and read back `getComputedStyle` for all seven selectors (including the `.panel-title` target) — all seven now report the identical `font-family: Bahnschrift, "Arial Narrow", "Segoe UI Semibold", "Franklin Gothic Medium", Arial, sans-serif` and `font-weight: 700`. D-28 POSITION CHECK: the same script measured all 20 player-die rows via `getBoundingClientRect()` — all 20 heights identical (19px) and all 20 face-btn left offsets identical (23px), matching BUILD 103's own measured baseline exactly, confirming the font change shifted no row's position or size. Screenshots of the full play column (`tests/build104_before.png`, `tests/build104_after.png`) taken via a fresh New Run into the opening fight confirm every number, name, weight badge and stat value still reads correctly and nothing disappeared. Build stamp updated to read STAGE 2.35 / BUILD 104 (index.html, one line).

Stage 2.34 — a trigger-count indicator next to each loaded face on the player die (BUILD 103, Fergus's idea this session, closes KI-11) — landed. Pure visibility, no balance/numbers/mechanic change, per the prompt's own framing. AUDIT FIRST: checked whether a generic per-face trigger counter already existed before adding one — it did not. Zeal's face.modData.accumulatedBonus (cards-mods.js) counts triggers only indirectly: it grows by 4 per trigger (not 1), is mod-specific (only Zeal writes it), and persists across fights (resetFight() never touches gameState.die) — none of which match "how many times has this face triggered this fight," so it was read, not duplicated, and left untouched. No other counter existed. IMPLEMENTATION: added gameState.die.triggerCounts (state.js), a plain array, index i holding face i+1's count — fight-scoped (unlike faces/weights/mods, which persist), reset to all-zero in clearFightScopedState() and startNewRun() (run-and-map.js) alongside the die's other fight-scoped state. Bumped exactly once per real trigger inside cards-mods.js's mod_dispatch listener (registered on MOD_TRIGGER) — the same single funnel modTriggeredThisTurn and the mod_trigger sound already use, so a Nat 20 counts each qualifying face once per pass, identically to a normal single-face roll; never mutated directly, always through updateDie() per Law 3. RENDERING: renderDieList() (rendering.js) now computes showTriggerBadges = (faces === gameState.die.faces) once per render call — true only when the array actually being rendered is the player's own live die (playerDieList, dieActionDieList's post-win picker, the map's mapPlayerDieList preview), false for the enemy die and the map's static elite/boss previews, which is what "player die only, not the enemy die" reduces to without a containerId allowlist. For each loaded face (modId !== null) with a non-zero count, appends a `↻N` span, class `die-trigger-count`, into the row's existing .die-mod-wrap right after the weight badge; zero triggers appends nothing at all, so an untouched face's DOM is byte-identical to before this build. STYLE: `.die-trigger-count` (index.html) is a byte-identical copy of `.die-weight`'s own rule (font-size 14px, color var(--blank), flex-shrink 0), and was added to the same three colour-inheritance compound selectors (.nat-one/.nat-twenty, .player-die .loaded, .enemy-die .loaded) that `.die-weight` already sits in — same box, same font, same size, no new palette, per D-28. VERIFIED (Playwright, real Chromium): dev-loaded Zeal onto face 5 of a fresh opening fight, force-rolled it three times across three separate rounds (round 2/3 each requiring an explicit nextPhase() call after forcePlayerRoll(), since only round 1 still has a live autoAdvance() timer pending) — gameState.die.triggerCounts[4] read exactly 3 after the third trigger, and the DOM badge on face 5's row read the string '↻3', both matching the log's own three '[MOD] zeal: ... triggered' lines. Screenshot (tests/build103_trigger_badge.png) shows face 5 ("Zeal") carrying the ↻3 badge while face 10 ("Consecrate", never rolled this fight) shows nothing extra — confirming the zero-case renders no badge. D-28 REVERSE CHECK: a full opening fight was played to a win through the real UI (real map click, natural rolls, real hand-card clicks, real End Turn — zero forced rolls) with zero console errors and zero page errors; before closing, every one of the player die's 20 rows was measured via getBoundingClientRect() — all 20 heights identical (19px) and all 20 face-btn left offsets identical (23px), regardless of which rows carried a weight badge, a trigger badge, both, or neither — confirming the new badge shifted no existing element's position or size. Build stamp updated to read STAGE 2.34 / BUILD 103 (index.html, one line).

Stage 2.33 — one constants file plus the facts assertion test (BUILD 102) — landed. Created js/config.js, loaded first (index.html), before state.js — eleven files total, updated everywhere the file count/order was documented (PROJECT, F26). Every tunable number/structural constant the prompt named was moved onto one object, GAME_CONFIG, with a header comment carrying the FACTS block (F01-F28) verbatim from the Notion "Die — V1" page, one line per fact beside the field(s) implementing it — the one place the F-numbers live in code. Constants moved, with source: PLAYER_MAX_HP/PLAYER_MAX_SOUL (state.js's gameState.player.hp/maxHp/soul/maxSoul literals, F01/F02); DRAW_COUNT (phase-machine.js's drawCards(5), F03); STARTING_DECK (cards-mods.js's config.classes.ordained.startingDeck array, F04); BLANK_ROLL_BLOCK (cards-mods.js's onBlankRoll dealBlock(2,...), F05); PENITENCE_TURNS (state.js's standalone const, F10); RITE_HEAL (rendering.js's riteChooseHeal() healPlayer(20) and the "Heal 20 HP" button text, F15); DIE_REWARDS.ELITE/SINGLE (phase-machine.js's win-branch ternary and rendering.js's/bootstrap.js's dieActionsRemaining defaults, F14); LANE_COUNT/SLOTS_PER_LANE/ELITE_SLOT_INDEX/ELITE_ALLOWED_SLOT_INDICES (documented against run-and-map.js's buildAct() shape, F16); NORMAL_FIGHT_HP/HP/INTENT (run-and-map.js's buildAct() per-slot hp/intentMin/intentMax literals, F17/F18); ENEMY_BUFF_POISON_STACKS/ENEMY_NAT_ONE_SELF_POISON (cards-mods.js's two separate poisonStacks+5 sites, F19/F23 — kept as two named constants, not collapsed, since they're independent knobs that only coincide in value); ELITE_DIE/BOSS_DIE (run-and-map.js's buildEnemyDieFaces() call-site array arguments, F20/F21); CHAIN_STEP_CAP (audio.js's standalone const, F27); SOUND_DURATION_CEILING_MS/SOUND_DURATION_CEILING_EXEMPT (new — F28 was previously prose only, no literal ceiling existed to move). AUDIT FINDING (not a duplicate-constant case, but the same "stop and report" spirit): state.js's placeholder gameState.enemy defaults (hp/intentMin/intentMax) were a second hardcoded copy of the normal-fight facts, provably dead — fully overwritten by beginFightFromSlot() before any real fight begins, never rendered while screen is 'map' — buildAct()'s copy is the live one; pointed the placeholder at the same GAME_CONFIG fields instead of leaving it as stray literals. SECOND FINDING: instrumented every SOUND_TABLE entry's real total duration live (window.playTone/setTimeout monkey-patched to record delay+duration, not read from the comment) and found F28 as originally worded ("Nat 20 and Nat 1 may exceed 200ms, all others under") was itself wrong — fight_won (210ms), fight_lost (260ms), fight_start_boss (320ms) and boss_defeated (400ms) already exceeded 200ms too, unrelated to this build's changes (no sound was touched, per "no numbers change"). F28's wording was corrected to name the true six-sound exemption set, on both config.js and the Notion FACTS block. tests/facts.test.js (new, Playwright, same shape as tests/mods.test.js) asserts all 28 facts against GAME_CONFIG and live gameState/DOM after a fresh New Run — draw count and blank-roll block via the real opening fight; Strike/Ward/Rite cost+effect via direct config.cards[...].effect() calls; poison decay via a forced START_OF_TURN tick; Penitence onset+once-per-fight+tick-down via two rounds of forced face-1 rolls; player Nat 20 repeatability via two rounds of forced face-20 rolls with a dev-loaded mod; Strengthen/Load offer eligibility via the real die action panel DOM; the enemy poison buff, enemy Nat 20 repeatability, and enemy Nat 1 once-per-fight+cancel+self-poison via dev-jumping to the boss and forcing faces 5/20/1 across two rounds; mod/card pool counts, lane/slot shape, elite/boss die layouts, the pitch-chain cap+reset, and the sound-duration ceiling all read directly off GAME_CONFIG and the built act. Verified: `node tests/facts.test.js` reports 23/23 passed (28 facts, several combined per test where they share one setup); `node tests/mods.test.js` still 11/11; a full opening fight played to a win through the real UI (real map click, natural auto-advanced rolls — zero forced rolls, real hand-card clicks, real End Turn button) ended with the real "Fight won — choose a die action" panel, zero console errors, zero page errors; grepped every moved literal (hp/soul/drawCards/dealBlock/PENITENCE_TURNS/healPlayer/dieActionsRemaining/poisonStacks+5/CHAIN_STEP_CAP/buildEnemyDieFaces args/intentMin/intentMax/hp per slot) across every non-config game file and found zero remaining occurrences outside a single harmless historical comment (phase-machine.js:151, describing BUILD 063's old shape, not live code). Build stamp updated to read STAGE 2.33 / BUILD 102 (index.html, one line).

Stage 2.32 — make the enemy roll visible and prove it resolves (BUILD 101) — landed. VERIFICATION FIRST, per the prompt: phase-machine.js's ENEMY_ROLL_PHASE branch (runPhase()) itself only resets the enemyRollResolved flag; the actual roll happens in nextPhase()'s transition out of that phase, which — when gameState.enemy.hasDie — calls rollDie(gameState.enemy.die.faces) to select a face, then resolveEnemyRoll(face) (pipeline.js), which reads face.modId and dispatches: ENEMY_NAT_ONE/ENEMY_NAT_TWENTY on those two modIds (boss only), ENEMY_BUFF_TRIGGER with { buffId, faceNumber } for any other loaded face, or logs '[ENEMY ROLL] blank' for modId null. All three permanent listeners this can reach (boss_nat_one_passive, boss_nat_twenty_passive, enemy_buff_dispatch — cards-mods.js) apply real state changes (poison stacks via updateEnemy()/updatePlayer(), not just logging). Conclusion: this is NOT a no-op and the face IS triggered — selects, reads, and dispatches, exactly mirroring resolvePlayerRoll()'s own shape. The one confirmed gap was visual: renderDieList() (rendering.js) computed its rolled-face highlight (BUILD 090) only for 'playerDieList'/'dieActionDieList', with an explicit comment "Never the enemy's die" — the enemy roll resolved and logged correctly but the roll strip never showed which enemy face had just landed. Two changes closed it. (1) Logging: resolveEnemyRoll() already logged '[ENEMY ROLL] face: N modId: X' (BUILD 097) in the same shape as the player's '[ROLL] face: N modId: X', and blank already logged a distinct '[ENEMY ROLL] blank' line — confirmed still correct, no change needed there. (2) Highlight: added gameState.turn.enemyRollOutcome/enemyRolledFaceNumber (state.js), set in resolveEnemyRoll() by the same outcome derivation resolvePlayerRoll() already uses (blank/mod/nat_twenty/nat_one), cleared alongside the player's own roll fields at START_OF_TURN (phase-machine.js) and on fight reset (run-and-map.js's resetFight()); renderDieList() now also tracks 'enemyDieList', reading these enemy-side fields instead of the player's when rendering that container, reusing the exact same die-row-rolled/-flash/-blank-flash CSS classes — no new palette, no new element type. Renders from gameState only, per the state schema, same as the player's own highlight. Verified via a new standalone Playwright script (tests/build101_enemy_roll.test.js, same shape as tests/mods.test.js — not part of the nine-file bundle): dev-jumped straight to the boss fight, force-rolled the enemy die's face 5 (a loaded poison face) every round through round 4, and confirmed each round's log shows '[ENEMY ROLL] face: 5 modId: enemy_buff_poison' immediately followed by '[ENEMY] applied 5 poison to player, stacks now N' (5, 9, 13 across three rounds) — 5 stacks per trigger, matching the enemy_buff_poison spec (ENEMY DIE PER TYPE) exactly. A screenshot taken immediately after the force-roll (tests/build101_roll_strip.png) shows the enemy die's face 5 row lit with the held gold background in the roll strip, the phase badge reading ENEMY_ROLL_PHASE, and the player's Active debuffs reading "poison x5" — the same round the log lines land. Zero balance changes, zero new numbers. Build stamp updated to read STAGE 2.32 / BUILD 101 (index.html, one line).

Stage 2.31 — "fix Vigil" plus a standing per-mod regression suite (BUILD 100) — landed, but not the way the prompt assumed. The prompt's premise, inherited from BUILD 099's documentation pass, was that Vigil registers its block-per-card-held listener on 'END_PLAYER_TURN', a phase name rather than a real hook, and so has never fired. That premise was checked first, per this build's own instruction, and found false: phase-machine.js's runPhase(phase) calls callListeners(phase) unconditionally, once per phase visit, near the top of the function — before that phase's own if-branch (including END_PLAYER_TURN's own hand-to-discard logic) runs — so any of the seven PHASE_ORDER strings is already a real, dispatched hook, and Vigil's registration on 'END_PLAYER_TURN' was never dead. Proven live, not just read: a Playwright script drove a fresh opening fight, dev-loaded Vigil onto face 2, force-rolled it, and stepped the phase machine from CARD_PHASE into END_PLAYER_TURN with 5 cards still in hand — player.block went from 0 to exactly 25 (5×5). To rule out a false positive from that harness itself, Vigil's own registerListener() hook string was then deliberately renamed to a decoy ('END_PLAYER_TURN_DECOY'), and the same test correctly failed (block stayed 0, expected 25); reverting the rename restored the pass — confirming the test suite actually detects a dead mod rather than trivially passing. Per the prompt's own explicit conditional ("if no such hook exists, add one... and say so"), no new hook was added and no game-logic file was touched, since the real hook already exists and Vigil already routes through it — adding a second, parallel dispatch would have double-fired it. The actual fix this build made was to the documentation: EVENT HOOKS' claim that "callListeners() is never invoked with a phase name" was corrected (it is, generically, via runPhase()'s own call), the "known dead registration" paragraph naming Vigil was corrected to state it fires and is verified, LISTENER API's matching false parenthetical was corrected, and the MODS section's Vigil entry was updated to drop "known dead" and describe the real, working behaviour. Item 2's audit (grep every registerListener() call site for its hook name, confirm each is dispatched somewhere): all ten registrations checked out — BLANK_ROLL, NAT_TWENTY, NAT_ONE, ENEMY_BUFF_TRIGGER, ENEMY_NAT_TWENTY, ENEMY_NAT_ONE, MOD_TRIGGER, ON_CARD_PLAY, and DAMAGE_MULTIPLIER (a pipeline hook, correctly read directly off the registry rather than via callListeners, per LISTENER API) are all genuinely called; END_PLAYER_TURN (Vigil) is the tenth, and per the finding above it is also genuinely called, via the generic per-phase callListeners(phase). Zero dead registrations found. tests/mods.test.js (new file, added to the standing file list in PROJECT) is a plain Node script — this project has no test runner installed, only the raw playwright library — that for each of the eleven config.mods entries dev-loads it onto face 2 and forces that roll through the real forcePlayerRoll()/resolvePlayerRoll()/MOD_TRIGGER dispatch (never a shortcut calling a mod's effect() directly), then asserts the exact numeric change on gameState the MODS section promises: Smite 16 damage, Penance 8 damage + 8 block, Offering +2 soul/draw 1, Blight +6 poison, Virulence poison 0→3→6, Sanctuary 16 block, Consecrate +2 soul, Zeal 10 damage plus its own face's accumulatedBonus counter going 0→4, Fervour doubling a subsequent attack-tagged dealDamage() call 10→20, Ordain 10 damage plus the triggering face's weight going 1→2, and Vigil ending the turn with 5 cards held for exactly 25 block via the real END_PLAYER_TURN phase transition — not a direct listener/effect() call. Verified: `node tests/mods.test.js` reports 11/11 passed, zero console/page errors captured per mod; separately, a full fight was played through the real UI (real map click into the opening fight, the real dev-load-mod select/input/button, forced rolls, and the real End Turn button — not nextPhase() called directly) with Vigil loaded on face 2 and cards deliberately left unplayed each turn — the log shows both '[MOD] vigil: registered, block scales with cards held at end of turn' and '[MOD] vigil: 5 cards held, granted 5x5 block', the second line carrying the existing 'log-mod' CSS class (the same class every other '[MOD]' line gets — no new category), block correctly clearing back to 0 at the following START_OF_TURN per the standing block-clear rule. Zero console errors, zero page errors, across both the test suite and the real-UI playthrough. Build stamp updated to read STAGE 2.31 / BUILD 100 (index.html, one line).

Stage 2.29 — the Nat 1 correction plus distinct normals and elites, closing the enemy half (BUILD 098) — landed. PART 1: boss_nat_one_passive corrected from 5 self-poison PER loaded poison face (15 on the boss, ~65 total decay damage) to a flat 5 regardless of face count; cancelled-attack behaviour and the player-facing 5-per-face amount are both unchanged. PART 2: buildEnemyDieFaces() generalised to buildEnemyDieFaces(poisonFaceNumbers, includeNats); the elite now carries its own die (poison on faces 7 and 14, deliberately not the boss's 5/10/15, no Nat faces — includeNats false leaves 1/20 as ordinary blanks); the boss keeps its unchanged die (5/10/15 + both Nats); normals/opening stay hasDie:false with no die object. Both dice are built once in buildAct() and never mutated; beginFightFromSlot() copies whichever slot's own die onto the live gameState.enemy.die on fight entry. Caught and fixed a real correctness bug in the same pass: the map's boss preview read the LIVE gameState.enemy.die.faces, which — once two slot types could carry different dice — would show whichever fight was most recently entered under the "BOSS DIE" label; fixed by having both the boss and a new elite map preview (#elitePreview/#eliteDiePreviewList, sitting between the player and boss previews, identical shape to the existing boss preview) read their own slot's static die.faces directly instead. The fight screen's enemy panel needed zero changes — its existing hasDie-keyed conditional already generically shows/hides the die column. Verified in real windowed Chromium via Playwright, file:// direct: boss Nat 1 now lands exactly 5 self-poison (decaying 5→4→3→2), cancelled-attack/once-per-fight/Restart-Fight-reset all still correct; each elite poison face (7, 14) individually lands 5 player stacks with correct decay; forcing face 20 and face 1 on the elite die both produce a bare blank log line with modId:null confirmed, no Nat dispatch, no cancellation, no self-poison; normals confirmed hasDie:false; the elite's die confirmed rendering with faces 7/14 loaded in both the map preview and the real fight-screen enemy panel while fighting it, and the boss's own map preview confirmed unaffected (still 5/10/15). Zero console/page errors. Build stamp reads STAGE 2.29 / BUILD 098.

Stage 2.28 — the boss die's enemy half, the first real enemy mechanic (BUILD 097) — landed; no changes to the player's die, cards, mods, or any player-facing number. Enemy POISON (die faces 5/10/15, loaded since BUILD 075 but never dispatched) now applies exactly 5 poison stacks to the player via a new 'enemy_buff_dispatch' permanent listener on ENEMY_BUFF_TRIGGER (cards-mods.js init(), registered unconditionally, not tied to player classId), routed through the same updatePlayer poisonStacks field every other poison source already uses — no second poison system, and the existing player-poison decay tick needed zero changes. Enemy Nat 20 ('boss_nat_twenty_passive' on ENEMY_NAT_TWENTY) mirrors the Ordained's own onNatTwenty exactly: every loaded buff face fires ascending, uncapped, not once per fight — on the current boss die, 15 total stacks from the three poison faces. Enemy Nat 1 ('boss_nat_one_passive' on ENEMY_NAT_ONE) does two things at once: sets a new turn-scoped gameState.turn.enemyAttackCancelledThisTurn, checked first thing in ENEMY_ACT_PHASE (phase-machine.js) which now returns immediately when it's true — no damage, no intent resolution — and self-poisons the enemy (updateEnemy poisonStacks, same field player cards already use) 5 stacks per loaded poison face on its own die, 15 on the boss. Gated on a new gameState.enemy.natOneFiredThisFight (state.js, mirrors the player's BUILD 084 field), reset in clearFightScopedState()/startNewRun() alongside the enemy's other fight-scoped fields; once fired, resolveEnemyRoll()'s ENEMY_NAT_ONE branch (pipeline.js, now a real dispatch mirroring resolvePlayerRoll()'s shape, replacing the old stub) logs a plain '[ENEMY ROLL] blank' with no re-trigger. All three new log lines use the existing '[ENEMY]' prefix/colour, no new category. Verified in real windowed Chromium via Playwright, file:// direct, dev-jumped to the boss: forcing faces 5/10/15 each landed 5 player poison stacks with correct decay afterward; forcing enemy Nat 20 fired all three poison faces ascending for 15 total stacks with the log reading exactly as specified; forcing enemy Nat 1 left player HP unchanged after ENEMY_ACT_PHASE, applied 15 self-poison to the enemy, ticked the enemy's own HP down by 15 on the next START_OF_TURN, and forcing face 1 again the same fight produced a plain blank with zero re-poison; Restart Fight reset the once-per-fight flag; normal/elite slots confirmed hasDie: false, unreachable. Zero console/page errors. Build stamp reads STAGE 2.28 / BUILD 097.

Stage 2.27 — a tone pass on the existing sounds (BUILD 096) — landed; no new sounds, events, gameplay, numbers, or visuals — only how the existing sounds sound. Fixed the structural cause first: almost every sound shared the same square-wave, same-instant-attack voice, differing only by frequency, so pitch alone could never make them feel distinct. Register lowered substantially on every frequently-fired sound (roll 1400->620Hz, card_attack 1800->480Hz, damage_enemy 2200->820Hz, mod_trigger 1100->420Hz, end_turn 550->200Hz). Waveform reassigned per the prompt: square/sawtooth reserved for sounds that should feel harsh (Nat 1 unchanged; damage_player kept sawtooth; fight_lost explicitly CHANGED sine->sawtooth, since it was named as one of the three harsh sounds and was the one still soft); everything else moved to sine/triangle. Short fade-ins added to soften former instant clicks (roll, card_attack, card_block, card_hybrid, mod_trigger, damage_enemy, block_absorb, end_turn, fight_won, fight_start_normal/elite, card_reward_basic/rich) — damage_player and fight_lost deliberately kept instant-attack (a hit and a loss should feel abrupt). card_attack/card_block/card_hybrid now differ across waveform, envelope, attack time, duration, and single-tone-vs-interval (hybrid is the only one built from two simultaneous tones) so they read as three different actions, not three notes. block_absorb gained real body (18ms fade, 170ms duration). end_turn lost its old square double-tick entirely, now one quiet 200Hz sine blip at gain 0.045 — the quietest sound in the table, since it fires every turn. Load, Strengthen, Nat 20, and Nat 1 are byte-for-byte unchanged, per the prompt's explicit exemption; fight_start_boss and boss_defeated were reviewed (not named exempt) but left unchanged since neither was ever square, tinny, or frequent. A full written spec (waveform/frequency/envelope/duration per sound) was added as a comment block at the top of audio.js. No SOUND_TABLE entries added/removed/renamed, no announce-site touched in any other file, no change to the two pitch chains or their 8-step cap, no change to mute. Verified in real windowed Chromium via Playwright, file:// direct: SOUND_TABLE key set matches BUILD 095 exactly; Strike/Ward/Rite in one turn fired card_attack/damage_enemy/card_block/card_hybrid/damage_enemy with Chain A offsets 0,1,2 across the card sounds; Nat 20 across 10 loaded faces still produced mod_trigger offsets 0..8,8; muting still silenced oscillator creation while announcing the event; a full fight to a win (5 rounds) produced 47 total sound events with no duplicate or orphaned firings. Zero console/page errors. Build stamp reads STAGE 2.27 / BUILD 096.

Stage 2.26 — the sound chain and five new sounds (BUILD 095) — landed; same rules as BUILD 094 throughout. PART 1: two independent Balatro-style pitch chains, Chain A (cards played) and Chain B (mods triggered), living entirely inside audio.js — playAudioEvent() itself detects a chained event name, passes the current step (capped at 8) as the sound function's argument, and advances the counter afterward, so every game-logic call site is unchanged and unaware chains exist. The step becomes a 2^(steps/12) frequency ratio applied to a chained sound's existing frequencies only — timbre untouched. Both chains reset via a new resetSoundChains(), called directly (not a sound) from runPhase()'s START_OF_TURN. Chain B needed a new mod_trigger event/sound, announced from the existing 'mod_dispatch' MOD_TRIGGER listener (cards-mods.js) — the one funnel both a normal roll and Nat 20's loop already share, so no new detection logic, just a new identity for something with one existing dispatch point. PART 2, five new sounds: fight_start_normal/elite/boss (beginFightFromSlot(), keyed off slot.label; boss is a slow-attack low drone allowed past 200ms "to carry weight," the same exception the Nat sounds get); boss_defeated, a new bigger sound branched inside runPhase()'s win guard so the boss case gets it INSTEAD OF fight_won, fixing boss-kill and normal-kill previously sounding identical; die_action_load (spooky: two 8Hz-detuned sine tones with a slow 40ms fade-in — playTone() gained an optional attackMs argument for this, every existing caller omits it unchanged) and die_action_strengthen (plain, instant, non-detuned) replacing BUILD 094's single shared die_action_confirmed entirely; and card reward split into card_reward_basic (Strike/Ward) / card_reward_rich (everything else) in cardRewardPickCard() — worth recording: gameState.config.cardPool never actually contains 'strike'/'ward' (grep-confirmed), so the basic branch is implemented exactly as specified but currently unreachable in real play; reported rather than worked around, pool contents untouched. Verified in real windowed Chromium via Playwright, file:// direct, with the SOUND_TABLE spy extended to also record each call's argument: five cards in one turn (3 attack/1 block/1 attack) fired with offsets 0,1,2,3,4 exactly, timbre still tracking each card's type, a manual mod trigger in between did not touch the card chain's counter, and the next turn's first card fired at offset 0 (reset confirmed); 10 loaded mods forced through Nat 20 fired mod_trigger 10 times with offsets 0,1,2,...,8,8 — climbing one step per mod and correctly holding at the cap; fight-start fired the correct one of three variants per slot type; boss defeat fired boss_defeated exactly once with no fight_won; Load/Strengthen fired distinct single events each; a real reward pick fired card_reward_rich, and a test-aliased 'strike' pick (temporary monkeypatch, not a production change) fired card_reward_basic; muting silenced oscillator creation while events still announced. A full fight to a win (6 rounds) produced 46 total sound events with no duplicate or orphaned firings. Zero console/page errors. Build stamp reads STAGE 2.26 / BUILD 095.

Stage 2.25 — the sound pass: twelve new synthesised sounds (BUILD 094) — landed; no new systems, no gameplay/number/visual changes. Every sound announces from a place the game logic already knows the event occurred — none invented a new detection point. Card play attack/block/hybrid: playCard() (cards-mods.js), classified by card.type with rite/litany special-cased to hybrid (the only two cards whose effect() calls both dealDamage() and dealBlock()); utility cards get no card-play sound. Damage-landing (enemy and player) share one hook inside dealDamage() (pipeline.js), gated on damage > 0 — poison ticks bypass dealDamage() entirely so they're silent for free. Block-absorb announces in ENEMY_ACT_PHASE (phase-machine.js) from Math.min(intent, block) > 0, placed before dealDamage() so a partially-blocked hit fires block-absorb then damage-landing, each exactly once; fully-blocked fires only block-absorb, unblocked fires only damage-landing. End turn announces from the endTurnBtn click handler. Nat 20/Nat 1 are new dedicated events split out of resolvePlayerRoll()'s rollOutcome branch (previously both shared the generic roll click); a spent Nat 1 still correctly falls through to roll_blank. Fight won/lost announce inside runPhase()'s existing win/loss guard, which already fires exactly once per fight. Die action confirmed announces from both dieActionPickLoadFace()/dieActionPickStrengthenFace() right after their own updateDie() commits. All twelve built on a new shared playTone(waveform, freqStart, freqEnd, durationMs, peakGain) synth in audio.js (playClick() is now a thin wrapper, unchanged behaviour) — distinguishability designed explicitly via waveform/register/sweep-direction/single-vs-multi-click/detuning, documented per sound. Nat 20 (three ascending clicks, ~260ms) and Nat 1 (a 260ms detuned descending sweep) are the only two exceeding 200ms, per the prompt; the other ten are all under 200ms by construction. Mute is checked once inside playTone(), so the existing checkbox silences all twelve with no duplication. No audio files; /audio/ confirmed still empty. Verified in real windowed Chromium via Playwright, file:// direct, using a SOUND_TABLE spy (every entry wrapped to record its name in order) plus an oscillator-creation counter rather than trying to hear anything: Strike fired exactly [card_attack, damage_enemy]; Ward fired [card_block]; Rite fired [card_hybrid, damage_enemy] with no block_absorb; a partially-blocked enemy attack fired [block_absorb, damage_player] in that order once each, a fully-blocked one fired block_absorb alone, an unblocked one fired damage_player alone; forcing face 20 fired only nat_20, forcing face 1 fresh fired only nat_1, forcing face 1 again (spent) fired only roll_blank; forcing enemy/player hp to 0 (via the same runPhase() re-entry the real win/loss paths use) fired fight_won/fight_lost exactly once each; Load and Strengthen each fired die_action_confirmed exactly once; muted, forcing Nat 20's three-click cascade still announced the event but created zero oscillators. A full fight played to a win through the real UI (6 rounds) produced 44 total sound events, fight_won exactly once, fight_lost zero times, no duplicate or orphaned firings. Zero console errors, zero page errors. Build stamp reads STAGE 2.25 / BUILD 094.

Stage 2.24 — three independent UI/audio items (BUILD 093) — landed, verified and reported separately per the prompt. ITEM 1: the rolled-face highlight's re-trigger bug is fixed — diagnosis confirmed first (renderDieList() rebuilds every row from scratch every render, so any class carrying a CSS animation always restarted it, even on an unrelated re-render like loading a mod mid-turn). .die-row-rolled is now the sustained no-animation class and .die-row-rolled-flash the animated one (blanks keep only .die-row-rolled-blank-flash, no sustained equivalent); renderDieList() tracks a per-container committed roll signature and only applies -flash when it differs from the last commit, deferring the commit itself via queueMicrotask() so a roll's whole synchronous resolution cascade (multiple renders from one roll, e.g. Consecrate's own soul grant) still flashes correctly rather than the flash being consumed by the first of several cascaded renders. No indent/wash/colour/timing values changed. ITEM 2: js/audio.js — one AudioContext + a SOUND_TABLE (same shape as the listener registry) mapping 'roll'/'roll_blank' to two short synthesised square-wave clicks (RuneScape-reference, under 200ms). Game logic never calls a sound function directly — resolvePlayerRoll() (pipeline.js) announces playAudioEvent('roll'|'roll_blank') at the same funnel the highlight uses. unlockAudioOnce() resumes the AudioContext on the page's first pointerdown ({once:true}, self-removing) per browser autoplay policy. A mute checkbox (default unmuted) sits in the existing DEV SETUP bar. No audio files — /audio/ stays empty. ITEM 3: die display order flipped to NAT 20 top / NAT 1 bottom by iterating faces.slice().reverse() in renderDieList() — the real faces array is never reordered, only the DOM append order, so every row still closes over its real face object and face.number; applies to player die, enemy die, and both map preview dice via the one shared function. Verified in real windowed Chromium via Playwright, three separate fresh file:// page loads (no server), one per item: Item 1 — flash on first roll, held (not re-flashed) across two separate mod-load re-renders, re-flashes on a genuine new roll, blank flashes once and goes fully plain after a later re-render — all six checks true. Item 2 — AudioContext running immediately after the first real click; non-blank and blank rolls invoke different SOUND_TABLE branches and each create exactly one oscillator; muted, the event still fires but creates zero oscillators. Item 3 — all four die displays read 20-to-1 top-to-bottom; clicking a Load-picker row displaying face 17 (sitting at an unrelated DOM position, to rule out a position-based mistake) logged 'loaded ... onto face 17' exactly, same for Strengthen, and hover text matched the clicked row's own face in both cases. Zero console/page errors across all three runs. Build stamp reads STAGE 2.24 / BUILD 093.

Stage 2.23 — three engine fixes carried over from the BUILD 091 code audit (BUILD 092) — landed; no new features, no numbers, no visual changes, plumbing for the systems work after this. registerListener() (listener-registry.js) now dedups on same hook + same id, logging '[LISTENER] duplicate registration skipped: HOOK ID' and skipping the push rather than silently accumulating — no existing call site touched, since all eight real ones already use stable unique ids. healPlayer(amount) + a new ON_HEAL hook added to pipeline.js (direct Math.min(hp+amount, maxHp) clamp, deliberately no multiplier stage per the prompt); riteChooseHeal() (rendering.js) now calls it instead of a bare updatePlayer({hp:...}), same 20 HP, same cap, same '[RITE] healed N HP (before to after)' log line, now observable via ON_HEAL. clearListeners('fight') deleted from its only two call sites (clearFightScopedState()/startNewRun() in run-and-map.js) — both were always dead no-ops, since grep across every registerListener() call confirmed only 'permanent'/'turn' are ever really passed as clearOn; clearListeners('turn') is untouched and still live. Verification only, no code change: registered a throwaway BLOCK_MULTIPLIER listener (×3) inside a disposable Playwright page.evaluate(), confirmed dealBlock(10,...) goes 10 → 30 → 10 (baseline / multiplied / after removing the throwaway listener) — generateBlock()'s BLOCK_MULTIPLIER stage works correctly when something registers against it, which is what Obelisk's design needs; the throwaway registration was never added to any file. Verified in real windowed Chromium via Playwright, file:// direct: build stamp reads STAGE 2.23 / BUILD 092; the dedup guard confirmed skipping a genuine duplicate hook+id registration (array stays length 1, fires once); a full fight played to a win (7 rounds) through the real UI, then die action Skip + card reward Skip back to the map; hp lowered by 15, then a real rite Heal 20 HP click landed correctly (47→67) with the same log line as before, and a throwaway ON_HEAL spy confirmed the heal actually dispatches through the hook. Zero console errors, zero page errors. Build stamp now STAGE 2.23 / BUILD 092.

Stage 2.22 — the file split: nine files under /js/ loaded via ordinary `<script src>` tags, replacing index.html's single ~3,217-line inline script (BUILD 091) — landed; structural only, zero behaviour/logic/number/visual changes, verified by playing a full fight through the real UI after the split. Split: state.js, listener-registry.js, pipeline.js, cards-mods.js (init() kept intact — cards/mods/classes aren't separable files in this codebase, they're all built inline in one function), run-and-map.js, phase-machine.js, rendering.js (log/refreshInspector/every render*/the three screen-flow state machines/the map renderer), dev-tools.js, bootstrap.js (every addEventListener wiring + the sole DOMContentLoaded->init kickoff). A static scan confirmed all 109 original top-level names land in exactly one new file each — zero missing, zero duplicated, none introduced. Ordering trap (item 4a) resolved explicitly: init() is never called eagerly anywhere, the only kickoff is bootstrap.js's DOMContentLoaded listener, and a full re-read confirmed the ONLY top-level immediately-executing statements in the whole original script are self-contained data literals and addEventListener registrations (whose bodies defer to click-time) — so every cross-file function-body reference (including the original dieActionStep case, state.js's helpers calling rendering.js's refreshInspector(), and a genuine rendering.js/run-and-map.js circularity) is safe regardless of file order, because nothing runs until every file has loaded. Chosen order is dependency-first anyway, for readability. Code audit (reported, not fixed, per the prompt): registerListener has no dedup (still live), healPlayer/ON_HEAL don't exist (still live, riteChooseHeal() heals via a bare updatePlayer() call), clearOn 'fight' is registered nowhere and stays dead code (still live), and dealBlock's "no multiplier stage" finding is now stale — generateBlock() has a full BLOCK_MULTIPLIER stage structurally, just presently unexercised by any mod. /audio/ and /art/ created, both empty. Verified in real Chromium via Playwright opening file:///.../index.html directly (no server): zero failed requests, build stamp reads STAGE 2.22 / BUILD 091, the BUILD 090 rolled-face highlight still works unchanged, and a full fight (roll → play cards → End Turn ×4 rounds → win → die action Skip → card reward Skip → back to map) completed with zero console errors, zero warnings, zero page errors. Build stamp now STAGE 2.22 / BUILD 091.

Stage 2.21 — the rolled-face highlight on the player's die column (BUILD 090) — landed; presentation plus one small piece of state, no combat logic/numbers/face behaviour touched. gameState.turn.rolledFaceNumber is the new field (initial schema, startNewRun(), resolvePlayerRoll(), and the existing START_OF_TURN clear alongside rollOutcome/rolledFaceWeight) — deliberately a gameState property rather than a module-scope variable, so it doesn't repeat the BUILD 078 riteStep lesson about module-scope flow state going stale across resets/dev-jumps. renderDieList() adds 'die-row-rolled' (flash to a sustained gold wash + slight right indent, holds for the rest of the turn) or 'die-row-rolled-blank' (a quiet dim --blank-coloured pulse that fades to nothing, no indent, no gold) to the row matching rolledFaceNumber, only on #playerDieList and #dieActionDieList — never the enemy die or the map's static preview. No outline anywhere (BUILD 080's lesson) — only transform and background, so text colour (.loaded/.nat-one/.nat-twenty) is never touched and box model/row width never changes. Confirmed the rolled state and the BUILD 079 picker's pickable state coexist on the same row (different classes, different CSS properties, both applied independently). Verified in real windowed Chromium via Playwright (now installed persistently in the project per Fergus's request): exactly one row highlighted per roll, sustained through CARD_PHASE, cleared before the next roll, blank rolls get the quiet variant only, row widths identical (474px) before/after, and a screenshotted picker row showing both the rolled wash and the pickable gold frame at once. Zero console/page errors. Build stamp now STAGE 2.21 / BUILD 090.

Stage 2.20 — the class card can now be removed at a rite; one restriction dropped (BUILD 089) — landed; ruling 7 (unique class cards cannot be discarded) protects against the in-fight discard mechanic (Shove/Crush/Take It — none of which exist in the codebase yet, grep-confirmed) where losing the class card mid-turn is an accident, not against a rite's own deliberate, between-fights removal choice. riteRemoveCard()'s classRestriction early-out and the picker's matching btn.disabled line are both gone — every owned card, Rite included, now removes exactly the same way (splice from ownedCards/deck/discard/hand, same log line, same closeRiteScreen()+advanceRun() close). The .die-action-row button:disabled CSS rule itself was left in place, per the prompt, even with no current caller. Checked and reported rather than assumed: nothing else in the file reads the class card's presence — Orison reads only rollOutcome, startingDeck is a static array read once by startNewRun() and never touches ownedCards, and the discard mechanic ruling 7 actually governs isn't built yet — so nothing else needed changing. Verified via a live jsdom drive of the real index.html: the picker rendered all 10 owned cards enabled including Rite, clicking Rite dropped ownedCards to 9 with the id gone from deck/discard/hand and the rite advancing normally; separately rebuilt a fresh 9-card deck from the post-removal ownedCards and ran drawCards(1) through 40 consecutive draw/discard/reshuffle cycles with zero exceptions and 'rite' never resurfacing. Removing a non-class card re-tested as byte-for-byte identical to BUILD 088. Zero JS errors. Build stamp now STAGE 2.20 / BUILD 089.

Stage 2.19 — a third rite option, Remove a card, alongside Heal and Take a die action (BUILD 088) — landed; the rite panel's #riteScreenPanel gained a second internal step (riteStep 'remove_pick_card', same choose-then-advance shape dieActionStep's own load_pick_mod/strengthen_pick_face sub-steps already use) rendering the player's full ownedCards collection as a picker, one button per card, reusing the exact per-card render shape (name + cost, CARD_EFFECT_TEXT hover-tip) renderCardRewardPanel() already builds. Rite (classRestriction: 'ordained') renders visibly disabled per ruling 7 (unique class cards cannot be discarded) — a new .die-action-row button:disabled CSS rule, plus a matching guard in riteRemoveCard() itself as the actual source of truth. Picking a card is the commit point: one updatePlayer() call splices it out of ownedCards and out of deck/discard/hand if present, logs '[RITE] removed CardName from deck, deck now N cards', then calls closeRiteScreen() and advanceRun() exactly as riteChooseHeal() already does — no back button anywhere, matching Heal/Take a die action's own irreversible-on-click behaviour. Verified via a live jsdom drive of the real index.html: dev-jumped to a rite, all three buttons render, Remove a card opened a 10-card picker with Rite disabled and every other card enabled, clicking a Strike button dropped ownedCards from 10 to 9 (the other four Strikes untouched), removed the id from deck too, closed the rite, and advanced run.currentSlot to the next slot — zero JS errors, and Heal/Take a die action re-confirmed byte-for-byte unchanged. Not verified this session: a real windowed browser (jsdom only, no Playwright/Chromium available), and an actual subsequent fight's hand/discard watched over several turns to visually confirm the removed card never reappears — confirmed instead by direct inspection that the id is gone from ownedCards/deck/discard/hand immediately after removal, which is what every later draw/shuffle reads from. Build stamp now STAGE 2.19 / BUILD 088.

Stage 2.18 — New Run moved away from End Turn and gated behind a confirm while a run is live (BUILD 087) — landed; presentation and one safety check only, no run logic changed, per the prompt. Motivated by Fergus's own playtest: New Run and End Turn sat side by side as visually identical buttons, and since New Run discards the current run permanently (permadeath), a misclick mid-run was unrecoverable. #startGameBtn moved out of .controls-buttons (which now holds only End Turn) into a new .top-bar div next to the build stamp at the very top of the page, restyled small and red-bordered (11px/red text/red border) rather than sharing End Turn's big 17px gold primary-action look — different position and different styling, not just a spacing nudge; the old shared `#startGameBtn, #endTurnBtn` CSS rule was split in two so End Turn's own look, disabled state, and click handler are byte-for-byte untouched. The safety check: the click handler now reads gameState.run.outcome (the same field enterSlot()/devJumpToSlot() already gate on) and, only when it's 'active', calls the native confirm('Abandon this run and start a new one?') before doing anything else — declining logs '[CLICK] New Run — cancelled at confirm' and returns with zero state touched; accepting, or a run that's already 'won'/'lost', falls straight through to the original '[CLICK] New Run' + startNewRun() exactly as BUILD 086 had it. confirm() was chosen over a second-click toggle per the prompt's own "whichever is less code" — one added conditional and two log lines, no new state, no second render pass. Re-read the new .top-bar markup/CSS, the trimmed .controls-buttons, the split button rules, and the click handler after editing — all landed as intended, and node --check equivalent (new Function() over the extracted inline script) passed with zero syntax errors. Not verified this session: the actual confirm() dialog and the visual separation were not eyeballed in a rendered browser (Playwright/Chromium unavailable), so the guard is verified by reading its logic against gameState.run.outcome's three possible values rather than a click-through. Build stamp now STAGE 2.18 / BUILD 087.

Stage 2.17 — the elite intent band drops from 12-22 to 10-18, average 14 (BUILD 086) — landed; one number pair in buildAct()'s upper-lane elite slot, same shape as BUILD 085, no new logic and no new systems. Motivated by BUILD 085 leaving the run's threat curve backwards: moving the boss to 10-20 (average 15) put it below the elite's still-12-22 (average 17), so the mid-run fight read as tougher on paper than the final one; Fergus's call was 10-18 for the elite (average 14), between normal fights' 12 and the boss's 15, restoring the intended climb. Confirmed there is no separate elite intent preview on the map to route — the only rendered intent preview in the file is #bossPreview (boss-only) — so this was a single-site buildAct() edit with no second reader to check; the in-fight roll (the same inline formula in runPhase()'s START_OF_TURN branch BUILD 085 already confirmed reads off gameState.enemy.intentMin/intentMax) needed no change. Brought the same multi-line comment above buildAct() BUILD 085 had already corrected for the boss up to date for the elite too, rather than letting it go stale the way it briefly did last build. Re-read buildAct() and its comment, and grep-confirmed the elite's HP (100), reward (dieActionsRemaining set to 2, gated on wonSlot.label === 'Elite'), and position (fourth slot, upper lane) are untouched — only intentMin/intentMax changed on that one object. Verified: the extracted inline script parses with zero syntax errors, and the exact roll formula plus all four exact numeric bands as they appear in the file were reproduced verbatim in a standalone Node script and rolled 10,000 times each with zero results outside 10-20 (boss), 10-18 (elite), 6-18 (normal), or 4-12 (opening). Not verified this session, same caveat as BUILD 085: an actual rendered browser dev-jump to the elite with ten-plus live rounds watched in a window — Playwright/Chromium was not available in this session's environment, so the ten-round confirmation rests on the formula/data match above, not an eyeballed run. Build stamp now STAGE 2.17 / BUILD 086.

Stage 2.16 — the boss intent band drops from 16-28 to 10-20, average 15 (BUILD 085) — landed; one number pair in buildAct()'s boss slot, no new logic and no new systems, per the prompt. Motivated by three full played runs all dying to the boss on arithmetic — roughly 15 points of cards a turn spent as either damage or block, never both, against a boss averaging 22 under the old 16-28 band; the recorded position is that the boss should threaten by its distribution, not its intent number, and its die stays inert this build. Confirmed both the map's boss preview and the in-fight intent roll already read off the slot config with no competing literal anywhere else in the file (grep-confirmed no other 16 or 28 in the codebase touches intent) — this was a single-site data edit, not a routing fix. Also found and corrected a stale multi-line comment directly above buildAct() that still described the boss band as "16-28 — unchanged this build" from an earlier point in this build's own edit history, after the data had already been changed to 10/20 but before the comment was; the comment and the data no longer disagree. Re-read buildAct(), the map preview, beginFightFromSlot(), and the intent-roll formula after editing — opening 50 HP/4-12, normals 70/78/85 at 6-18, elite 100 HP/12-22, boss 100 HP/10-20/hasDie true, all byte-identical to BUILD 084 apart from the one number pair. Verified: the extracted inline script parses with zero syntax errors (new Function() over the whole script block), and the exact roll formula plus the exact numeric bands as they appear in the file were reproduced verbatim in a standalone Node script and rolled 10,000 times per band with zero results outside 10-20 (boss), 12-22 (elite), 6-18 (normal), or 4-12 (opening). Not verified this session, and flagged rather than assumed: an actual rendered browser click-through (fresh map showing "Intent 10-20", a real dev-jump to the boss, ten-plus live rounds watched in a window) — Playwright/Chromium was not available in this session's environment, so the ten-round confirmation rests on the formula/data match above, not an eyeballed run. Build stamp now STAGE 2.16 / BUILD 085.

Stage 2.15 — Nat 1 once per fight, Penitence 3 turns (BUILD 084) — landed and verified in a real browser, not only headless jsdom: Playwright driving the full Chromium build against index.html over file://, 20 checks, zero console errors and zero page errors, with a screenshot confirming the stamp, the dev chrome, penitence in Active debuffs and three ascending Nat 20 passes in the log. Both of Fergus's rulings from the BUILD 082 played run are in: face 1 fires once per fight and then resolves as an ordinary blank (2 block) for the rest of it, and Penitence runs exactly 3 turns from onset rather than the rest of the fight. The dead second Nat 1 that prompted the ruling is gone — it now pays out like any other blank, reads that way in the log, and as of this session reads that way to cards too. Resolved from the last session's open question: rollOutcome on a spent Nat 1 is now 'blank', not 'nat_one'. Orison confirmed both ways in the browser and in jsdom — 5 damage on the fight's first face 1, 9 damage and the "blank roll" log branch on the second. The assignment in resolvePlayerRoll() is a single ternary on natOneFiredThisFight, and it is correct only because of ordering: the flag is still false when rollOutcome is computed on the first face 1 (onNatOne() sets it later, during the dispatch), so the outcome string tracks onNatOne()'s own branch exactly. Nothing else in the file read the string 'nat_one' — the assignment was its only occurrence — so Orison is the only behaviour this touched. Two other design points worth confirming. (1) The once-per-fight guard reads natOneFiredThisFight, not penitenceActive. Those are now different things: Penitence expires, the spent Nat 1 does not, so a face 1 rolled on turn 9 of a fight whose Penitence ended on turn 4 still comes out a blank instead of re-arming — verified in the browser. (2) The timing works out as three soul deductions because onset happens in ROLL_PHASE, after that turn's tick has already run, so the ticks land on the three START_OF_TURNs after onset, the third both deducts and clears, and the fourth is clean. That matches the success criterion exactly, but it is worth stating because "3 turns from onset" could also have meant the onset turn counts as one. Not taken literally, and flagged for that reason: the prompt said to log the onset line as it currently does, but that line stated "for the rest of this fight", which the build makes false. It now reads "for the next 3 turns", and the player-facing face-1 hover (NAT_DESCRIPTION.NAT_ONE) was reworded the same way. Both are one-line reverts if the original wording was wanted. Untouched, per the prompt: Nat 20 (uncapped — verified firing on three consecutive passes in ascending face order), the Strengthen-on-face-20 rule (verified through the real picker — face 20 plus every loaded face, never face 1), the mod pool, the rest of the card pool, dealDamage/dealBlock, the boss intent band (BUILD 085), and the BUILD 083 map/dev-chrome work. Needs Fergus's own edit, since Claude Code must not rewrite these sections: the STATE SCHEMA section does not list penitenceActive, penitenceTurnsRemaining or natOneFiredThisFight on player, and the NAT 20 AND NAT 1 — THE ORDAINED section still describes Nat 1 as "for the rest of this fight" with no once-per-fight rule and no note that a spent Nat 1 reads as a blank to cards.

Stage 2.14 — all dev chrome behind one toggle, closed by default, and the setup pause made optional (BUILD 083) — just landed, verified in headless jsdom (a 29-check drive of every stated success criterion, plus a full run played start to finish with the chrome closed and Begin Roll never clicked: 21 rolls, natural loss, zero console errors); not yet eyeballed by a human in a real windowed browser. This closes the two dev-tool hazards named in the prompt. The 4 September false bug report — a dev Next Phase click during CARD_PHASE discarding a hand — is now unreachable by accident, because the whole DEV STEP row is not rendered until someone deliberately opens the dev chrome. And the BUILD 036/037 setup pause, which fired at the start of every fight in a real run and left it waiting indefinitely for Next Phase or Begin Roll, is off by default: init(), New Run, every map slot entry and Restart Fight now run START_OF_TURN and go straight into ROLL_PHASE using the same autoAdvance() every later round already uses. Ticking "Pause before first roll" restores BUILD 037 exactly. One shared entry path either way — startFreshTurnPaused() is still the single function all four routes reach, gated on the checkbox in one line rather than forked. The two dev inputs that live outside the container are inert while it is closed (die-face force rolls, and the BUILD 076 map dev-jump nodes, which render fully disabled the way they already do once a run is won or lost); the BUILD 079 Load/Strengthen die-row picker is player-facing and untouched, and still works with the chrome closed. Not changed, and worth stating because the prompt drew the line there: ROLL_PHASE timing (ROLL_PHASE_PAUSE_MS still 1800), enemy numbers, the player die, the card and mod pools, run state, slot dispatch, the map's real node logic, the phase order, and the hover system. Open for the next session: the dev chrome is hidden with display:none rather than removed from the DOM — the elements still exist and refreshInspector() still writes the inspector's JSON into a hidden <pre> every time it runs, which is what lets the State Inspector toggle and every dev button keep working unchanged when the chrome is opened. Nothing a player can click or focus, but not literally absent either; if "not rendered" is later meant literally, refreshInspector() would need an early-out for the hidden inspector and the dev-button disabled lines would need guarding.

Stage 2.13 — the act grows from three lane slots to five, and the elite now grants two die actions instead of one (BUILD 082) — just landed, verified two ways: a full headless jsdom drive playing both lanes start to finish (real click path for upper, dev-jump for lower), confirming the act's shape, HP progression, elite reward count, and that both lanes correctly reach the boss after their fifth slot rather than early; and, separately, an actual Playwright/Chromium screenshot of the rendered map at both 1400x900 and 1600x1080 — 13 nodes (Start, opening, 5+5 lane slots, boss), centred to within 0px, no overflow, connections/fork/merge legible, boss preview and both dice unchanged — the first build since BUILD 076 to get real eyes-on visual confirmation instead of code-reasoning alone. Confirmed first (as asked) that the elite previously granted nothing beyond a normal fight's single reward, since SLOT_HANDLERS dispatches by type only, never by label. Found and fixed a real bug this change would otherwise have exposed: advanceRun() had a hardcoded `index < 2` bound left over from the old three-slot lane, which would have sent a five-slot lane to the boss after only three slots — now reads the lane's own live length instead of a second magic number. Map rendering and dev-jump needed no changes at all, both already fully dynamic over whatever length gameState.run.act's lanes happen to be. Next up: going back to actually eyeball BUILD 077 through 081 now that Playwright/Chromium is confirmed working (all shipped on code-reasoning/jsdom alone) would close that remaining gap; beyond that, the manual human playtest pass (Stages 2.0 through 2.13), the BUILD 074 open question about the original opening-fight bug report, and the BUILD 071 enemy_buff_poison gap.

Stage 2.12 — widened the Load/Strengthen die-row picker so the longest mod name plus a ×N badge no longer clips (BUILD 081), one CSS rule — just landed, and unlike the last two builds this one WAS visually/numerically verified against a real render: standalone Playwright/Chromium (installed via npm this session, separate from the claude-in-chrome extension declined earlier) measured Consecrate (longest mod name, ~88px at the row's actual 16px Consolas font) plus a two-digit ×N badge (~26px) needing ~122px inside .die-mod-wrap, found the container was only rendering ~134px total despite already declaring max-width: 260px (an auto-sized ancestor flex context was shrink-wrapping it well below that ceiling), added min-width: 220px to force the room open, then re-measured with getBoundingClientRect() to confirm zero clipping and all twenty rows uniformly 220px wide. Fight screen (#playerDieList/#enemyDieList) and map (#mapPlayerDieList/#bossDiePreviewList) die rows use their own separate ID selectors, confirmed not shared with #dieActionDieList, so per the prompt's own instruction they were correctly left untouched. Re-ran the standing jsdom behavioural drive afterward — Load/Strengthen flow, eligibility, and the two-line Strengthen hover all byte-for-byte identical to BUILD 079/080, zero JS errors. Next up: now that real browser tooling has been confirmed available this session, going back to actually eyeball BUILD 077 through 080 (all of which shipped on code-reasoning/jsdom alone, not a rendered screenshot) would close that standing gap; beyond that, the standing manual human playtest pass (Stages 2.0 through 2.12), the BUILD 074 open question about the original opening-fight bug report, and the BUILD 071 enemy_buff_poison gap.

Stage 2.11 — a CSS-only presentation pass fixing three browser-reported bugs in the BUILD 079 die-row picker (BUILD 080) — just landed; the fix itself could not be visually confirmed this session (browser tooling unavailable), only reasoned from the stylesheet's own box-model numbers (the BUILD 040/041 "measure before you clip" habit) and re-verified behaviourally unchanged via the same jsdom drive BUILD 079 used. Root cause of all three reports (doubled gold borders between adjacent eligible rows, mod-name-disappears-into-solid-gold-hover with a clipped ×N badge, and inconsistent row widths) traced to one thing: .die-row-pickable's outline-offset: 2px painted outside the row's own box, which flush-stacked rows with no gap never account for. Swapped to an inset box-shadow (never paints outside the box, can't be clipped, can't create a gap-separated double line) and replaced the hover's losing text-colour override (beaten by .loaded/.nat-twenty's higher-specificity colour rules — worst case literally --nat-on-under--nat for face 20) with a translucent --nat wash that never touches text colour at all. No eligibility, flow, weight arithmetic, or JS of any kind was touched. Next up: real browser verification of this build once tooling is available (this and BUILD 077-079 have all shipped without eyes-on confirmation), the standing manual human playtest pass (Stages 2.0 through 2.11), the BUILD 074 open question about what the original opening-fight bug report actually saw, and the BUILD 071 enemy_buff_poison gap.

Stage 2.10 — the die action screen's own die becomes the Load/Strengthen face picker, both separate face-button screens deleted (BUILD 079) — just landed, verified via a live jsdom drive of the real index.html (forced win → Load → mod pick → clicked a blank die row → mod landed and advanced straight to card reward; Strengthen → face 20 pickable with a two-line hover (current description, then the same description at weight+1) → click → weight 1→2; face 1 confirmed unpickable in both flows; zero JS errors) — not yet verified in an actual browser, headless or windowed, since browser tooling was unavailable this session. renderDieList() took an optional pickConfig arg with every other call site (fight screen, map die previews) passing nothing and rendering unchanged; only the die action screen's own die preview passes it, only during load_pick_face/strengthen_pick_face, using the exact eligibility filters the deleted button screens used. Strengthen's header fixed from "Choose a loaded face to strengthen" to "Choose a face to strengthen" (face 20 was always offered despite carrying no mod). Next up: the standing manual human playtest pass (now Stages 2.0 through 2.10, none eyeballed by a human, and nothing since 2.7 has had any browser verification at all), the BUILD 074 open question about what the original opening-fight bug report actually saw, the BUILD 072 load_pick_face die-preview question (now moot — load_pick_face shows and uses the die as of this build), and the BUILD 071 enemy_buff_poison gap.

Stage 2.9 — the riteStep/dieActionStep/cardRewardStep reset in devJumpToSlot() (BUILD 078) — just landed; not yet verified in any browser, headless or windowed (browser tooling was unavailable this session; verified only via a node --check syntax pass on the extracted scripts). Closes the rite-panel-abandonment gap BUILD 077 traced but did not fix: devJumpToSlot() now nulls all three module-level flow variables immediately after its run.outcome guard, so jumping away from an unresolved rite/die-action/card-reward panel dismisses it instead of leaving it rendered, clickable, and permanently blocking End Turn. Nothing else in devJumpToSlot() changed. Next up: the standing manual human playtest pass of Stages 2.0 through 2.9 (still none eyeballed by a human, and no build since 2.7 has had any browser verification at all), the BUILD 074 open question about what the original opening-fight bug report actually saw, the BUILD 072 load_pick_face die-preview question, and the BUILD 071 enemy_buff_poison gap.

Stage 2.8 — run-outcome guards on the current map node's real click path and on Restart Fight, plus a rite-panel abandonment trace (BUILD 077) — just landed; not yet verified in any browser, headless or windowed (browser tooling was unavailable this session; verified only via a node --check syntax pass on the extracted scripts). Both dev paths BUILD 076 left open are now closed: enterSlot() — the shared function every real current-node click (opening/lane/boss) calls — now no-ops once gameState.run.outcome !== 'active', matching devJumpToSlot()'s guard; devRestartFightBtn's click handler and its refreshInspector() disabled line now check run.status/run.outcome the same way BUILD 076 gated Skip to Die Action. New gap found and reported, not fixed: dev-jumping away from an unresolved rite (Heal/Die-action choice still open) leaves the rite panel stuck rendered and clickable indefinitely, because riteStep lives outside gameState and devJumpToSlot() never resets it — this also leaves endTurnBtn permanently disabled for the rest of the run, since its own guard requires riteStep === null. Smallest fix identified (reset riteStep/dieActionStep/cardRewardStep at the top of devJumpToSlot()) but intentionally not applied per this build's trace-only scope. Next up: applying that rite-abandonment fix if wanted, the standing manual human playtest pass of Stages 2.0 through 2.8 (still none eyeballed by a human, and this build has had no browser verification at all), the BUILD 074 open question about what the original opening-fight bug report actually saw, the BUILD 072 load_pick_face die-preview question, and the BUILD 071 enemy_buff_poison gap.

Stage 2.7 — a dev tool to jump to any map slot, plus the BUILD 074 Skip to Die Action guard fix (BUILD 076) — just landed, verified via a real headless-browser (Playwright/Chromium) drive of index.html; not yet eyeballed by a human in a real windowed browser. Every map node not currently standing-on is now dev-jumpable — completes the path to it and enters through the exact same enterSlot()/SLOT_HANDLERS/beginFightFromSlot() every real arrival uses, no parallel path — styled distinctly (--muted, dotted border) from a real choice/current node (--nat, solid/dashed) so it can't be mistaken for one during a normal playthrough. Both dev-jump nodes and the now-fixed Skip to Die Action button correctly no-op once the run is won or lost. Reported, not fixed, discovered while verifying that loss-guard: the genuinely-current node's own real click path (untouched by this build) has no run.outcome/status guard of its own and stays clickable even after the run has ended — a distinct, pre-existing gap from the Skip to Die Action one just closed, out of scope for "fix that Skip to Die Action gap" specifically. Next up is deciding whether that newly-found current-node gap is worth its own small fix, the standing manual human playtest pass of Stages 2.0 through 2.7 (still none eyeballed by a human), the BUILD 074 open question about what the original opening-fight bug report actually saw, the BUILD 072 load_pick_face die-preview question, and the BUILD 071 enemy_buff_poison gap.

Stage 2.6 — boss HP 150→100, and the enemy die removed from normals/elites (BUILD 075), verified via headless browser, not yet human-eyeballed — see CONFIRMED WORKING. Reported, not fixed: ENEMY_BUFF_TRIGGER/ENEMY_NAT_ONE/ENEMY_NAT_TWENTY/enemy_buff_poison remain reachable exclusively through the boss's own die.

Stage 2.5 — BUILD 074 diagnosed a reported "opening fight grants no card reward" bug and could not reproduce it; see CONFIRMED WORKING for the full four-part trace. No code was changed beyond the build stamp; there was no defect to fix.

Stage 2.5 — a shared opening fight between Start and the fork (BUILD 073), verified via headless browser at both 1400x900 and 1600x1080, not yet human-eyeballed — see CONFIRMED WORKING. A run now opens on a 50 HP/4-12-intent fight before either lane is reachable; the fork's two options are correctly inert until it's won.

Stage 2.5 — a shared opening fight between Start and the fork (BUILD 073), verified via headless browser at both 1400x900 and 1600x1080, not yet human-eyeballed — see CONFIRMED WORKING. A run now opens on a 50 HP/4-12-intent fight before either lane is reachable; the fork's two options are correctly inert until it's won.

Stage 2.4 — the player die alongside the boss die on the map, and during the die action (BUILD 072), verified via headless browser, not yet human-eyeballed — see CONFIRMED WORKING.

Stage 2.3 — hover descriptions on the die action Load list and both die rows (BUILD 071), verified via headless browser, not yet human-eyeballed — see CONFIRMED WORKING.

Stage 2.2 — hover descriptions on reward/die-action choices, plus the reward duplicate fix (BUILD 070), verified via headless browser, not yet human-eyeballed — see CONFIRMED WORKING.

Stage 2.1 — a visual pass on the act map, plus the fightScreen/controls-row overlap regression (BUILD 069), verified via headless browser, not yet human-eyeballed — see CONFIRMED WORKING.

Stage 2.0 — the run scaffold (BUILD 068), verified via headless browser, not yet human-eyeballed — see CONFIRMED WORKING.

Stages 1.13, 1.14, and 1.15 — die face weight display and the Strengthen rule change, the Nat 20 mechanic, and Nat 1's Penitence — were all browser-confirmed by Fergus on 24 August 2026. All three are fully human-verified, not just headless-verified. With this confirmation, both halves of the 24 August 2026 NAT 20 AND NAT 1 — THE ORDAINED decision are implemented and human-confirmed (Nat 20 in BUILD 066, Penitence in BUILD 067). No further items from that section remain queued.

Stage 1.13 — die face weight display plus the Strengthen rule change (BUILD 065), verified via headless browser, not yet human-eyeballed — see CONFIRMED WORKING.

Stages 1.1 through 1.10 confirmed working. The full loop is playable end to end: draw, roll, play cards, block, take enemy damage, reach win or loss. Two playtest bugs found and fixed in BUILD 013 (see CONFIRMED WORKING): win/loss now checked at the top of every phase transition instead of only at the CHECK_WIN_LOSS-named phase, and playCard() now refuses outside CARD_PHASE. Three CSS/markup-only visual passes followed: BUILD 014 (initial readability sizing), BUILD 015 (taller panels, doubled die faces, centrepiece roll display, real hand cards), and BUILD 016 (full-width divider-separated stat rows, stronger red/blue panel tints and borders, raised secondary-text contrast, centred/enlarged Start Game & Next Phase, card-play shake/pulse/pop feedback, HP-change number pops) — see CONFIRMED WORKING for the full breakdown, including the three read-only log()-parsing hooks (roll display, turn reset, card-play fx) added across these passes for live behaviour that pure CSS can't provide. Not yet re-verified in browser — needs a playtest pass confirming: the game halts immediately when enemy HP crosses 0 mid-CARD_PHASE; clicking a hand card outside CARD_PHASE is refused and logged with no state change; all twenty faces are visible on both dice with no scrolling; the roll-hero updates correctly on each roll and resets at the start of each turn; hand cards show name/cost/effect and dim correctly when unaffordable; both panels read as clearly red/blue tinted at a glance; Start Game/Next Phase are centred beneath the hand and easy to click while the inspector toggle stays small; playing Strike/Ward/Rite produces the correct panel and number feedback; HP changes visibly pop the number; and none of BUILD 014/015/016 introduced a functional regression (draw, roll, card play, enemy action, phase transitions all still work, zero console errors) while the log panel remains pixel-identical throughout.

Consecrate + die-face-clipping fixes (BUILD 018) not yet browser-verified — needs a playtest pass confirming: player die face 10 reads Consecrate and dev_test appears nowhere (die list, mods, log); all twenty face numbers on both dice render fully with no clipping/overlap; rolling face 10 takes soul 3→5; each card played that turn (including ones paid for with the granted soul) adds 3 block and logs correctly; advancing a turn resets soul to 3 with no carryover; ordinary card play (strike/ward/rite) still works correctly with no console errors across a full round.

Dev tool — force roll (BUILD 019) also not yet browser-verified — needs a playtest pass confirming: Start Game, reach ROLL_PHASE, click player face 10 → [DEV] forced roll: face 10 then Consecrate fires (soul 3→5); a second face click that same ROLL_PHASE visit does nothing; clicking a blank face on a later turn gives 3 block; clicking face 20 fires the Nat 20 routing; clicking any player face outside ROLL_PHASE is refused and logged; the same all holds for the enemy die faces gated to ENEMY_ROLL_PHASE; hover state is visibly obvious on the face buttons; zero console errors across the pass. This is dev tooling only, explicitly marked for removal before release — not a gameplay substage.

Dev tool — load mod on demand (BUILD 020) also not yet browser-verified — needs a playtest pass confirming: load Consecrate onto face 5, die list shows it there; force-roll face 5, soul goes 3→5 exactly as face 10 does; clear face 5, die list shows blank; force-roll face 5 again, 3 block with no soul gain and no leftover Consecrate behaviour; load a mod onto two faces at once and confirm both trigger correctly when forced; attempting to load/clear face 1 or 20 is refused and logged; zero console errors. This is dev tooling only, explicitly marked for removal before release — not a gameplay substage.

Dev mod loader readability fix (BUILD 021) not yet browser-verified — needs confirmation that the DEV LOAD MOD row is legible without leaning in, the dropdown/face-input/Load/Clear are comfortably clickable, it still visually reads as a dev tool separate from Start Game/Next Phase, load+clear still work end to end, and zero console errors.

Blank passive value change (BUILD 022) not yet browser-verified — needs a playtest pass confirming: force-rolling any blank face logs 2 block generated and player Block increases by exactly 2; a loaded face (e.g. Consecrate on whatever face it's currently on) still triggers its mod normally and unaffected; advancing a turn still clears block to 0 at START_OF_TURN; zero console errors.

Force-roll stacking fix (BUILD 023) not yet browser-verified — needs a playtest pass confirming: Start Game, reach ROLL_PHASE, force a blank face — Block reads exactly 2 (not 4), only one [BLANK] line in the log; on a later turn, force Consecrate on face 10 — soul goes 3→5 with no blank block underneath it; a turn where nothing is forced still resolves exactly one natural roll when Next Phase is clicked to leave ROLL_PHASE; the same holds for the enemy die in ENEMY_ROLL_PHASE; zero console errors.

Smite (BUILD 024) not yet browser-verified — needs a playtest pass confirming: dev-load Smite onto face 7, force roll it, log reads [MOD] smite: 16 damage and enemy HP drops from its (now 70) max with the HP number flashing per existing feedback; clear face 7, force roll it, back to 2 block with no residual Smite behaviour; load Smite and Consecrate onto separate faces and confirm each triggers correctly and independently; zero console errors.

Fight-length balance pass (BUILD 025) not yet browser-verified — needs a playtest pass confirming: enemy panel reads 70/70; hand cards show Strike as 5 damage and Rite as 5 damage 6 block; playing three Strikes in one turn drops the enemy from 70 to 55; force-rolling Smite still lands 16 damage correctly on top of the new HP total; a full fight with no mods loaded takes roughly five turns rather than three; zero console errors.

Variable enemy intent (BUILD 026) not yet browser-verified — needs a playtest pass confirming: Start Game shows an Intent number and "Attacks for N" label between 6 and 18 before CARD_PHASE is reached; advancing several turns shows the intent changing each turn, always in range, with one [ENEMY] intent set to N line per turn; a turn with block less than intent lands the correctly reduced damage; a turn with block exceeding intent lands zero damage; six turns show a visible spread of different intent values; zero console errors.

Penance (BUILD 027) not yet browser-verified — needs a playtest pass confirming: dev-load Penance onto face 4, force roll it, log reads [MOD] penance: 8 damage, 8 block, enemy HP drops by exactly 8, player Block increases by exactly 8; both the enemy HP number and player Block number show their visual feedback; advancing a turn still clears block normally at START_OF_TURN; clearing face 4 and force-rolling it returns to 2 block with no residual Penance behaviour; zero console errors.

Offering (BUILD 028) not yet browser-verified — needs a playtest pass confirming: dev-load Offering onto face 8, force roll it, log reads [MOD] offering: +2 soul this turn, draw 1 followed by a [DRAW] line, Soul goes 3→5, hand grows 5→6; playing five one-cost cards is all permitted; advancing a turn resets Soul to 3 and hand back to 5 with no carryover; force-rolling Offering on a turn with an empty deck triggers the reshuffle correctly; clearing face 8 and force-rolling it returns to 2 block with no residual soul or draw; zero console errors.

Turn-flow auto-advance (BUILD 029) not yet browser-verified — needs a playtest pass confirming: the page loads straight into Round 1, CARD_PHASE, 5 cards in hand, a roll already resolved; playing cards then clicking End Turn once runs the enemy attack and lands back in CARD_PHASE of round 2 with a fresh hand and a new roll, with every phase header present in the log; a full fight can be played to victory using only End Turn and card clicks, with the win halt still working; the relocated dev step control (bottom of page) still advances exactly one phase per click; force roll still works when the game is sitting in ROLL_PHASE/ENEMY_ROLL_PHASE (via the dev step control pausing there); Restart Fight correctly resets to round 1; zero console errors.

Enemy poison tick (BUILD 030). START_OF_TURN's phase-machine block gained an enemy poison branch (placed immediately after the existing player-poison branch): if gameState.enemy.poisonStacks > 0, damage = calculateDamage(poisonStacks) per Law 1, applied straight to enemy.hp via updateEnemy alongside poisonStacks - 1 in the same call, logged as [POISON] N damage, stacks now M; at 0 stacks it logs nothing and does nothing, matching the spec exactly. Poison bypasses block by construction — it's applied via calculateDamage() directly to hp, never routed through the intent-minus-block subtraction ENEMY_ACT_PHASE uses — with an explicit comment recorded at the call site for when enemy block exists later. Persistence needed no new code: poisonStacks already lived in gameState.enemy and resetFight() already zeroed it, so it now persists turn-to-turn within a fight and clears on Restart Fight for free. The Poison Stacks stat panel (#enemyPoisonValue) already read gameState.enemy.poisonStacks live in renderStats() from a prior stage — no rendering change was needed, it started reflecting real values the instant the tick started writing them. Added a DEV ONLY control (#devPoisonApplier: number input + "Apply to Enemy" button) beside the existing DEV LOAD MOD bar, wired to devApplyPoison() which adds the entered amount to gameState.enemy.poisonStacks via updateEnemy and logs [DEV] applied N poison to enemy — no mod or card applies poison yet; this is purely for exercising the tick ahead of Blight/Virulence. No card, mod, Consecrate/Smite/Penance/Offering, blank passive, enemy intent, or turn-flow logic was touched. Build stamp now STAGE 1.10 / BUILD 030.

Verified via a full jsdom load-and-drive of the actual index.html (not a rewrite, not a unit test of an extracted function): applying 6 poison via the dev control set gameState.enemy.poisonStacks to 6 and the panel read 6; six consecutive End Turn clicks produced enemy HP drops of exactly 6, 5, 4, 3, 2, 1 (21 total) with stacks reading 5, 4, 3, 2, 1, 0 after each; a seventh End Turn produced no [POISON] log line with stacks staying at 0; Restart Fight after re-applying 9 poison brought poisonStacks back to 0 and the panel back to 0; zero JS errors were captured across the whole run. Still needs a manual hard-refresh browser pass per the standard five-consecutive-clean-fights bar before advancing past this substage — the jsdom pass covers mechanical correctness, not a human eyeballing the actual page.

Blight and Virulence (BUILD 031) not yet browser-verified — needs a playtest pass confirming: dev-load Blight onto face 6, force roll it, log reads [MOD] blight: applied 6 poison and Poison Stacks panel reads 6 immediately; ending the turn ticks poison down per the existing decay behaviour; on a fresh fight, dev-load Virulence onto face 6, force roll it, log reads [MOD] virulence: applied 3 poison, doubled to 6 and the panel reads 6 immediately; on another fresh fight, Blight on face 6 and Virulence on face 7, force-rolling Blight then Virulence on a later turn correctly doubles whatever poison remains from decay, not a fixed baseline; zero console errors. Mechanically confirmed via jsdom load-and-drive of the actual index.html across all three scenarios (see CONFIRMED WORKING) with zero JS errors, but not yet eyeballed in a real browser.

Force-roll auto-advance pause fix (BUILD 032) — SUPERSEDED. Real human playtesting found the 300ms pause was consistently too short to actually click a face; clicks kept landing after auto-advance had already moved to CARD_PHASE, logged as [DEV] cannot force roll outside ROLL_PHASE. The underlying mechanism (defer via setTimeout, let nextPhase()'s existing playerRollResolved guard decide forced vs natural) was correct and was kept; only the pause length changed. See BUILD 033 below.

Force-roll pause length fix (BUILD 033) not yet browser-verified — needs a playtest pass confirming: ending a turn shows ROLL_PHASE visibly pause for roughly 1.5-2 seconds before resolving on its own if nothing is clicked; clicking a face during that pause forces it correctly with only one [ROLL] log line in the log and no "cannot force roll" refusal; a turn where nothing is clicked still resolves naturally after the pause with no errors; End Turn and other controls stay inert (as they already did) during the pause; ENEMY_ROLL_PHASE and every other phase transition are not slowed down; zero console errors. Mechanically confirmed via jsdom load-and-drive of the actual index.html across four checks (see CONFIRMED WORKING) with zero JS errors, including a click landing 1000ms into the pause — the exact scenario that failed under BUILD 032 — but not yet eyeballed in a real browser; the perceptibility/feel of the pause is a human judgement the automated pass can't make.

Sanctuary (BUILD 034) not yet browser-verified — needs a playtest pass confirming: dev-load Sanctuary onto a face, force roll it during the ROLL_PHASE pause, log reads [MOD] sanctuary: 16 block and player Block increases by exactly 16 with the Block number's visual feedback firing; advancing a turn clears block back to 0 at START_OF_TURN as normal; clearing the face and force-rolling it again returns to 2 block (blank passive) with no residual Sanctuary behaviour; zero console errors. Mechanically confirmed via jsdom load-and-drive of the actual index.html (see CONFIRMED WORKING) with zero JS errors, but not yet eyeballed in a real browser.

Vigil (BUILD 035) not yet browser-verified — needs a playtest pass confirming: dev-load Vigil onto a face, force roll it during the ROLL_PHASE pause, play some cards but leave at least 2 in hand, click End Turn, and confirm the log shows the correct card count and 5xN block with player Block increasing by exactly that amount; confirm that block survives into ENEMY_ACT_PHASE and reduces incoming damage correctly; advance to the next turn and confirm block clears normally and Vigil does not fire again without being rolled; a turn where the whole hand is played down before ending turn confirms 0 block granted with no errors; zero console errors. Mechanically confirmed via jsdom load-and-drive of the actual index.html (see CONFIRMED WORKING) with zero JS errors, but not yet eyeballed in a real browser.

Restart Fight dev-loading pause (BUILD 036) not yet browser-verified — needs a playtest pass confirming: clicking Restart Fight stops the game right after drawing the hand, before rolling, with the dev tools comfortably usable in that window; two mods can be loaded onto two different faces there; clicking Begin Roll proceeds into ROLL_PHASE and the existing force-roll pause behaves normally from there (both the auto-resolve-after-~1.8s path and the force-a-face-mid-pause path); playing that fight through to End Turn shows every later turn still auto-advancing exactly as before with no extra pauses introduced anywhere else; zero console errors. Mechanically confirmed via jsdom load-and-drive of the actual index.html (see CONFIRMED WORKING) with zero JS errors, but not yet eyeballed in a real browser.

init() dev-loading pause fix (BUILD 037) not yet browser-verified — needs a playtest pass confirming: a hard refresh of the page (not Restart Fight) stops the game right after drawing the opening hand, before rolling, with time to load mods; two mods load correctly during that window; clicking Begin Roll enters ROLL_PHASE with the normal force-roll pause behaving as usual; Restart Fight, tested again mid-session afterward, still pauses correctly via the same shared code path; zero console errors. Mechanically confirmed via jsdom load-and-drive of the actual index.html, simulating a true hard refresh (see CONFIRMED WORKING) with zero JS errors, but not yet eyeballed in a real browser.

Post-fight die action loop (BUILD 038) not yet browser-verified — needs a playtest pass confirming: the dev shortcut ("Skip to Die Action") jumps straight to the Load/Strengthen/Skip screen without needing to win a fight, and all three paths work cleanly through it; a real fight won naturally shows the same screen automatically at victory; choosing Load offers exactly 3 mods excluding Consecrate/Zeal/Fervour/Ordain and anything already on the die, picking one loads it onto a chosen blank face; starting a new fight afterward shows the newly loaded face still present while HP/deck/hand have all reset to fresh-fight state; winning again and choosing Strengthen on an existing loaded face visibly bumps its weight; winning a third time and choosing Skip changes nothing and a new fight still starts cleanly; zero console errors. Mechanically confirmed via jsdom load-and-drive of the actual index.html across both the dev-shortcut path and a real played-to-victory fight (see CONFIRMED WORKING) with zero JS errors, but not yet eyeballed in a real browser — this is the first genuinely player-facing (non-dev-tool) screen in the project, so a human read of its usability/legibility matters more here than on prior dev-tool-only substages.

DEV POISON target selector (BUILD 039) not yet browser-verified — needs a playtest pass confirming: with Player selected and 6 entered, Apply logs [DEV] applied 6 poison to player and the Active debuffs row reads poison x6 immediately; with Enemy selected (the default), behaviour is unchanged from BUILD 038. IMPORTANT — flagged for planning, not fixed here: the player poison tick itself (unmodified, per the prompt's explicit instruction) does not decay. Confirmed via jsdom by calling runPhase('START_OF_TURN') directly five times with poisonStacks pinned at 6: every call dealt exactly 6 damage and poisonStacks was still 6 afterward every time — the tick's code has no poisonStacks decrement at all, unlike the enemy poison tick. So the success scenario's expected 6/5/4/3/2/1-then-stop sequence will NOT be observed in a manual browser pass; expect a flat 6 damage every turn indefinitely instead until the tick itself is fixed in a future substage. Block is correctly never touched by the tick (confirmed by code read, not just runtime), and Restart Fight still zeroes player poison correctly regardless. Mechanically confirmed via jsdom load-and-drive of the actual index.html (see CONFIRMED WORKING) with zero JS errors, but not yet eyeballed in a real browser.

Combat-panel align-items fix (BUILD 040) — superseded by BUILD 041, see below. Correctly fixed the internal distortion (hero-stats undistorted, Deck/Discard cleared #handRow) but left a residual overlap because it couldn't touch the outer #playerPanel min-height that was the real constraint. Kept for the historical record of the investigation; BUILD 041 closes it out.

Combat-panel min-height fix (BUILD 041) — RESOLVES BUILD 040's residual overlap; the player-panel/#handRow bleed-through bug is now closed. Verified via the same real-browser harness (Playwright driving the system's actual msedge.exe against the real index.html) at both required viewports, 1400x900 and 1600x1080: #playerPanel and #enemyPanel both render at the full measured-natural 512px, hero-stats stays undistorted at 255px, and heroStats/debuffsRow/deckRow/discardRow all test false for overlapping #handRow's bounding box — confirmed both numerically and visually via direct screenshots of the hand-row region at both viewports, showing Active debuffs/Deck/Discard fully legible with a clean gap above the hand cards and nothing visible in the card gaps. .fx-pulse-blue's glow confirmed still unclipped (same ancestor-chain check as BUILD 040, no overflow: hidden anywhere between #playerPanel and the document). .left-col now scrolls at every tested height (scrollHeight 1628 vs clientHeight 670-1050) since neither panel compresses below natural size any more — the expected, correct tradeoff. #log, .die-col, die-face row structure, and .fx-pulse-blue's own rule were untouched. Not yet eyeballed by a human in an actual windowed browser (verified via headless real-browser screenshots and measurements instead), but this closes the bleed-through item that BUILD 038-040 opened and left outstanding.

Player poison decay + calculateDamage() fix (BUILD 042) — RESOLVES the divergence flagged by BUILD 039 and reconfirmed by direct investigation before this fix. Player poison now decays exactly like enemy poison (N, N-1, ... down to 0, silent at 0) and routes through calculateDamage(), closing the last known Law 1 bypass. Verified via jsdom load-and-drive of the actual index.html using the real DEV POISON bar: applying 6 poison to the player and ticking seven times produced 6, 5, 4, 3, 2, 1, 0 damage (tick seven silent), 21 total damage, poisonStacks reaching 0, and the Active debuffs panel visibly stepping poison x6 down to — tick by tick; enemy poison re-verified unaffected in the same run (6 applied, one tick, correctly 6 damage/stacks now 5). Not yet eyeballed in a real browser, but this closes both items BUILD 039 left open for player poison.

Dev tool visibility sizing pass (BUILD 043) — NOT COMPLETE, needs a planning decision before this is closed out. A real ~10.3% reduction was measured and applied to the three named regions (combat panels, mid-strip) via padding/row-spacing only — no hero-number font sizes touched, no new overlap or bleed-through (re-verified against BUILD 040/041's fix), confirmed via the same real-browser method used for BUILD 040/041/042. But the actual shortfall between total left-col content (1460px after this pass) and the viewport (870px at 1400x900, 1050px at 1600x1080) was roughly 46% of total content before this pass — far too large for "roughly 10%" tightening on three regions to close. #devPoisonApplier, the last dev-tool row, still sits ~590px (1400x900) / ~410px (1600x1080) below the fold. The stated success criterion "all four dev tool rows visible without scrolling" is not met. Not pushed further because doing so would mean either touching regions the prompt didn't name (hand-row, controls-row, the dev-tool blocks' own spacing) or cutting the three named regions far more aggressively than "roughly 10%" (risking the legibility/no-overlap sub-criteria the same prompt protects) — flagged for a planning decision rather than taken unilaterally.

Zeal (BUILD 044) not yet browser-verified — needs a playtest pass confirming: force-load Zeal onto a face, first trigger deals exactly 10 damage and logs bonus now +4; second trigger deals 14 and logs bonus now +8; Restart Fight, trigger again — damage reflects the persisted bonus rather than resetting to 10 (verified as 18, continuing correctly from +8, not the prompt's own arithmetically-inconsistent "still 14" example — see CONFIRMED WORKING note); triggering five times total across two fights produces no duplicate log lines, no console warnings, and no listener ever registered with 'zeal' in its id; Zeal now appears in the post-fight Load reward pool (DIE_ACTION_EXCLUDED_MOD_IDS no longer includes it) alongside the other seven pool mods; zero console errors. Mechanically confirmed via jsdom load-and-drive of the actual index.html (see CONFIRMED WORKING) with zero JS errors, but not yet eyeballed in a real browser.

Fervour (BUILD 046) not yet browser-verified — needs a playtest pass confirming: force-load Fervour onto a face, trigger it, log confirms the trigger with no damage dealt; play Strike (normally 5) for 10, logged distinctly as doubled; play a second Strike same turn, also 10; end turn, play a Strike next turn for 5, confirming clearOn: 'turn' expired it; apply poison during a Fervour turn and let it tick — poison damage unaffected, not doubled. Also worth a human's attention: Fervour is currently NOT reward-pool-eligible (still in DIE_ACTION_EXCLUDED_MOD_IDS, unlike Zeal) since this prompt didn't ask for that change — a planning decision on whether it should be, deferred rather than assumed. Also flagged: the prompt's tagging list named "Ordain" as an existing mod-damage call site to tag, but Ordain isn't built yet — nothing to tag, noted rather than silently skipped. Mechanically confirmed via jsdom load-and-drive of the actual index.html (see CONFIRMED WORKING) with zero JS errors, but not yet eyeballed in a real browser.

Ordain (BUILD 047) not yet browser-verified — needs a playtest pass confirming: force-load Ordain onto a face at weight 1, force-trigger it, log confirms 10 damage and weight now 2; trigger again, weight now 3; Restart Fight, weight persists at 3; roll a full round of natural rolls afterward and confirm via the die-face list, State Inspector, or log that the elevated weight is genuinely reflected (die-face list itself has no numeric weight display yet — verified instead via rollDie()'s own pool and the State Inspector, both confirmed correct). Mechanically confirmed via jsdom load-and-drive of the actual index.html, including an explicit additive-stacking check against Strengthen (see CONFIRMED WORKING) with zero JS errors, but not yet eyeballed in a real browser. (The reward-pool-eligibility gap this note originally flagged is now resolved — see BUILD 048 below.)

With Zeal, Fervour, and Ordain all built, all three previously-deferred mods named in the code audit are now complete — the mod pool stands at eleven (Consecrate, Smite, Penance, Offering, Blight, Virulence, Sanctuary, Vigil, Zeal, Fervour, Ordain).

Reward pool eligibility check (BUILD 048) — CONFIRMED AND FIXED. Fervour and Ordain were both still excluded from the post-fight Load reward pool going into this check; both are now eligible, verified via 60 real reward-pool draws showing every non-anchor mod appearing at least once and Consecrate never appearing. Not yet eyeballed in a real browser, but mechanically closed.

Restart Fight loss-loop fix (BUILD 049) — RESOLVES a real, reported bug: after a loss, Restart Fight was stuck logging [INIT] fight reset immediately followed by [LOSS] player defeated forever, never reaching START_OF_TURN. Root cause: resetFight() never restored player.hp (the documented "hp carries over" behaviour), so a loss (hp <= 0 by definition) stayed at hp <= 0 through the reset, re-tripping the win/loss guard the instant run.status flipped back to 'active'. Fix: resetFight() now restores player.hp to maxHp. IMPORTANT — FLAGGED FOR PLANNING, not decided unilaterally: this changes previously-documented behaviour (CLAUDE.md's FIGHT RESET section, which Claude Code cannot edit, still reads "hp carries over," and CORE NUMBERS documents this under a stated Permadeath design). The fix matches this prompt's own explicit success criteria (70/70 after Restart Fight following either a win or a loss) and is the only way Restart Fight can keep functioning as the repeatable dev/testing reset it's been used as throughout this entire build, but it does mean a loss is no longer permanently unrecoverable via this button. Whether that's meant to be permanent (not just a dev-period convenience) needs a planning decision and a FIGHT RESET spec update from Fergus, not assumed here. Verified via jsdom load-and-drive of the actual index.html across both a forced real loss and a forced real win, each followed by Restart Fight: no [LOSS]/[WIN] line reappeared, player.hp read 70/70, phase correctly reached START_OF_TURN and paused there, and the game was confirmed genuinely playable afterward (Begin Roll -> CARD_PHASE with a fresh 5-card hand, End Turn enabled) — not just superficially reset. Zero JS errors. Not yet eyeballed in a real browser.

Card reward screen (BUILD 050), Stage 1.11 not yet browser-verified — needs a playtest pass confirming: win a fight, resolve the die action panel, and the card reward panel appears offering three cards (gold border, "Fight won — choose a card", matching the die action panel's look); picking a card logs the add and the deck total genuinely rises by one (see the CONFIRMED WORKING note on the literal "Deck" stat vs. total card count — they're not the same number at pick time, by pre-existing design unrelated to this build); starting the next fight shows the card in the deck, drawable and playable; Restart Fight leaves the added card in the permanent collection; skipping on a later fight logs the skip and changes nothing; the dev Skip to Card Reward button reaches the panel without playing a fight. Mechanically confirmed via jsdom load-and-drive of the actual index.html across both the dev-shortcut path and a real forced win (which correctly chains die action -> card reward, not before) with zero JS errors, but not yet eyeballed in a real browser — this is the second genuinely player-facing (non-dev-tool) screen in the project, so a human read of its usability/legibility matters more here than on dev-tool-only substages, same reasoning noted back at the original die action screen (BUILD 038). (The three TEST_A/B/C placeholder cards this note originally referenced were deleted in BUILD 051 and replaced with the real tier-1 pool — see below; the screen/flow behaviour this note describes is unchanged, only the offered cards are different now.)

Real card pool, tier 1 (BUILD 051), Stage 1.11 substage 2, not yet browser-verified — needs a playtest pass confirming: the reward screen after a win offers three of the eight real cards and never a TEST card; taking Censure puts it in the deck and it deals 14 on the next fight; Rebuke plays at 0 soul without being dimmed; Scripture/Communion/Censer/Judgement all behave per their stated numbers (see the Communion arithmetic note in CONFIRMED WORKING — net soul change is +1, not +2, because its own 1-soul cost is paid first); all eight survive Restart Fight once owned; zero console errors. Mechanically confirmed via jsdom load-and-drive of the actual index.html across five scenarios (see CONFIRMED WORKING) with zero JS errors, but not yet eyeballed in a real browser. NOTE: Communion's 1-soul cost was changed to 0 in BUILD 052 (see below) — the "net +1, not +2" arithmetic this note describes no longer applies; Communion is now a clean 0-cost +2 soul.

Communion fix and tier-3 state-read conditionals (BUILD 052), Stage 1.11 substage 3, not yet browser-verified — needs a playtest pass confirming: Communion shows a 0-cost badge, is playable at 0 soul, and takes soul from 3 to 5; against a clean 70 HP enemy, Purge deals 6 (enemy reads 64); with 4 poison stacks (via the DEV POISON bar), Purge deals 10; with 4 stacks, Reckoning deals 11, with 0 stacks it deals 3; playing Censer then Reckoning in the same turn deals 11, confirming Reckoning reads stacks applied earlier that turn; at intent 8 or lower Interdict grants 5 block, at intent 12 or higher it grants 10; every play of the four cards prints a log line naming the branch that fired; zero console errors. Mechanically confirmed via jsdom load-and-drive of the actual index.html across every named scenario (see CONFIRMED WORKING) with zero JS errors, but not yet eyeballed in a real browser. Retribution (the fourth flagged state-reading card) was deliberately not built this pass, per the prompt.

Mid-CARD_PHASE victory fix (BUILD 053), not yet browser-verified — needs a playtest pass confirming: against a 70 HP enemy reduced to 4 HP, playing a Strike for 5 immediately prints [WIN] enemy defeated, shows the VICTORY banner, and opens the die action panel, without End Turn being clicked; enemy HP reads 0 or -1, never a larger negative from cards played after the kill; remaining hand cards are visibly disabled and End Turn is inactive; exactly one [WIN] line appears in the log; choosing a die action leads to the card reward screen as before; winning normally via End Turn (kill landing in ENEMY_ACT_PHASE or on poison at START_OF_TURN) still behaves exactly as in BUILD 052. Mechanically confirmed via jsdom load-and-drive of the actual index.html (see CONFIRMED WORKING) with zero JS errors, but not yet eyeballed in a real browser. FLAGGED, not a gap introduced by this build: playCard() itself has no run.status guard — a card played after victory (only reachable by calling playCard() directly, bypassing the disabled hand-card button a real player is limited to) would still execute its own effect, though it cannot re-trigger [WIN] or reopen anything since runPhase()'s own guard is gated on run.status === 'active'. This mirrors the pre-existing loss halt, which also relies solely on button-disabling rather than a function-level status check, and the prompt did not ask for that to change.

Logging fix, not a new substage — card reward count and Nat 1/20 not-implemented lines (BUILD 054), not yet browser-verified — needs a playtest pass confirming: taking a card reward with a 13-card collection logs "deck now 14 cards" (matching the true ownedCards total, not the draw pile); skipping a reward logs the unchanged true total; forcing a player roll of face 20 prints a Nat 20 not-implemented line, and face 1 the equivalent Nat 1 line; forcing an enemy roll of face 1 prints the equivalent enemy-side line; enemy face 20's existing not-implemented line is unchanged; no face gains any actual effect (HP/state unaffected); zero console errors. Mechanically confirmed via jsdom load-and-drive of the actual index.html across all four log-line scenarios plus both card-reward count scenarios (see CONFIRMED WORKING) with zero JS errors, but not yet eyeballed in a real browser. FLAGGED: the two new player-side lines use the '[ROLL]' prefix rather than literally reusing '[ENEMY]' — see the INTERPRETATION FLAG in the CONFIRMED WORKING entry above for the reasoning; worth a planning confirmation that '[ROLL]' was the right read of "matching... in style and prefix" rather than introducing a new '[PLAYER]' tag.

Tier 4, Retribution (BUILD 055), Stage 1.11 substage 4, not yet browser-verified — needs a playtest pass confirming: with 0 block, Retribution deals 0; playing a Ward for 5 block then Retribution deals 5, logged uncapped; two Wards for 10 block then Retribution deals 10, logged uncapped; loading Sanctuary onto a face, forcing it, then playing Retribution at 16 block deals 12, logged with the capped variant naming the true 16; Retribution appears in a real reward draw with no exclusion list; zero console errors. Mechanically confirmed via jsdom load-and-drive of the actual index.html across all four damage scenarios plus the pool-eligibility check (see CONFIRMED WORKING) with zero JS errors, but not yet eyeballed in a real browser. With this build, all twelve tier 1-4 cards named across BUILD 051/052/055 are complete — the card pool stands at twelve (rebuke, censure, judgement, vestment, litany, scripture, communion, censer, purge, interdict, reckoning, retribution).

Roll-result exposure (BUILD 056), Stage 1.11 substage 5, not yet browser-verified — needs a playtest pass confirming: force a blank roll and the State Inspector's turn object reads rollOutcome: "blank"; force a mod roll (e.g. Consecrate or Sanctuary) and it reads "mod"; force face 20 or face 1 and it reads "nat_twenty" or "nat_one" respectively; load Sanctuary onto a face at weight 1, Strengthen it twice to weight 3, force that face, and rolledFaceWeight reads 3; advancing to the next turn updates both fields to the new turn's values rather than persisting the old ones; zero console errors. Implementation: gameState.turn gained rollOutcome (null | 'blank' | 'mod' | 'nat_twenty' | 'nat_one') and rolledFaceWeight (null | number), both set via updateTurn() inside resolvePlayerRoll() — the single function both the natural (nextPhase) and forced (forcePlayerRoll) roll paths already funnel through — immediately after the existing '[ROLL] face:' log line and before the NAT_TWENTY/NAT_ONE/MOD_TRIGGER/BLANK_ROLL branch that already existed there. Weight is read straight off the face object passed in, exactly as rollDie()'s own pool-building already does. Both fields are cleared back to null in START_OF_TURN immediately after the existing clearListeners('turn') call, so they reset alongside the existing turn-listener sweep. No card reads either field yet — this substage is exposure only; Orison, Covenant, and Rapture were not built. Mechanically confirmed via a Node/vm load-and-drive of the actual index.html's script (document/window stubbed, no jsdom dependency available) across blank, mod, nat_twenty, nat_one, and a weight-1-to-3 Strengthen-style scenario, plus a simulated START_OF_TURN confirming both fields clear while round advances — zero JS errors — but not yet eyeballed in a real browser. Build stamp now STAGE 1.11 / BUILD 056.

Covenant (BUILD 057), Stage 1.11 substage 6, not yet browser-verified — needs a playtest pass confirming: on a turn where the rolled face has weight 1 (any untouched face), Covenant deals 5; with a face at weight 3 (double-Strengthened), Covenant deals 11; Covenant deals identically whether the rolled face resolved as blank or as a mod trigger — only weight matters, not outcome type; Covenant appears in a real reward draw with no exclusion list; zero console errors. Implementation: config.cards['covenant'] (1 soul, attack) reads gameState.turn.rolledFaceWeight directly at play time — never re-derives weight from gameState.die.faces — computes calculateDamage(2 + 3 * weight, 'attack') exactly like every other damage card, and logs [CARD] covenant: N damage (face weight M). Registered into config.cardPool with no exclusion list, same pattern as the other twelve. Added a CARD_EFFECT_TEXT entry ('2 damage + 3 per face weight') for display consistency; CARD_FX_TYPE was not touched, matching the other tier 2+ cards which also have no fx entry. BUILD 056's rolledFaceWeight/resolvePlayerRoll exposure code was not touched. Mechanically confirmed via a Node/vm load-and-drive of the actual index.html's script (document/window stubbed, no jsdom dependency available): weight-1 play deals exactly 5, weight-3 play deals exactly 11, and a full resolvePlayerRoll()-then-play pass through both a blank face and a mod-loaded face (each forced to weight 2) each deal exactly 8, confirming outcome type has no bearing on the card — zero JS errors — but not yet eyeballed in a real browser. Build stamp now STAGE 1.11 / BUILD 057.

Rapture (BUILD 058), Stage 1.11 substage 7 — the last card in the pool, not yet browser-verified — needs a playtest pass confirming: on a turn with no mod trigger, Rapture costs 2 soul and deals 12; on a turn where a mod face was rolled (e.g. force Consecrate or Sanctuary), Rapture is playable at 0 soul remaining and still deals 12; playing it on a Fervour turn deals 24 for 0 soul; the hand-card cost badge itself reads 0 on a mod turn, not just the play logic; Rapture appears in a real reward draw with no exclusion list; zero console errors. Implementation: config.cards['rapture'] (attack, base soulCost 2) reads gameState.turn.rollOutcome directly at play time (never re-derived from anywhere else) via a new optional card.getCost(gameState) function, returning 0 when rollOutcome === 'mod' and 2 otherwise. A new shared getCardCost(card) helper (returns card.getCost(gameState) if present, else card.soulCost) was introduced and wired into the two places that previously read card.soulCost directly for gameplay purposes: playCard()'s soul-affordability gate/spend/log, and renderCardButtons()'s affordability check and cost badge — this is the mechanism that makes the badge itself reflect the reduced cost, not just the play logic. All thirteen existing cards are unaffected: none define getCost, so getCardCost() falls through to their unchanged static soulCost. Damage always routes through calculateDamage(12, 'attack') regardless of which cost branch applied, so it composes correctly with any active DAMAGE_MULTIPLIER listener (e.g. Fervour). Effect logs [CARD] rapture: 12 damage (2 soul) on a normal turn or [CARD] rapture: 12 damage (free, mod triggered) on a mod turn, in addition to the pre-existing generic [CARD] played Rapture (cost N) line playCard() already logs for every card. Registered into config.cardPool with no exclusion list, same pattern as the other thirteen. The card reward screen's own soulCost display (line ~1600, cardRewardOptions loop) was deliberately left reading the static base soulCost, per the instruction not to touch that screen. calculateDamage(), gameState.turn.rollOutcome, and the BUILD 056 exposure code were not touched. Mechanically confirmed via a Node/vm load-and-drive of the actual index.html's script (document/window stubbed, no jsdom dependency available): a blank-turn play costs 2 soul and deals 12; a mod-turn play at 0 soul succeeds and deals 12; getCardCost() directly reports 0 on a mod turn and 2 otherwise (covering the badge path); and a mod-turn play with a registered DAMAGE_MULTIPLIER of 2 (simulating Fervour) deals 24 for 0 soul — zero JS errors — but not yet eyeballed in a real browser. Build stamp now STAGE 1.11 / BUILD 058. With this build, the full fourteen-card pool named across the tier 1-5 substages (BUILD 051/052/055/057/058) is complete.

Orison (BUILD 059), Stage 1.11 substage 8 — the last card in the fifteen-card pool, not yet browser-verified — needs a playtest pass confirming: force a mod face (Consecrate or Sanctuary) and Orison deals exactly 5, logged with the base variant; force a blank face and Orison deals exactly 9, logged with the blank variant; force face 20 or face 1 and Orison deals 5, confirming Nat rolls are not treated as blank; Orison appears in a real reward draw with no exclusion list; zero console errors. Implementation: config.cards['orison'] (1 soul, attack) reads gameState.turn.rollOutcome directly at play time — never re-derives blankness from the die, the face, or the blank passive listener — dealing calculateDamage(9, 'attack') when rollOutcome === 'blank' and calculateDamage(5, 'attack') otherwise. This is the deciding detail: NAT_ONE/NAT_TWENTY faces carry no loaded mod, so any version inferring blankness from "face.modId === null" would incorrectly pay Orison's 9-damage branch on a Nat roll; reading rollOutcome directly (set by BUILD 056 to 'nat_one'/'nat_twenty', never 'blank', on those faces) is what keeps them on the 5-damage branch. Logs [CARD] orison: 9 damage, blank roll on a blank turn and [CARD] orison: 5 damage otherwise. Registered into config.cardPool with no exclusion list, same pattern as the other fourteen. gameState.turn.rollOutcome, the BUILD 056 exposure code, the blank passive (still its own separate 2-block-generating listener, untouched), getCardCost(), calculateDamage() itself, the mod pool, the die action panel, the card reward screen, and all fourteen existing cards were not touched. Mechanically confirmed via a Node/vm load-and-drive of the actual index.html's script (document/window stubbed, no jsdom dependency available), driving resolvePlayerRoll() through a real mod-loaded face, a real blank face, face 20, and face 1 in turn and playing Orison after each: mod → 5, blank → 9, Nat 20 → 5, Nat 1 → 5 — zero JS errors — but not yet eyeballed in a real browser. Build stamp now STAGE 1.11 / BUILD 059. With this build, the full fifteen-card pool named across the tier 1-5 substages (BUILD 051/052/055/057/058/059) is complete — Orison, Covenant, and Rapture (the three die-interaction cards BUILD 056's roll exposure exists to serve) are all now built.

Stage 1.12 audit remediation — classId (BUILD 060). gameState.player.classId changed from null to 'ordained' as the schema's initial value (not a runtime mutation, no helper needed). The two class lookup sites that previously used the hardcoded literal config.classes['ordained'] — the onBlankRoll listener registration and the startingDeck read in init() — now read config.classes[gameState.player.classId]. The prompt's own instructions also asked, contradictorily, for the class definition assignment itself (gameState.config.classes['ordained'] = {...}) to be keyed off classId too, so that a grep for config.classes['ordained'] would return zero matches; that edit was made as well, converting the definition assignment to gameState.config.classes[gameState.player.classId] = {...} (corrected in BUILD 061, see below). onNatTwenty/onNatOne remain unregistered empty stubs, untouched; card.classRestriction remains declared and unread, untouched. Behaviour unchanged: classId equals 'ordained' at object-construction time, before init() ever runs, so every lookup resolves identically to the old hardcoded literal — blank passive still grants 2 block, starting deck still 5 Strike/4 Ward/1 Rite. Build stamp now STAGE 1.12 / BUILD 060.

One-line correction to BUILD 060 (BUILD 061). BUILD 060's own prompt contained an internal contradiction, flagged rather than silently resolved: keying the class *definition* assignment off gameState.player.classId is architecturally backwards — definitions should be keyed by the class's own id, with player.classId selecting among them, not the other way round; a second class defined the same way would silently overwrite the first under whatever classId happened to hold at construction time. The definition assignment is reverted to the literal, gameState.config.classes['ordained'] = {...}; nothing inside the object changed. Both lookup sites (the onBlankRoll registration and the startingDeck read) are untouched, still keyed off gameState.player.classId exactly as BUILD 060 left them. Grepping index.html for config.classes['ordained'] now returns exactly one match — the definition assignment itself. Behaviour unchanged from BUILD 060: classId still 'ordained' on fresh load and after Restart Fight, blank passive still 2 block, starting deck still 5 Strike/4 Ward/1 Rite. Build stamp now STAGE 1.12 / BUILD 061.

Stage 1.12 audit remediation — Zeal's counter (BUILD 062). Closes the one Law 3 exception the audit found: Zeal's effect() used to write config.mods['zeal'].accumulatedBonus directly, bypassing all five state helpers, deliberately (registerListener() has no dedup, so a per-trigger registration would have accumulated duplicates forever) but in contradiction of STATE HELPERS' "config is set once at init and never changed." The counter now lives on the triggering face's own new optional modData field in gameState.die.faces instead — the same kind of per-instance persistent value Ordain's weight already is — read and written through the identical clone-then-write updateDie() pattern Ordain already uses (slice the faces array, Object.assign a new object for the one changed entry with modData: { accumulatedBonus: N }, write the whole array back). A face with no modData (never triggered Zeal) is treated as bonus 0; modData was deliberately not pre-added to every face at init, only ever written the first time a face's mod actually triggers. Because the value is per-face rather than per-mod, Zeal loaded onto two different faces accrues independently on each. accumulatedBonus was deleted from config.mods['zeal'] entirely — grep-confirmed it no longer appears as a field on that object, only inside face.modData now; config.mods['zeal'] itself is just {id, name, effect} again, identical in shape to every other mod. effect() gained a data parameter (reading data.faceNumber, exactly as Ordain's effect(data) already does) since the MOD_TRIGGER dispatcher already forwards {modId, faceNumber} to every mod's effect() — no dispatcher change needed. Damage progression is unchanged (10, 14, 18, 22, ... — base 10 plus the pre-increment bonus, still tagged calculateDamage(N, 'attack')), and resetFight() still never touches gameState.die, so the bonus still survives Restart Fight exactly as the old config field did. Log line now shows both numbers explicitly: [MOD] zeal: N damage (bonus +B, face F now +B+4) — B is the bonus this trigger was computed from, the incremented value is the one that face carries into its next trigger. registerListener(), the listener system, Ordain's own weight mutation (confirmed byte-for-byte unchanged by direct re-read), calculateDamage(), every card, and the other nine mods were not touched. Build stamp now STAGE 1.12 / BUILD 062.

Stage 1.12 audit remediation — START_OF_TURN lethal-mid-phase halts (BUILD 063). Closes the last two of the audit's lethal-mid-phase findings, both in START_OF_TURN, same shape as the BUILD 053 CARD_PHASE fix: BUILD 013's guard only fires at the top of runPhase() on the next phase-transition attempt, so a poison tick that drops hp to 0 mid-body previously let the rest of START_OF_TURN's own logic keep running for an already-decided fight. Two new in-place checks added, no new state: immediately after the player poison tick, if gameState.player.hp <= 0, the code calls runPhase(phase) — re-entering the function fires the exact same top-of-function guard that CHECK_WIN_LOSS already relies on (updateRun({status:'loss'}), log('[LOSS] player defeated'), return — the identical BUILD 053 pattern, no second loss path written) — then the outer call itself returns immediately, so nothing past that point in START_OF_TURN executes. Checked before the enemy tick, per spec: a dead player never reaches the enemy tick at all. Symmetrically, immediately after the enemy poison tick, if gameState.enemy.hp <= 0, the same runPhase(phase)-then-return pattern fires the guard's win branch (updateRun({status:'win'}), log('[WIN] enemy defeated'), openDieActionScreen(), return) before block clear/soul reset/clearListeners('turn')/roll-outcome clear/drawCards(5) can run. Exactly-once firing needed no new guard, per spec: runPhase()'s own top check is gated on run.status === 'active', so the moment either branch flips status away from 'active', the recursive call's own guard is what actually fires the log/status change, and the outer call's return is what stops the rest of the body — nothing here adds a second status flag. Verified via direct re-read: a poison tick that does not cross 0 falls through both new checks untouched and the rest of START_OF_TURN (block clear, soul reset 3, clearListeners('turn'), drawCards(5)) still runs exactly as before; a lethal enemy tick logs the poison line then exactly one [WIN] line with no block-clear/soul-reset/draw lines after it; a lethal player tick logs the poison line then exactly one [LOSS] line with no enemy-tick or draw line after it. The poison decay model itself, the damage amounts, PHASE_ORDER, calculateDamage(), every card, and every mod were not touched — re-verified by re-reading the modified section after editing. Build stamp now STAGE 1.12 / BUILD 063.

Stage 1.12 audit remediation — shared damage/block helpers (BUILD 064). Closes the last item of the four-part audit remediation queue. Added two new shared dispatch functions, placed immediately after generateBlock(): dealDamage(amount, sourceType, sourceId) runs the exact three-step sequence every attack card/mod effect() previously repeated inline — calculateDamage(amount, sourceType), apply the result to enemy.hp via updateEnemy(), fire ON_DAMAGE_DEALT with { amount, source: sourceId } — and returns the final computed damage so callers that log it still can; dealBlock(amount, sourceId) is the equivalent for generateBlock()/player.block/ON_BLOCK_GENERATED. sourceType has no default value on dealDamage, per the prompt — every call must name its source explicitly. All 16 actual calculateDamage()-and-apply-to-enemy call sites (strike, ward→n/a, rite, rebuke, censure, judgement, vestment→n/a, litany, purge, interdict→n/a, reckoning, retribution, covenant, rapture, orison, smite, penance, zeal, ordain — 12 cards + 4 mods, one more than the audit's own count of 15, which undercounted by one) and all 10 actual generateBlock()-and-apply-to-player call sites (ward, rite, vestment, litany, interdict, the blank passive, consecrate's per-card listener, penance, sanctuary, vigil — two more than the audit's own count of 8, which missed Consecrate's ON_CARD_PLAY listener and Vigil's END_PLAYER_TURN listener, both following the identical pattern) were replaced with single dealDamage()/dealBlock() calls. Every site's own conditional logic, branch logging, and every non-boilerplate line (poison-stack reads, intent checks, weight mutation, modData mutation, flashElement feedback) was left in place around the new call — only the three-line damage/block boilerplate itself was collapsed. ENEMY_ACT_PHASE's calculateDamage(rawDamage) — the audit's one untagged call site — now reads calculateDamage(rawDamage, 'enemy_attack'); it keeps its own inline call rather than routing through dealDamage(), since dealDamage() always applies to enemy.hp and this damages the player instead. 'enemy_attack' is deliberately distinct from 'attack' so it stays outside Fervour's sourceType === 'attack' check — verified directly, enemy damage is not doubled by a player-side Fervour trigger. Two call sites were deliberately NOT touched despite the success criteria's own grep phrasing implying otherwise: the player and enemy poison ticks in START_OF_TURN still call calculateDamage(gameState.X.poisonStacks, 'poison') directly, since the prompt's own explicit constraint list said not to touch the poison decay model, and poison's shape (stack decrement bundled into the same state-helper call, no ON_DAMAGE_DEALT fire, applies to either side) doesn't fit dealDamage()'s signature anyway — flagged as a contradiction resolved toward the explicit "do not touch" instruction, the same resolution BUILD 061 used for BUILD 060's classId contradiction. calculateDamage() and generateBlock() themselves are untouched, confirmed byte-for-byte identical by direct re-read. Verified via a Node vm load-and-drive of the actual index.html's script (document/window stubbed): Strike deals 5, Censure 14, Judgement 20, Ward grants 5 block, Vestment 13, Smite 16, Sanctuary 16; Fervour still doubles a 5-damage Strike to 10 and does NOT double a 4-stack poison tick (stays 4) or a 10-intent enemy attack (stays 10); Zeal on one face across four triggers (including a resetFight() in between) reads 10, 14, 18, 22, 26, confirming both the progression and BUILD 062's persistence are intact; Ordain on a different face still deals 10 and increments only its own face's weight, with no cross-talk into Zeal's modData or Zeal's weight; Retribution still deals 12 (capped, block 20) and 5 (uncapped, block 5) — all as PASS with zero JS errors. Build stamp now STAGE 1.12 / BUILD 064. With this build, all four items of the BUILD 059 audit's remediation queue (classId, Zeal's counter, the START_OF_TURN lethal-mid-phase halts, and these shared helpers) are closed.

Correction to BUILD 064 — dealDamage() gained an explicit target parameter, same build stamp (a follow-up prompt asked specifically for dealDamage(target, amount, sourceType, sourceId) rather than the enemy-only shape above, the same kind of same-numbered-build correction BUILD 061 made to BUILD 060). Signature is now dealDamage(target, amount, sourceType, sourceId, fireListener): applies to enemy.hp when target === 'enemy', player.hp otherwise; a new fireListener parameter (default true) exists solely so ENEMY_ACT_PHASE can opt out of firing ON_DAMAGE_DEALT, which that site never fired before this build and the prompt's own constraint said not to start. All 16 existing card/mod call sites were updated to pass 'enemy' as the new first argument — no other argument or behaviour changed at any of them, confirmed by direct re-read. ENEMY_ACT_PHASE now routes through dealDamage('player', rawDamage, 'enemy_attack', null, false) instead of calling calculateDamage() inline; its own damage===0-vs-else branch (the "fully blocked" vs "dealt" log lines, and the fact that a 0-damage hit does no meaningful state change) stays exactly as it was, since dealDamage() applies its result unconditionally regardless of whether that result is 0 — this is the call site's own decision logic, not boilerplate, per the prompt's own rule that per-site decisions stay at the call site. Re-verified via a fresh Node vm load-and-drive of the actual index.html's script: Strike 5, Ward 5 block, blank passive 2 block, Smite 16, Penance 8 damage/8 block, Fervour-doubled Strike 10, a same-turn enemy_attack under Fervour still landing undoubled at intent-minus-block (10, confirmed not 20), Retribution capped at 12 (block 20) and uncapped at 5 (block 5) — the earlier capped-Retribution run showed 24 instead of 12 purely because that particular test script had not cleared a still-active Fervour listener from an earlier assertion in the same run, not a code defect; re-run with clearListeners('turn') between assertions confirmed 12 exactly. Both poison ticks re-confirmed byte-for-byte untouched and still decaying correctly (4→3, 3→2 stacks, correct damage each). grep confirms calculateDamage() is now called from exactly three places: inside dealDamage() itself, and the two untouched poison ticks — the old three-line inline sequence appears nowhere else. Zero JS errors across all checks. Not yet eyeballed in a real browser. Build stamp remains STAGE 1.12 / BUILD 064.

Stage 1.13 — die face weight display and Strengthen rule change (BUILD 065), per the 24 August 2026 NAT 20 AND NAT 1 — THE ORDAINED decision. renderDieList() now wraps each row's mod span in a new .die-mod-wrap flex container (justify-content: space-between) and, only when face.weight > 1, appends a sibling .die-weight span reading '×' + face.weight — a face at weight 1 renders identically to BUILD 064, since the span is never created. Weight is read straight off the same face object the row already renders from (gameState.die.faces / gameState.enemy.die.faces via the existing forceRollFn-parameterised renderDieList), so Ordain, Strengthen, or any future weight source shows on the very next refreshInspector() with no new state added. .die-weight is folded into the existing nat/player-mod/enemy-mod colour rules alongside .die-mod so the badge always matches its row's colour, including on face 20 (nat colour). The Strengthen face picker's eligible-face filter (in renderDieActionPanel's 'strengthen_pick_face' branch) changed from f.modId !== null && f.number !== 1 && f.number !== 20 to an explicit three-branch check: face 1 always excluded, face 20 always included, every other face included only if modId !== null — so the eligible set is now loaded faces plus face 20, face 1 still untargetable. dieActionPickStrengthenFace() and its log line ('[DIE ACTION] strengthened face N to weight W') were already mod-name-free — untouched, no no-mod special case needed there. The button label (modDisplayName(f.modId) + ' (weight ' + f.weight + ')') was also already safe for face 20 without any change, since modDisplayName() has handled the 'NAT_TWENTY' id (returning 'NAT 20') as its own branch, before ever touching config.mods, since BUILD 004 — confirmed by direct re-read, no other code path in index.html assumes a Strengthen target carries a mod (Covenant already reads gameState.turn.rolledFaceWeight directly, independent of modId, per its BUILD 057 note). Build stamp now STAGE 1.13 / BUILD 065. Verified via a real headless-browser (Playwright/Chromium) drive of the actual index.html: fresh load shows zero .die-weight badges and zero row/wrap overflow at 1600x1080; dev-loading Ordain onto face 7 and force-triggering it renders face 7's row as ×2; Skip to Die Action → Strengthen offers exactly Face 7 (Ordain), Face 10 (Consecrate, the starting face), and Face 20 (NAT 20) — Face 1 is not offered; strengthening face 20 twice renders ×2 then ×3; acquiring Covenant via Skip to Card Reward, strengthening a loaded face to weight 3, and playing Covenant against it deals exactly 11 damage (2 + 3×3); forcing a natural face-20 roll at weight 3 still logs only '[ROLL] Nat 20 not yet implemented' with no other behaviour, and a normal blank-face roll still generates its 2 block unchanged — zero console errors throughout. A stress case (longest mod name "Consecrate" loaded at weight 20, face 20 also at weight 20) confirmed no wrapping, clipping, or column shift: every row's face-btn and die-mod-wrap left edge stayed pixel-identical across all twenty rows, and no row or wrap element's scrollWidth exceeded its clientWidth. Not yet eyeballed by a human in a real windowed browser. No roll/weight mathematics, mod/card logic, poison ticks, dealDamage()/dealBlock(), or the onNatTwenty/onNatOne stubs were touched.

Stage 1.14 — the Nat 20 mechanic (BUILD 066), per the 24 August 2026 NAT 20 AND NAT 1 — THE ORDAINED decision. The Ordained's onNatTwenty stub is now real: filters gameState.die.faces for loaded faces (modId !== null, excluding the NAT_ONE/NAT_TWENTY stub ids), logs one announcement line ('[ROLL] Nat 20: N loaded face(s) trigger(s)'), then dispatches each through callListeners('MOD_TRIGGER', { modId: f.modId, faceNumber: f.number }) — the exact same call resolvePlayerRoll already makes for a single rolled mod face, so no second copy of the trigger logic exists anywhere. Ascending order falls out for free: gameState.die.faces is invariantly stored in ascending face-number order (DIE FACE OBJECT STRUCTURE), so Array.filter() preserves it with no explicit sort needed. Face 1 never participates (excluded by the modId check, it's never 'loaded'); face 20 never triggers itself (it's what's filtered against, not what's dispatched) — it is the cause, not a participant. Zero loaded faces logs the announcement ('0 loaded faces trigger') and the forEach body simply never runs — no blank passive, no block floor, confirmed already guaranteed by resolvePlayerRoll's existing modId-based branch structure (NAT_TWENTY never falls into the BLANK_ROLL branch), not something this build added. Registered as a permanent NAT_TWENTY listener ('ordained_nat_twenty_passive') in init(), mirroring onBlankRoll's own permanent BLANK_ROLL registration — registered once, not per-turn, so repeated forced Nat 20s never accumulate duplicate listeners. resolvePlayerRoll's NAT_TWENTY branch itself is unchanged except the old stub log line ('[ROLL] Nat 20 not yet implemented') is gone — callListeners('NAT_TWENTY', {}) is the same call BUILD 054 wrote, only what it now reaches has changed. onNatOne and its own stub log line are untouched, confirmed by direct re-read — Penitence is a separate build. Rapture's cost/free condition (read the existing implementation first, per the prompt): it read gameState.turn.rollOutcome === 'mod' directly, which cannot distinguish a Nat 20 with loaded faces (rollOutcome reads 'nat_twenty', never 'mod') from a blank-die Nat 20 (also 'nat_twenty') or a plain Nat 1/blank roll. Fixed with the smallest change that isn't a parallel state field: a new gameState.turn.modTriggeredThisTurn boolean, set true inside the existing permanent 'mod_dispatch' MOD_TRIGGER listener (the one dispatch path both a normal single-face mod roll and Nat 20's loop both call through) whenever a real mod effect actually runs, declared false in the STATE SCHEMA turn object and reset to false every START_OF_TURN alongside rollOutcome/rolledFaceWeight (same updateTurn() call, same clear). Rapture's getCost() and effect() both switched from rollOutcome === 'mod' to reading this flag directly — it is not a duplicate of what rollOutcome already meant, it is the same "did a mod trigger" condition rollOutcome === 'mod' used to carry, made explicit so it also survives a Nat 20 turn. Covenant (reads rolledFaceWeight, untouched) and Orison (reads rollOutcome === 'blank', untouched and still correctly false on any Nat roll) needed no change — grep-confirmed rollOutcome === 'mod' now appears nowhere in index.html outside this Rapture comment block. Build stamp now STAGE 1.14 / BUILD 066. Verified via a real headless-browser (Playwright/Chromium) drive of the actual index.html, both as isolated state-injection checks and a full BUILD 065 regression re-run: with Ordain on face 2, Fervour on face 4, and Consecrate on the starting face 10, a forced face 20 logs '[ROLL] Nat 20: 3 loaded faces trigger' then Ordain's line (10 damage, face 2 weight 1→2), Fervour's line (doubler registered), and Consecrate's line (+2 soul) in that ascending order, with enemy HP, player soul, and face 2's weight all reflecting every effect having actually applied; clearing the die to zero loaded faces and forcing face 20 logs only the announcement ('0 loaded faces trigger') with player block, soul, and enemy HP all unchanged; Rapture costs 0 on a loaded-die Nat 20 and 2 (full price) on a blank-die Nat 20, confirmed via getCardCost(); forcing face 20 twice in a row on a die with one Ordain face produces weight 1→2 then 2→3 (no doubled trigger either time) with the NAT_TWENTY listener registry staying at exactly one entry across both forces (no accumulation); the full BUILD 065 weight-display/Strengthen/Covenant regression suite still passes unchanged, with the sole expected difference being that a natural face-20 roll now actually fires loaded mods instead of the old stub line — zero console errors across every check. Not yet eyeballed by a human in a real windowed browser. The weight display, the Strengthen filter, dealDamage()/dealBlock(), the poison ticks, the card pool's own per-card logic (beyond Rapture's condition source), and every mod's individual effect() were not touched.

Stage 1.15 — Nat 1's Penitence (BUILD 067), per the 24 August 2026 NAT 20 AND NAT 1 — THE ORDAINED decision (Penitence itself decided 21 August 2026). The Ordained's onNatOne stub is now real: rolling face 1 sets a new gameState.player.penitenceActive flag (declared false in STATE SCHEMA, alongside poisonStacks — the other fight-scoped player debuff — not on gameState.turn, per the prompt's explicit instruction) and logs its onset; rolling face 1 again while it's already true changes nothing, logging '[ROLL] Penitence already active' instead — confirmed no double-flip, no stacking. Registered as a permanent NAT_ONE listener ('ordained_nat_one_passive') in init(), mirroring BUILD 066's NAT_TWENTY registration exactly — registered once, not per-turn, so repeated forced Nat 1s never accumulate duplicate listeners. resolvePlayerRoll's NAT_ONE branch is unchanged apart from the old stub log line ('[ROLL] Nat 1 not yet implemented') being gone — callListeners('NAT_ONE', {}) is the same call BUILD 054 wrote, only what it now reaches has changed, same shape as BUILD 066's NAT_TWENTY change. The actual 1-soul loss is a separate step, deliberately not inside onNatOne itself: a new block in START_OF_TURN, placed immediately after the existing 'soul reset to maxSoul' line (so a Penitence turn begins at maxSoul - 1, e.g. 2, never negative — Math.max(0, soul - 1) guards the floor), that fires only when penitenceActive is true and logs '[START] Penitence: 1 soul lost, now N' on its own line every turn it applies. resetFight() now clears penitenceActive back to false in the same updatePlayer() call that already clears poisonStacks, so Restart Fight ends Penitence exactly like it ends poison. The Active debuffs panel (playerDebuffsValue, in renderStats()) previously only ever had one possible entry (poison), shown via a single ternary — extended to a small array + join(', ') so Penitence can appear alongside poison, reusing the join(', ') shape enemyBuffsValue already uses for its own multi-entry active-effects list rather than inventing a new display format. Build stamp now STAGE 1.15 / BUILD 067. Verified via a real headless-browser (Playwright/Chromium) drive of the actual index.html, driving the real phase machine (nextPhase()/forcePlayerRoll(), no shortcuts): forcing face 1 applies Penitence with its onset line and leaves the current turn's soul unchanged (3, since that turn's reset already happened); the next START_OF_TURN logs 'soul reset to 3' then 'Penitence: 1 soul lost, now 2' as its own line, landing at 2; forcing face 1 again mid-Penitence logs only 'Penitence already active' with soul unchanged, and the following turn still loses exactly 1 soul (3→2 again, not 2→0) — confirmed no stacking across two consecutive full turns; Restart Fight resets penitenceActive to false and the next turn's soul reset lands at 3 with no loss; a Nat 20 turn with Penitence active still fires all loaded faces normally (Ordain + Consecrate both triggered, ascending order, exactly as BUILD 066 verified) and Consecrate's +2 soul composes with a same-turn Penitence tick as ordinary arithmetic (3 → 2 after the tick → 4 after Consecrate, i.e. plain addition, no special-casing anywhere); the Active debuffs panel reads 'penitence' once active — zero console errors across every check. Not yet eyeballed by a human in a real windowed browser. onNatTwenty, the BUILD 066 dispatch, modTriggeredThisTurn, the weight display, the Strengthen filter, dealDamage()/dealBlock(), the poison ticks, and every mod/card's own logic were not touched.

Human browser confirmation, 24 August 2026 — Fergus confirmed Stages 1.13 (die face weight display and the Strengthen rule change, BUILD 065), 1.14 (the Nat 20 mechanic, BUILD 066), and 1.15 (Nat 1's Penitence, BUILD 067) directly in a real windowed browser. All three moved from headless-only to fully human-verified; see CURRENT SUBSTAGE.

Stage 2.0 — the run scaffold (BUILD 068). The first system above a single fight: a seven-slot act in two lanes (a shared start, upper: fight/elite/rite, lower: fight/rite/fight, rejoining at a shared boss), a map screen shown between slots, and the lane-lock/win/loss rules the prompt specified. gameState.run gained four new fields (screen: 'map'|'fight', outcome: 'active'|'won'|'lost', lane: null|'upper'|'lower', act: built by buildAct()) alongside a fifth, currentSlot (null at the fork | {lane,index} | 'boss') — run.status itself is untouched and stays exactly what it always was, the CURRENT FIGHT's own 'active'/'win'/'loss', still driving the existing per-fight VICTORY/DEFEAT banner (renderResultBanner(), byte-for-byte unchanged) and the existing dev-button gating; outcome is the new, separate WHOLE-RUN flag. gameState.enemy gained intentMin/intentMax (default 6/18, replacing a hardcoded Math.floor(Math.random()*13)+6 in START_OF_TURN with the exact same range read from these fields) so a slot's fight can carry its own band. Slot dispatch is a lookup table, SLOT_HANDLERS = {fight, rite}, keyed by slot.type and read by enterSlot() — adding a further slot type later means one more key, never a rewritten branch, per the prompt's explicit instruction. buildAct() hardcodes the prompt's own numbers directly (not a formula): upper fight 70 HP/6-18 intent, elite 100/12-22, rite; lower fight 70/6-18, rite, fight 85/6-18; boss 150/16-28 — the two normal-fight endpoints (70, 85) are exactly the "rising to 85" the prompt named, and every fight-type slot keeps the existing 6-18 band unless the prompt named a wider one, closing the recorded gap that intent never scaled while HP did. init() no longer builds the die/enemy-die/starting-deck inline and ends by jumping into a fight (startFreshTurnPaused()) — that inline setup is now buildFreshPlayerDieFaces()/buildEnemyDieFaces() (extracted verbatim, byte-identical output), and init() ends by calling the new startNewRun() instead, which performs that setup, builds a fresh act, and lands on the map screen — a fresh session no longer starts mid-combat. startNewRun() is also what #startGameBtn now calls: that button is relabeled "New Run" and is the only player-facing way to recover from a run-ending win or loss (not gated on outcome — always available, same always-enabled UX the old Restart Fight had) or to abandon an in-progress run; it fully rebuilds HP, the die, and the deck from scratch per the prompt's explicit success criterion. The old Restart Fight behaviour (full HP heal, single-fight reset) survives verbatim as a new dev-only #devRestartFightBtn inside #devStepControl, disabled outside gameState.run.screen === 'fight' — the prompt's explicit instruction that this control must never be a player-facing way to undo a loss. resetFight() (now dev-only) and the new shared clearFightScopedState() (used by both beginFightFromSlot() and advanceRun()) split what resetFight() used to do in one block: clearFightScopedState() resets everything that must reset between fights exactly as it always did (block, soul, poisonStacks, penitenceActive, deck reshuffled from the persistent ownedCards collection, turn-scoped listeners) but never touches HP, the die, or ownedCards itself; resetFight() layers a full HP heal on top of that shared reset, HP restoration being unique to the dev tool. beginFightFromSlot(slot) sets gameState.enemy's hp/maxHp/intentMin/intentMax from the slot's own data, calls clearFightScopedState(), switches screen to 'fight', and starts the turn exactly as init() used to. advanceRun() marks the current slot completed, computes the next currentSlot (next lane index, or 'boss' after the third), returns to the map, and calls clearFightScopedState() again (deliberately redundant with beginFightFromSlot()'s own call — both idempotent, no call-order dependency) — it is never called for a boss win, which the runPhase() win branch short-circuits into outcome:'won' directly, skipping the reward flow entirely (there's no next fight to reward for) and leaving the frozen battlefield up under the existing VICTORY banner. The player-death branch in that same runPhase() guard now also sets outcome:'lost' alongside the unchanged status:'loss' — this is what makes death end the whole run, not just the current fight; both lethal-mid-phase re-entry paths BUILD 063 already wired (the player and enemy poison ticks) funnel through this same single top-of-function guard, so no other lethal path needed touching. Rite slots reuse the die action panel completely unchanged, per the prompt's explicit instruction: openDieActionScreen(origin) gained an optional origin parameter ('reward', the default, preserves every existing call site's behaviour byte-for-byte; 'rite' is new) read once by closeDieActionScreen(), which now branches — 'rite' calls advanceRun() directly, skipping the card reward screen a rite never offers; anything else keeps the exact BUILD 050 behaviour of chaining into openCardRewardScreen(). closeCardRewardScreen() now always calls advanceRun() at the end, since that screen is only ever reached via the fight-win reward flow — never the rite path. The rite screen itself (riteStep, openRiteScreen()/closeRiteScreen()/riteChooseHeal()/riteChooseDieAction()/renderRiteScreen()) is a new small panel following the exact same null/'choose' step shape and .die-action-title/.die-action-row CSS reuse the card reward screen already established; riteChooseHeal() heals Math.min(maxHp, hp+20) — exactly 20, capped at 70, never both a heal and a die action since they're mutually exclusive button choices in the same panel. The map screen (renderMapScreen(), #mapScreen) renders both lanes full-length every time — a fork click both picks the lane and enters that lane's first slot in one click (there is no separate confirm-the-lane step, since the two lanes' first slots are the only choice a fork click could mean); every other lane's nodes render disabled and without a click handler the instant a lane is chosen, so the other lane is doubly unreachable, not just visually inert. The boss node and its preview (HP/intent text plus the actual enemy die reused via renderDieList() with a null forceRollFn, so it renders identically to a real enemy die but isn't clickable) read only gameState.run.act.boss's own static config — never gameState.enemy's live in-fight values — so they're legible from the very first map render and never change regardless of what happens in the fights leading up to the boss. #fightScreen is a new wrapper around the four elements (enemy panel, mid-strip, player panel, hand row) that used to be direct .left-col flex children; it carries .left-col's own display:flex/flex-direction:column/gap:6px so every child's existing flex sizing (the BUILD 040/041 fixes) sees the identical flex-item context, just one level deeper — #dieActionPanel/#cardRewardPanel/#riteScreenPanel stay outside it as direct .left-col siblings, since a rite's die action panel needs to render correctly reached from the map, not just from a fight. refreshInspector() toggles #fightScreen/#mapScreen display based on run.screen, and the dev step-control buttons (Next Phase, Begin Roll) are now additionally gated on screen==='fight' so they can't be misused from the map; endTurnBtn and #startGameBtn's disabled checks were extended to also read riteStep, matching how they already read dieActionStep/cardRewardStep. Build stamp now STAGE 2.0 / BUILD 068. Verified via a real headless-browser (Playwright/Chromium) drive of the actual index.html, clicking through real map nodes (not shortcuts) for every transition: a full lower-lane run (fight → rite → fight → boss) with a die action taken after the first fight (Strengthen face 10 to weight 2) still reading weight 2 at the third fight and at the boss, unchanged through every intervening slot; a card picked at the third fight's reward present in ownedCards at the boss; the heal rite capping at exactly 70 from both 55 and 65 starting HP (never above, never a simultaneous die action); the lower lane's nodes staying non-clickable (0 clickable elements) both immediately after the upper lane was chosen and after returning to the map from upper's first fight; the boss preview (150 HP, 16-28 intent) identical before entering and the moment its fight actually begins; beating the boss producing outcome:'won', the existing VICTORY banner, and no reward flow (dieActionStep/cardRewardStep both null); dying to an elite (100 HP, 12-22 intent, confirmed against the slot data) producing outcome:'lost' and the existing DEFEAT banner with dev step-control buttons disabled; New Run always available (even mid-run and after a loss) and resetting HP to 70, the die to fresh weight-1 faces, and ownedCards to the exact starting 10-card list; #startGameBtn correctly labelled "New Run" and #devRestartFightBtn correctly living inside #devStepControl, disabled on the map and enabled only mid-fight — zero console errors across every check. A separate geometry pass at 1400x900 and 1600x1080 confirmed #fightScreen's four children still show zero row overflow (identical to pre-068 die-row geometry) and neither #mapScreen nor .left-col overflow horizontally at either viewport. Not yet eyeballed by a human in a real windowed browser. The fight phase order, the card pool, the mod pool, Nat 20/Nat 1 behaviour, dealDamage()/dealBlock(), the poison ticks, the weight display, and the Strengthen filter were not touched — confirmed by direct re-read of each. One scope note volunteered per the prompt's own instruction to flag anything larger than it reads: this build touches more surface area than any prior single build (new run-level state, a new top-level screen, a new slot-dispatch system, a new reusable-panel entry point, and edits to init()/resetFight()/runPhase()'s win-loss guard/refreshInspector()) — it was built and verified as one coherent system because the prompt's own success criteria required a full start-to-finish run, but it is the largest single build in this project's history and is called out here rather than silently treated as routine.

Stage 2.1 — visual pass on the act map, plus a fightScreen/controls-row overlap regression fix (BUILD 069). Presentation only, per the prompt's own explicit framing — no run state, slot dispatch, or enemy numbers were touched; confirmed by a full re-run of BUILD 068's own test suites showing byte-identical results (same paragraph below). Layout regression first, per the prompt's own ordering: #fightScreen's min-height:0 (added in BUILD 068 to mirror .left-col's own rule) let that wrapper shrink below what its children's own min-heights (.combat-panel's 436px each, the BUILD 040/041 figure) actually needed; with no overflow:hidden on the wrapper, the children then rendered past its shrunk bottom edge, and .controls-row — a later sibling positioned relative to that same shrunk box — painted directly over the overflowed hand row, exactly matching "End Turn visibly overlaps the cards." Root-caused by dumping every .left-col child's actual getBoundingClientRect() (not guessed from the CSS alone): before the fix, #fightScreen's own box read top:38/bottom:620 while its own hand-row child read top:1078/bottom:1210 — physically impossible for a normal child unless the parent's box had already been forced smaller than its content, which is exactly what min-height:0 plus the default flex-shrink:1 did. Changed #fightScreen from min-height:0 to flex-shrink:0, restoring the exact BUILD 041 contract ("let panels take their natural size, let .left-col scroll for any excess") instead of the newer wrapper silently reintroducing the shrink-to-fit failure mode BUILD 041 had already closed. Re-measured after the fix at both required viewports (1400x900, 1600x1080): #fightScreen's box now reads bottom:1210 (matching its hand-row child exactly, no overflow) and .controls-row starts at top:1216, a clean 6px gap (the existing .left-col gap value) below it, zero overlap, all 5 hand cards individually confirmed fully within #handRow's bounds and not covered by any later element. Map centring and connections: renderMapScreen()'s DOM was restructured into a single centred .map-composition flex row — [START marker] [fork column] [.map-lanes, two rows] [merge column] [BOSS node] — with the fork/merge columns using align-self:stretch so their one vertical line's height is simply "however tall .map-lanes is" (recomputed by the browser every layout, no per-viewport pixel math anywhere in this build), and each lane row carrying its own absolutely-positioned ::before/::after stubs reaching into the zero-gap space next to those columns to visually complete the fork/merge join. Inter-node .map-connector elements (a fixed 20x2px bar) were added between each of the 3 nodes in a lane row, and both the stubs and connectors read one of three states via a lane-level modifier class (map-lane-committed/map-lane-abandoned/neutral, computed once per lane in the new mapLaneStateClass() and applied to both the row and every connector inside it) — the chosen lane's whole path (fork stub through every connector to the merge stub) reads --nat, the abandoned lane reads --muted, and before any lane is picked everything reads the plain --line neutral, so "a completed or committed path reads differently from one no longer available" is literal, not just implied by the nodes at its ends. Measured centring directly (not eyeballed): .map-composition's own centre-x lands exactly on #mapScreen's centre-x at both 1400px and 1600px widths (0px offset, computed via getBoundingClientRect on both elements) — the boss preview row (HP/intent/die) sits directly beneath the composition as its own centred block, sharing the same horizontal centre rather than trailing to one side. Node restyling reuses the existing colour vocabulary only, no new palette, via a new mapNodeStateClass() shared by lane nodes and the boss node alike: completed (--blank border/text plus a "✓ " prefix, the same tone already used for "present but inactive" elsewhere), current (solid 2px --nat border — the single next slot on an already-chosen lane), choice (dashed 1px --nat border — the fork's two pre-choice options, same colour as current since both are genuinely clickable now, dashed vs solid is the only distinction), inert (dim --muted, opacity 0.45 — the abandoned lane, and any not-yet-unlocked slot on the chosen one; the label itself is never hidden or truncated in any state, only the frame changes). The START marker reuses map-node-current/map-node-completed as a plain (non-button) div reading "you are here" before a lane is chosen and "already behind you" after. Boss preview: byte-identical HTML/text to BUILD 068 (HP, intent band, the full 20-row die via the same renderDieList() call with a null forceRollFn) — confirmed via direct string/count comparison, only its container position moved, into the new dedicated .map-boss-preview-row beneath the composition. End Turn: refreshInspector() now also sets #endTurnBtn's own style.display to 'none' outside gameState.run.screen === 'fight' (previously only .disabled was toggled, so the button stayed visible-but-inert on the map) — confirmed absent from the map's rendered DOM display, present and functional on the fight screen. Build stamp now STAGE 2.1 / BUILD 069. Verified via a real headless-browser (Playwright/Chromium) drive of the actual index.html: the fightScreen/controls-row geometry check above at both viewports; a full re-run of BUILD 068's own two test suites (the complete lower-lane run — fork choice, a die action persisting to the boss, a card reward persisting to the boss, the heal rite capping at exactly 70, the boss preview's static config, the VICTORY banner with no reward flow after a boss win — and the upper-lane elite death, DEFEAT banner, New Run recovery) both reproducing results identical field-for-field to their BUILD 068 runs, confirming no run behaviour changed; direct computed-style checks on a chosen lane's nodes and connectors showing exactly the designed rgb values for --nat/--blank/--muted on the committed vs abandoned lane; #endTurnBtn confirmed display:none on the map and visible on the fight screen — zero console errors across every check. Not yet eyeballed by a human in a real windowed browser. Run state, slot dispatch, enemy numbers, the fight phase order, the card/mod pools, and everything inside a fight beyond the button row's position were not touched — confirmed by direct re-read of every changed section.

Stage 2.2 — hover descriptions on reward/die-action choices, plus a reward duplicate fix (BUILD 070). Card reward hover: each card reward button now carries a child .hover-tip span reading CARD_EFFECT_TEXT[cardId] — the exact constant renderCardButtons() already reads for hand-row cards, byte-identical strings, no new copy written for any of the 15 pool cards (all 15 confirmed present in that constant by direct cross-reference against config.cardPool's own key list). Die action hover: explicitly NOT implemented, per the prompt's own stop-rather-than-invent instruction — grepped the whole file for a mod description/effect-text source (a MOD_EFFECT_TEXT-style constant, or a description: field on any of the eleven mod objects) and found neither; every mod's only effect text lives inside its effect() function's own log() calls, which is exactly the "log lines only" case the prompt named as out of scope. This covers both places the prompt asked about — the Load list (mod name only, via modDisplayName()) and the Strengthen face picker (same modDisplayName() call, same gap) — both are equally blocked by the same missing data, not two separate gaps. The hover mechanism itself (.hover-tip: position:absolute under its own button, opacity/visibility toggled by button:hover, pointer-events:none, max-width:220px so a long effect string wraps instead of stretching sideways into a neighbour) is built generically enough to cover mods the moment a description source exists, but no dieActionPanel button was actually given one. Reward duplicate fix: openCardRewardScreen() drew 3 cards via three independent Math.random() picks with replacement — the stale comment above it ("duplicates... allowed, since the pool is only 3 entries") was true when written but the pool has since grown to 15 (BUILD 051-059), leaving the draw logic never updated to match, producing exactly the observed Purge/Orison/Purge case. Fixed with shuffle(poolIds.slice()).slice(0, 3) — the identical shuffle-then-slice pattern dieActionChooseLoad() already uses for its own 3-mod offer — so all three reward slots are now guaranteed distinct; pool, DIE_ACTION_EXCLUDED_MOD_IDS-equivalent (there is none for cards, unchanged), and the offer count (3) were not touched. Checked the die action Load list for the same bug and found it did not have it: dieActionMods = shuffle(eligible.slice()).slice(0, 3) was already shuffle-based since it was first written, confirmed by direct re-read — nothing there needed fixing, reported here rather than silently left unmentioned. Build stamp now STAGE 2.2 / BUILD 070. Verified via a real headless-browser (Playwright/Chromium) drive of the actual index.html: hovering a reward card shows its tooltip with the correct CARD_EFFECT_TEXT string (e.g. Retribution → "damage = block, capped at 12"); all four reward-row buttons' bounding rects captured before and after the hover are byte-identical (JSON-equal), confirming no layout shift; the hovered tooltip's own rect checked against every sibling button's rect shows zero overlap with any of them; 10 consecutive openCardRewardScreen() calls each produced 3 distinct card ids (zero duplicates across all 10); 10 consecutive dieActionChooseLoad() calls likewise showed zero duplicates, confirming the Load list's pre-existing correctness; Load list buttons confirmed to carry no .hover-tip child (correctly not implemented) — zero console errors across every check. Not yet eyeballed by a human in a real windowed browser. Run state, slot dispatch, enemy numbers, the map, the fight phase order, and every card/mod effect were not touched — confirmed by direct re-read.

Stage 2.3 — hover descriptions on the die action Load list and both die rows (BUILD 071). Mod descriptions (the blocker BUILD 070 left open) were derived from each mod's own log() text, not authored: a new MOD_DESCRIPTION constant, one entry per pool mod, built by stripping each effect()'s '[MOD] name: ' log prefix and, only where a number is genuinely dynamic per-trigger (Zeal's stacking bonus, Virulence's doubled total), substituting the same static base figures already written into that mod's own dealDamage()/updateEnemy() call — e.g. Zeal reads '10 damage (bonus +B...)' in its log but the code's own base is a hardcoded 10 and its own growth is a hardcoded +4, so the description reads '10 damage, permanently gains +4 damage per trigger' with no new fact introduced. Vigil's registration log ('registered, block scales with cards held at end of turn') is used almost verbatim, dropping only the log-specific 'registered, ' prefix — this is the exact phrase the prompt itself quoted as an example, confirming the derivation approach. All eleven pool mods had usable log text; none needed to be left undescribed. A parallel NAT_DESCRIPTION constant covers the player's two Nat faces: face 20 reused the NAT 20 AND NAT 1 — THE ORDAINED section's own wording (every loaded face triggers, ascending order); face 1 reused BUILD 067's own Penitence onset log line almost verbatim. The enemy's Nat faces were deliberately left out of NAT_DESCRIPTION and get no hover text: both ENEMY_NAT_ONE and ENEMY_NAT_TWENTY still log 'not yet implemented' (BUILD 054), so there is no real behaviour to describe, per the prompt's own instruction to leave stubs alone. A genuine pre-existing gap was found and is reported here rather than fixed: enemy_buff_poison (the enemy's POISON face) has no registered listener for ENEMY_BUFF_TRIGGER anywhere in the file — grep-confirmed the hook is dispatched (callListeners('ENEMY_BUFF_TRIGGER', {buffId: face.modId}) in resolveEnemyRoll()) but nothing is listening, so rolling that face currently does nothing at all despite being coloured and labelled as a real buff; the only text describing its intended behaviour ('on trigger, apply 1 poison stack to player') lives in this file's own TEST ENEMY section as design intent that was never implemented in code. Describing that unrealized intent as if it were real behaviour would have been inventing a description for something that doesn't happen, so it was left with no hover text instead — this is flagged as a discovered implementation gap, not fixed, since fixing it would be a real behaviour change and out of scope for a presentation-only build. Blank faces were a deliberate judgement call, reported rather than decided silently: left un-hoverable. Eighteen identical '2 block on roll' tooltips across two dice would be repetitive noise for a fact the row's own blank '—' already communicates passively, and the die rows are dense (the exact BUILD 040/041/065 density concern) — quiet was chosen over complete. A shared faceHoverText(face) helper (new) resolves a face to its description or null (never inventing one), appending the face's own '×N' when weight is above 1, in the same notation the row's own weight badge already uses — 'the row already shows ×N and the two belong together' is now literal, not just adjacent. This flows into renderDieList() (used by both dice and the map's boss-preview die, so all three benefit uniformly from the same logic with no extra code) via the exact same .hover-tip component and CSS class BUILD 070 built for the card reward panel — the box's own appearance (background/border/text/font) is untouched, only its position differs: die-action buttons still pop the tip downward (nothing important below them), but a die row has another die row immediately beneath it, so its tip instead pops out to the row's right, vertically centred, via a new .die-row .hover-tip positioning override — .die-col has room next to .stats-col, and position:absolute means this can never affect document flow regardless of direction. The Load list and Strengthen face picker both gained the same .hover-tip child on their buttons, reading MOD_DESCRIPTION (Load list) or MOD_DESCRIPTION-or-NAT_DESCRIPTION (Strengthen picker, since face 20 can appear there with no mod at all, per BUILD 065's rule change). Build stamp now STAGE 2.3 / BUILD 071. Verified via a real headless-browser (Playwright/Chromium) drive of the actual index.html: face 6 loaded with Smite at weight 3 shows a hover tooltip reading '16 damage (×3)' with zero overlap against the row directly beneath it, and all twenty player-die rows' bounding rects are byte-identical with and without the hover active, confirming no layout shift; face 20 and face 1 show the designed Nat wording; a blank face and both enemy Nat faces and the enemy POISON face all confirmed to carry no .hover-tip child at all; the Load list forced to offer Virulence/Fervour/Ordain each showed their correct MOD_DESCRIPTION text on hover with the button's own bounding box unchanged before vs after — zero console errors across every check. Not yet eyeballed by a human in a real windowed browser. The roll/weight mathematics, every mod's actual effect(), run state, the map, the card pool, and the fight phase order were not touched — confirmed by direct re-read of every changed section.

Stage 2.4 — the player die alongside the boss die on the map, and during the die action (BUILD 072). Map: the boss-only .map-boss-preview-row was generalised to .map-die-preview-row (renamed, CSS otherwise untouched) and now holds a player-die half before the existing boss half — #mapPlayerPreview ("YOUR DIE" + live player HP, the exact same innerHTML shape #bossPreview already used for HP/intent) and #mapPlayerDieList, separated by a plain 1px .map-die-preview-divider hairline (var(--line), no new colour). Both halves render via the identical renderDieList(containerId, faces, null) call the boss preview already used — no third die renderer, confirmed by grep showing renderDieList() has exactly one implementation, called from three sites now instead of two (fight screen, map — both dice, die action screen). Because it's the same function, both dice on the map keep everything BUILD 065/071 already gave them — face numbers, mod names, ×N weights, and every hover-tip description — for free, with no extra code; verified directly by hovering a mod face on the map's own player die and confirming the same MOD_DESCRIPTION text (and same .hover-tip box) the fight screen already shows. Die action screen: renderDieActionPanel() gained a showDiePreview flag, set only inside the 'load_pick_mod' and 'strengthen_pick_face' branches (the two steps the prompt named explicitly — "choosing a mod to load, or a face to strengthen"), which appends a third .die-col ("YOUR DIE", id dieActionDieList) above the choice row via the same renderDieList() call, before that step's own buttons; the initial 'choose' (Load/Strengthen/Skip) step and the 'load_pick_face' step (choosing which blank face, once a mod is already picked) do not show it, since the prompt named only the other two steps — flagged in the completion message as a possible narrow reading worth a second look, not decided silently. Confirmed and reported per the prompt's own request, not fixed: Load can never overwrite an already-loaded face — dieActionChooseLoad()'s picker at the 'load_pick_face' step filters gameState.die.faces to f.modId === null only (read directly, this was never a bug, it's the literal implementation of "Load: place a mod on a blank face" from SCOPE — V1), confirmed by loading a mod onto face 2 and re-checking the offered face list, which no longer included Face 2. Build stamp now STAGE 2.4 / BUILD 072. Verified via a real headless-browser (Playwright/Chromium) drive of the actual index.html: the map shows both 20-row dice side by side (sideBySide confirmed via bounding-rect comparison), the composition's own centring is unchanged from BUILD 069 (0px off-centre, re-measured); hovering a loaded face on the map's player die shows its real MOD_DESCRIPTION text with zero layout shift across the whole preview row (every child's bounding rect byte-identical with/without the hover); the die action screen shows a 20-row die during both load_pick_mod and strengthen_pick_face, shows none during the initial choose step, and the die preview never overlaps the button row beneath it; loading a mod onto face 2 then re-opening the Load face picker correctly omits Face 2 from the offer — zero console errors across every check. Not yet eyeballed by a human in a real windowed browser. Run state, slot dispatch, enemy numbers, roll/weight mathematics, mod effects, the card pool, and the fight phase order were not touched — confirmed by direct re-read of every changed section.

Stage 2.5 — a shared opening fight between Start and the fork (BUILD 073). buildAct() gained an eighth slot, opening: { type: 'fight', label: 'Fight', enemy: { hp: 50, intentMin: 4, intentMax: 12 } } — the exact 'fight' type every normal/elite/boss slot already uses, dispatched through the same unmodified SLOT_HANDLERS table, so it grants a card reward and a die action on victory with zero special-casing, exactly as the prompt required. gameState.run.currentSlot gained a third possible value, the string 'opening' (alongside null and {lane,index}/'boss'), meaning "the shared opening fight is next" — startNewRun() now sets currentSlot: 'opening' instead of null, so a fresh run opens on this fight rather than at the fork. enterSlot() gained an 'opening' branch (mirroring its existing 'boss' branch — both are single slots living directly on act, not inside an upper/lower array) and advanceRun() gained a matching branch that marks act.opening.completed and sets currentSlot back to null (the fork) — lane stays null throughout, so resolving the opening fight is what first makes the fork's two options real choices. That gating is explicit: renderMapScreen()'s isForkChoice condition, previously just run.lane === null && index === 0, now also requires act.opening.completed, closing what would otherwise have been a visible bug (both lane-1 nodes reading as immediately clickable from the very first render, before the opening fight had even happened). The Start marker's own completed/current state moved from being keyed off run.lane to being keyed off act.opening.completed, since the opening fight — not the fork — is now the thing directly ahead of Start. Map rendering: a new Start→Opening .map-connector (the same plain node-to-node connector every lane segment already uses, left at its default neutral colour — there's no "abandoned" alternative for a mandatory shared segment, so no committed/abandoned styling was added here) plus the Opening node itself (button, .map-node-opening, using the same mapNodeStateClass() every other node already reads) were inserted between the existing Start and fork elements; the margin that used to create the Start→fork visual gap moved from .map-start-node to .map-node-opening, so the fork's own spacing is pixel-identical to before, just one stop further along. Both dice, the boss preview, and every hover-tip are untouched — renderMapScreen()'s die-preview row and renderDieList() itself were not touched at all, confirmed by direct re-read. Report, not fix, per the prompt's explicit request: the "Choose a blank face for Zeal" panel visible at −10 HP with the run already logged lost is reachable only through the dev Skip to Die Action button, never through real play. Traced directly: runPhase()'s loss branch (gameState.player.hp <= 0) only calls updateRun({status:'loss', outcome:'lost'}) and log() — it never opens dieActionStep or cardRewardStep, and once a real win opens the die action screen, phase progression halts (the top-of-runPhase guard no longer re-enters, since run.status leaves 'active'), so a player cannot take further lethal damage while a real post-win panel is open. #devSkipToDieActionBtn, by contrast, calls openDieActionScreen() completely unconditionally — grep-confirmed it has no disabled-state entry anywhere in refreshInspector() (unlike devNextPhaseBtn/devBeginRollBtn/devRestartFightBtn, which are all gated on run.screen/run.status) — so it remains clickable at any time, including after a logged loss, and does exactly this when clicked. Confirmed empirically: forcing player.hp to −10 and running the loss branch left dieActionStep at null (loss opens nothing); clicking #devSkipToDieActionBtn immediately afterward set dieActionStep to 'choose' while run.outcome stayed 'lost' and player.hp stayed −10 — reproducing the exact observed scenario through the dev shortcut alone. Nothing about this was changed. Build stamp now STAGE 2.5 / BUILD 073. Verified via a real headless-browser (Playwright/Chromium) drive of the actual index.html at both 1400x900 and 1600x1080: a fresh run's map shows exactly 9 .map-node elements and 5 .map-connector elements (the new Start→Opening link plus the two pre-existing links inside each 3-slot lane), only the Opening node clickable, both fork options correctly disabled/inert until Opening is won; winning the Opening fight (50/50 HP, intent 4-12, confirmed against the slot data) flips currentSlot to null and both fork options become clickable; by the time the elite is reached (opening fight + one lane fight, each resolved with a card pick and a Strengthen), ownedCards has grown by exactly 2 and the Strengthened face's weight has risen by exactly 2, and the elite's own stats are untouched (100 HP, intent 12-22); the composition stays perfectly centred (0px off, re-measured) with no horizontal overflow at either viewport; both die previews (20 rows each) and the boss preview text are byte-identical to BUILD 072 — zero console errors across every check. Not yet eyeballed by a human in a real windowed browser. The card pool, mod pool, roll/weight mathematics, the fight phase order, the hover system, and every other enemy's numbers were not touched — confirmed by direct re-read of every changed section.

Stage 2.5 — BUILD 074, diagnosis of a reported opening-fight reward bug. Reported: winning the opening fight showed the die action screen but no card reward. This did not reproduce. Four findings, gathered before any code was touched, per the prompt's own ordering: (1) line-by-line trace — the opening slot's post-victory flow was compared directly against a normal lane fight's and the boss's by driving all three through the identical instrumented sequence (force the win, read state, click the real Skip button in the real DOM, read state, click the real reward Skip button, read state) and diffing the results: opening and the lane fight produced byte-identical dieActionStep/cardRewardStep transition sequences (null→'choose'→null then null→'choose'→null), differing only in currentSlot's own value (which slot you're on, not whether the flow ran) — zero divergence found anywhere in beginFightFromSlot(), the runPhase() win guard, openDieActionScreen(), closeDieActionScreen(), openCardRewardScreen(), or closeCardRewardScreen(); none of these five functions contain any conditional keyed on slot identity except the one named in finding (3) below. (2) None of the four listed failure modes occurred: the card reward was not skipped (cardRewardStep genuinely reached 'choose' with 3 real distinct options), not rendered-and-dismissed (it stayed open and interactive until explicitly clicked), not hidden behind another panel (direct DOM inspection showed #cardRewardPanel at display:flex with its real card buttons and hover-tips present), and the order was unchanged (die action first, then card reward, same as every other fight). (3) The BUILD 068 boss short-circuit (runPhase()'s win branch) reads if (gameState.run.currentSlot === 'boss') — a strict string comparison against the literal 'boss', grep-confirmed as the only such check gating the reward flow anywhere in the file (four other 'boss' string comparisons exist, all in map rendering or slot dispatch, none gating reward/die-action). The opening slot's own currentSlot value is the distinct string 'opening'; the two can never compare equal, so this condition structurally cannot fire for the opening slot — confirmed directly in the same instrumented trace, which showed dieActionStep correctly reaching 'choose' (not being skipped) immediately after the opening fight's win. (4) BUILD 073's own test, re-read directly: it force-won each fight, then — gated on dieActionStep !== null and cardRewardStep !== null actually being true, not assumed — clicked a real Strengthen target and called the real cardRewardPickCard() with a card actually drawn from the real cardRewardOptions array, then asserted gameState.player.ownedCards.length had grown by exactly 2 across two fights. This counts actual completed picks, gated on the real screen states, not deck size or "a reward panel exists somewhere" — if the reward had been skipped for the opening slot, the pick would have been skipped too (the test's own if (cardRewardOpen) guard), ownedCards would have grown by only 1, and the assertion would have failed. It did not fail because the flow genuinely worked, re-confirmed here three independent ways: a fully natural multi-turn combat playthrough (no forced HP, no scripted clicks — actual playCard() calls and phase advancement) reaching dieActionStep: 'choose' after a real card-play kill; a forced win resolved via real DOM button clicks (not scripted state pokes) reaching a fully rendered, populated card reward panel; and the side-by-side diff against a lane fight and the boss described in (1) and (3). Conclusion: no bug exists in the current code, and no fix was applied — "the smallest change that makes the opening fight grant a card reward exactly like any other fight" is zero changes, since it already does, byte-for-byte. Applying a change here would have been a change without a defect to justify it, which the prompt's own instructions rule out. Build stamp updated to STAGE 2.5 / BUILD 074 as instructed; no other index.html logic was touched. One process note: the prompt referenced a "PROCESS AUDIT" section of this file to read before starting; no such section exists here (grep-confirmed against every # heading in the file) — flagged rather than silently proceeding as if it had been read. Zero console errors across every diagnostic run.

Stage 2.6 — boss HP 150→100, and the enemy die removed from normals/elites (BUILD 075). Two independent dials, per the prompt's own ordering: buildAct()'s boss.enemy.hp changed from 150 to 100; boss.enemy.intentMin/intentMax (16/28), the elite's hp/intent (100/12-22), and every normal fight's hp/intent (70-85/6-18) are untouched, confirmed by direct re-read of the whole buildAct() body. Die-carrying is now a property of the enemy: gameState.enemy gained a hasDie boolean (default true, matching the pre-075 always-has-a-die behaviour at the schema level), and every slot's enemy config in buildAct() gained an explicit hasDie field — false for opening/every normal fight/the elite, true only for the boss. beginFightFromSlot() now reads hasDie: !!slot.enemy.hasDie straight off the slot into gameState.enemy, the same pattern every other per-slot enemy field (hp, intentMin/Max) already uses — a future enemy gets a die by setting this one flag true on its own slot config, not by restoring deleted roll/render code. Neither die.faces itself nor buildEnemyDieFaces() nor buildFreshPlayerDieFaces() were touched — the enemy die system and its 20-face structure are fully intact and still built once per run in startNewRun(), exactly as before; hasDie only gates whether that structure is ever rolled or rendered for the current enemy. Phase order: PHASE_ORDER itself is byte-for-byte unchanged (still seven phases, ENEMY_ROLL_PHASE still among them) — nextPhase()'s ENEMY_ROLL_PHASE handling gained a single if (gameState.enemy.hasDie) guard around its existing rollDie()/resolveEnemyRoll() call; for a dieless enemy the phase still fires ([PHASE] ENEMY_ROLL_PHASE and the [LISTENER] calling hook line still log, confirmed in the captured log below), it simply has nothing to roll, so neither rollDie() nor resolveEnemyRoll() — and none of their log lines — run. forceEnemyRoll() (dev-only) gained the same guard for robustness, though it can no longer be reached via a real click since no die panel exists to click on. Rendering: refreshInspector()'s enemy die-list handling now branches on hasDie — true renders exactly as before (renderDieList('enemyDieList', ...), unchanged call); false hides #enemyDieList (style.display = 'none', not innerHTML-cleared, so a stale panel-title/rows sit inert rather than being destroyed and needing rebuilding if hasDie ever flips back) and adds a new combat-panel-no-die class to #enemyPanel, which centres the panel and caps .stats-col at a fixed 320px (was flex:1, which would otherwise stretch to fill the entire panel width left behind by the hidden die-col — noticeably wider than every other stats-col, reading as a hole rather than a deliberate layout) — no new colours, just flex/width rules reusing the panel's own existing structure. Report, not fix, per the prompt's explicit request: with normals/elites/the opening fight all dieless, ENEMY_BUFF_TRIGGER, ENEMY_NAT_ONE, ENEMY_NAT_TWENTY, and enemy_buff_poison do NOT lose their caller outright — buildEnemyDieFaces() (unchanged, grep-confirmed byte-identical) still puts ENEMY_NAT_ONE on face 1, enemy_buff_poison on faces 5/10/15, and ENEMY_NAT_TWENTY on face 20, and the boss is still the one enemy with hasDie: true, so resolveEnemyRoll() still runs for the boss every ENEMY_ROLL_PHASE and can still reach all four exactly as before. What changed is who can reach them: previously any enemy (normal, elite, or boss) could roll into these four; now only the boss can, since normal/elite/opening enemies never roll at all. ENEMY_BUFF_TRIGGER itself still has zero registered listeners anywhere (the BUILD 071-discovered gap, unaffected by this build — enemy_buff_poison's dispatch through it still fires into a void regardless of which enemy rolls it), and ENEMY_NAT_ONE/ENEMY_NAT_TWENTY remain the same "not yet implemented" stubs they were before. None of the four were deleted or altered. Build stamp now STAGE 2.6 / BUILD 075. Verified via a real headless-browser (Playwright/Chromium) drive of the actual index.html across a full run: the opening fight (dieless) shows hp 50/hasDie false, its die panel hidden and combat-panel-no-die applied; a captured turn's full log shows [PHASE] ENEMY_ROLL_PHASE and its [LISTENER] line firing normally, followed immediately by [PHASE] ENEMY_ACT_PHASE with zero [ROLL]/[ENEMY ROLL] lines in between, and the enemy still deals its normal intent-based damage; both lane fights confirmed dieless with HP unchanged (70, 85); the boss confirmed hp 100/maxHp 100/intent 16-28/hasDie true, its die panel visible with all 20 rows, combat-panel-no-die absent from its panel; a fresh run's map preview shows the updated 'HP 100' text with its own die still rendering all 20 rows, confirming the boss's map preview is untouched by the HP change — zero console errors across the entire run. Not yet eyeballed by a human in a real windowed browser. The player die, the card pool, the mod pool, run state, slot dispatch, the map's node logic, and the hover system were not touched — confirmed by direct re-read of every changed section.

Stage 2.7 — a dev tool to jump to any map slot, plus the BUILD 074 Skip to Die Action guard fix (BUILD 076). Dev chrome only, per the prompt's own framing — no game logic, no numbers, no player-facing change. Every node the run isn't currently standing on (already completed, not yet unlocked, or on the lane never committed to) is now clickable via a new devJumpToSlot(laneName, index), reached from a new shared attachDevJumpIfEligible(node, laneName, index) wired into all three node-creation sites in renderMapScreen() (opening, the two lane loops, boss) in place of their old node.disabled = true fallback — the genuinely-current/fork-choice branch (the real click path) is completely untouched. devJumpToSlot() writes exactly the run state a played run would have left — every slot strictly before the target on its path marked completed, run.lane set to the target's own lane (reassigning it if a different lane was previously committed, which the prompt explicitly allows), run.currentSlot set to the target — then calls the exact same enterSlot(laneName, index) every real arrival calls, which dispatches through the same unmodified SLOT_HANDLERS table (beginFightFromSlot() for a fight, openRiteScreen() for a rite) — no parallel path, no bypass, confirmed by direct re-read that neither function was touched. Jumping to the boss with no lane yet chosen defaults to completing the whole 'upper' lane (an arbitrary but deterministic choice, since reaching the boss legitimately requires some lane to be fully walked); jumping to a lane slot always completes that same lane's own earlier slots, never the other lane's. No fight along the way actually runs, so ownedCards/die/HP are left exactly as they already are — confirmed empirically (jumping straight to the boss from a fresh run left playerHp at 70 and ownedCards at the starting 10, with the boss itself arriving at exactly 100 HP/16-28 intent/hasDie true/its full 20-row die, byte-identical to a boss reached by playing three real slots). Guarded properly: devJumpToSlot() itself no-ops when gameState.run.outcome !== 'active' (checked first, before any state is touched — confirmed by calling it directly against a lost run and diffing gameState.run before/after, byte-identical), and attachDevJumpIfEligible() renders the node fully disabled (no listener attached at all, not just visually) under the same condition, so a won/lost run shows every non-current node as plainly inert, not dev-jumpable. Visual distinction, reusing only existing vocabulary: dev-jump nodes get a new .map-node-dev-jump class — --muted (the same colour .map-node-inert already used for "not a real option") with a dotted border and a pointer cursor, deliberately never the --nat gold that means "pick me" on a genuine choice or current node — plus a .hover-tip (the same BUILD 070 component, reused, not reinvented) reading 'Dev: jump here — skips earlier slots, grants no rewards'. Verified directly: with a fresh run, every non-opening node reads isChoice:false, isDevJump:true (0 real choices exist yet, since the opening fight hasn't been won); once the opening fight is won, exactly 2 real .map-node-choice nodes exist (the fork) alongside 6 .map-node-dev-jump nodes; after a real lane pick, every node on the un-chosen lane reads isChoice:false/isCurrent:false/isDevJump:true — the unchosen lane is never a real choice, exactly as BUILD 075 left it, only ever reachable via the dev tool. Skip to Die Action: the BUILD 074-found gap (fired unconditionally, no run.status/outcome check, no disabled entry — what produced the phantom "Choose a blank face for Zeal" panel at −10 HP) is closed with exactly the two lines the prompt asked for: a guard at the top of its click handler (if (gameState.run.status !== 'active' || gameState.run.outcome !== 'active') return;) and a matching disabled entry in refreshInspector() mirroring the pattern devRestartFightBtn already uses. Skip to Card Reward was deliberately left untouched — the prompt named only Skip to Die Action, and fixing the other would have been unrequested scope. Report, not fixed, discovered while verifying the loss-guard: the genuinely-current node's own real click path (the unmodified branch — e.g. the opening fight while it's still gameState.run.currentSlot) has no run.outcome/status guard of its own and remains clickable even after the run is already won or lost, since that branch was never touched by BUILD 068-075 and isn't a "dev skip" this build's guard requirement named. This is a distinct, pre-existing gap from the one just closed — flagged here rather than fixed, since it falls outside "fix that Skip to Die Action gap" and outside "do not change any other logic." Build stamp now STAGE 2.7 / BUILD 076. Verified via a real headless-browser (Playwright/Chromium) drive of the actual index.html: dev-jumping straight to the boss from a fresh run produces lane 'upper', currentSlot 'boss', all three upper slots completed, opening completed, screen 'fight', enemy hp/maxHp 100/100, intent 16-28, hasDie true, 20 die rows — force-winning that boss then produces outcome 'won'/status 'win' with no reward flow, identical to a boss reached by playing; dev-jumping to a rite slot (upper index 2) correctly marks upper[0]/upper[1] completed (not the target itself), opens the real rite panel (riteStep 'choose', #riteScreenPanel visibly flex) via openRiteScreen(), and leaves screen at 'map' (rites never switch to the fight screen); after a forced loss, devSkipToDieActionBtn is disabled and a forced click leaves dieActionStep at null, every non-current map node is disabled, and calling devJumpToSlot() directly leaves gameState.run byte-identical before and after — zero console errors across every check. Not yet eyeballed by a human in a real windowed browser. Enemy numbers, the player die, the card and mod pools, run state beyond marking skipped slots complete, and the fight phase order were not touched — confirmed by direct re-read of every changed section.

Next action is a planning decision on the FIGHT RESET spec change BUILD 049 made (does hp-restore-on-Restart-Fight stay permanent, or was permadeath-with-no-recovery the intended final design), a planning decision on how to actually get the dev tools above the fold (BUILD 043 closed the gap by ~10% but left ~590px/~410px still below it), plus the standing manual browser playtest across the outstanding BUILD 014-029 UI/turn-flow passes, the BUILD 030 poison tick, the BUILD 031 Blight/Virulence pass, the BUILD 033 force-roll timing fix (which supersedes BUILD 032), the BUILD 034 Sanctuary mod, the BUILD 035 Vigil mod, the BUILD 036 Restart Fight pause, the BUILD 037 init() pause fix, the BUILD 038 die action loop, the BUILD 039 dev tool addition, the BUILD 040-041 hand-row bleed-through fix, the BUILD 042 player poison decay fix, the BUILD 043 sizing pass, the BUILD 044 Zeal mod, the BUILD 046 Fervour mod, the BUILD 047 Ordain mod, the BUILD 048 reward-pool fix, the BUILD 049 Restart Fight fix, the BUILD 050 card reward screen, the BUILD 051 tier-1 card pool, the BUILD 052 Communion fix plus tier-3 conditionals, the BUILD 053 mid-CARD_PHASE victory fix, the BUILD 054 logging fix, the BUILD 055 Retribution card, the BUILD 056 roll-result exposure, the BUILD 057 Covenant card, the BUILD 058 Rapture card, the BUILD 059 Orison card, the BUILD 060 classId fix, the BUILD 061 correction, the BUILD 062 Zeal counter migration, the BUILD 063 lethal-mid-phase fix, and the BUILD 064 shared-helpers pass (plus its same-build target-parameter correction) — the full four-item audit remediation queue is now closed, no further audit-remediation items queued — not further building.

---

